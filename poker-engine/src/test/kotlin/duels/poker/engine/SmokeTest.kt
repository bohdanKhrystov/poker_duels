package duels.poker.engine

import org.junit.jupiter.api.Assertions.assertTrue
import org.junit.jupiter.api.Test

class SmokeTest {
    @Test
    fun testRunnerIsWired() {
        // This test proves the JUnit 5 platform is configured and assertions are wired
        assertTrue(true)
    }
}
