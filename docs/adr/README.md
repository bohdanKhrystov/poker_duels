# Architecture decision records

One file per significant decision. Small, dated, immutable.

An ADR is never edited to change its decision. If a decision is reversed, write a new ADR that
supersedes the old one and mark the old one `Superseded by ADR-NNNN`. The wrong turns are as
valuable as the right ones — especially for Product B.

## When to write one

Write an ADR when a choice:

- constrains something outside its own module,
- was contested, or had a plausible alternative,
- will make a future reader ask *"why is it like this?"*.

Do not write one for choices the code already makes obvious.

## Naming

`ADR-NNNN-short-kebab-title.md`, sequential, never reused.

## Template

```markdown
# ADR-NNNN — Title

- **Status:** Proposed | Accepted | Superseded by ADR-MMMM
- **Date:** YYYY-MM-DD

## Context
The forces at play. What makes this a real decision.

## Decision
What we are doing. Present tense, unambiguous.

## Consequences
What this buys, what it costs, and what it forecloses.

## Alternatives considered
Each with the reason it was not chosen.
```

## Index

| ADR | Title | Status |
| --- | --- | --- |
| [0001](ADR-0001-event-sourced-engine-contract.md) | Event-sourced engine contract | Accepted |
| [0002](ADR-0002-server-authoritative.md) | The server is authoritative | Accepted |
| [0003](ADR-0003-technology-stack.md) | Technology stack | Accepted |
| [0004](ADR-0004-branching-and-ticket-workflow.md) | Branching and ticket workflow | Accepted |
| [0005](ADR-0005-analysis-behind-an-interface.md) | Hand analysis sits behind an interface | Accepted |
| [0006](ADR-0006-mandatory-review-gate.md) | Every task ends in a reviewed, merged pull request | Amended by 0007 |
| [0007](ADR-0007-token-lean-agent-workflow.md) | Token-lean agent workflow | Accepted — amended by 0068 (the three-file cap) |
| [0008](ADR-0008-loser-mucks-at-showdown.md) | The loser mucks at showdown | Accepted |
| [0009](ADR-0009-match-events-are-their-own-hierarchy.md) | Match events are their own hierarchy | Accepted |
| [0010](ADR-0010-engine-takes-a-serialization-dependency.md) | The engine may depend on kotlinx.serialization | Accepted |
| [0011](ADR-0011-postgres-in-v01.md) | PostgreSQL lands in v0.1 | Accepted — amends 0003 |
| [0012](ADR-0012-device-bound-anonymous-profiles.md) | Anonymous profiles, bound to a device | Accepted |
| [0013](ADR-0013-disconnect-grace-period.md) | A dropped connection gets a grace period, then folds | Amended by 0023, 0028 |
| [0014](ADR-0014-duel-coin-economy.md) | The winner takes a coin, the loser gives one, a draw pays nothing | Accepted |
| [0015](ADR-0015-a-draw-writes-two-result-rows.md) | A draw writes two result rows of zero, not no rows | Accepted |
| [0016](ADR-0016-a-room-is-serialised-by-its-own-mutex.md) | A room is serialised by its own mutex, not by an actor | Accepted |
| [0017](ADR-0017-the-server-says-when-a-duel-ends.md) | The server says when a duel ends | Accepted |
| [0018](ADR-0018-a-second-socket-adopts-the-seat.md) | A second socket adopts the seat, and the first is closed | Accepted |
| [0019](ADR-0019-the-duel-table-records-hands-played.md) | The duel table records how many hands were played | Accepted |
| [0020](ADR-0020-typescript-protocol-from-serial-descriptors.md) | TypeScript protocol types are emitted from the serial descriptors | Accepted |
| [0021](ADR-0021-a-profile-gains-a-display-name.md) | A profile gains a player-chosen display name | Accepted |
| [0022](ADR-0022-the-room-code-is-the-invite.md) | The room code is the invite, and failed joins are budgeted | Accepted |
| [0023](ADR-0023-an-absent-seat-checks-when-nothing-is-owed.md) | An absent seat checks when nothing is owed, folds when facing a bet | Accepted — amends 0013; amended by 0028 |
| [0024](ADR-0024-design-follows-the-code-workflow.md) | Design follows the code workflow, in the repository, mirrored to claude.ai/design | Accepted — §2's "size" bounded by 0033 |
| [0025](ADR-0025-one-ticker-coroutine-drives-both-sweeps.md) | One ticker coroutine on the application scope drives both sweeps | Accepted |
| [0026](ADR-0026-vite-and-npm-drive-the-web-client.md) | Vite and npm drive the web client, and its checks are their own CI job | Accepted |
| [0027](ADR-0027-the-session-outranks-the-device-id.md) | A session token outranks a device id, and the handshake carries it | Accepted — amended by 0030, 0054; §1's session lifetime by 0062; §6's two numbers fixed by 0074 |
| [0028](ADR-0028-the-wire-names-an-absent-opponent.md) | The wire names an absent opponent, and marks every action taken for one | Accepted — §1's `ActedForAbsentSeat` renamed `ActedForAbsent` by 0071 |
| [0029](ADR-0029-a-display-name-is-unique-and-permanent.md) | A display name is unique, case-insensitively, and permanent once set | Accepted — amended by 0038, 0051 |
| [0030](ADR-0030-a-claim-adds-a-credential-and-moves-nothing.md) | A claim adds a credential and moves nothing; a sign-in swaps identity and moves nothing | Accepted — amended by 0037 |
| [0031](ADR-0031-an-optional-verified-recovery-email.md) | An optional recovery email, proven before it can do anything, and a handle that is not a name | Accepted — token lifetimes amended by 0062; §5's `reset-password` `422` amended by 0080 (the status stays, its *"when the token was good"* precondition goes, and §4 is what stands) |
| [0032](ADR-0032-react-subscribes-to-a-store-it-does-not-own.md) | React subscribes to a store it does not own, and the tab's one connection is wired at boot | Accepted |
| [0033](ADR-0033-component-anatomy-is-born-in-its-canonical-card.md) | A component's anatomy is born in its canonical card; the sheet holds the vocabulary | Accepted — bounds 0024 §2 |
| [0034](ADR-0034-the-value-gate-reads-css-string-aware.md) | The value gate reads CSS regions, string-aware, and fails rather than guesses | Accepted |
| [0035](ADR-0035-a-duel-is-a-freezeout.md) | A duel is a freezeout | Accepted |
| [0036](ADR-0036-an-account-is-offered-never-required.md) | An account is offered after a first win, and never required | Accepted |
| [0037](ADR-0037-the-device-is-a-credential-until-revoked.md) | The device stays a credential until the player revokes it | Accepted — amends 0030 |
| [0038](ADR-0038-a-name-is-screened-when-set-and-can-be-taken-away.md) | A display name is screened when set, can be taken away, and is burned when it is | Accepted — amends 0029; amended by 0051 |
| [0039](ADR-0039-v01-offers-no-account-deletion.md) | v0.1 offers no account deletion, and the schema keeps every answer open | Accepted |
| [0040](ADR-0040-a-duel-may-be-watched-without-hole-cards.md) | A duel may be watched live, minus every hole card | Accepted |
| [0041](ADR-0041-a-handle-and-a-password-are-the-only-credential.md) | A handle and a password are the only credential v0.1 ships | Accepted |
| [0042](ADR-0042-the-run-reports-itself-every-two-hours.md) | The run reports itself every two hours, over Telegram | Accepted |
| [0043](ADR-0043-a-rejection-closes-no-decision-point.md) | A rejection closes no decision point; the client keeps the turn and counts the refusal | Accepted |
| [0044](ADR-0044-a-rematch-is-one-intent-and-one-room-fact.md) | A rematch is one client intent and one room fact, and EPIC-02 ships the server half | Accepted |
| [0045](ADR-0045-presence-belongs-to-the-table.md) | Presence belongs to the table, not to reconnect, and EPIC-02 ships the server half | Accepted — §4 extended by 0047 |
| [0046](ADR-0046-the-table-says-away-timed-out-and-back.md) | The table says *away*, *timed out* and *back*, and names the server when it acts | Accepted |
| [0047](ADR-0047-a-protocol-version-is-claimed-in-a-ledger.md) | A protocol version is claimed in a ledger, and the second claim conflicts | Accepted — extends 0045 §4; §6's count amended by 0068 |
| [0048](ADR-0048-a-password-has-one-rule-and-it-is-length.md) | A password has one rule, and it is length | Accepted |
| [0049](ADR-0049-a-device-binding-is-a-row-and-revoking-is-final.md) | A device binding is its own row, and revoking one is final | Accepted — amends 0030 |
| [0050](ADR-0050-revoking-the-device-signs-out-everywhere-but-here.md) | Revoking the device signs the player out everywhere but here | Accepted — amends 0049 |
| [0051](ADR-0051-a-name-is-registered-before-it-is-held.md) | A name is registered before it is held, and a takedown is one function call | Accepted — amends 0029, 0038; amended by 0052, 0053 |
| [0052](ADR-0052-a-takedown-is-told-to-the-player-it-happened-to.md) | A takedown is told to the player it happened to, and to nobody else | Accepted — amends 0051 §1, §6 |
| [0053](ADR-0053-the-profile-says-the-name-was-removed.md) | The profile says the name was removed, in one correlated boolean | Accepted — amends 0051 §1 |
| [0054](ADR-0054-a-raised-argon2-cost-is-a-ledger-entry-and-a-rehash.md) | A raised Argon2 cost is a ledger entry and a rehash on the next sign-in | Accepted — amends 0027 §1 |
| [0055](ADR-0055-sign-up-is-budgeted-by-address-and-over-budget-says-so.md) | Sign-up is budgeted by address, and over budget says so | Accepted — extends 0048 §6; §2's `AttemptBudget` extended by 0074 with one method |
| [0056](ADR-0056-a-throttled-sign-up-says-so-and-keeps-what-was-typed.md) | A throttled sign-up says so, and keeps what was typed | Accepted |
| [0057](ADR-0057-a-cursor-names-the-filter-it-was-drawn-under.md) | A cursor names the filter it was drawn under | Accepted |
| [0058](ADR-0058-where-a-name-would-be-the-client-prints-no-name.md) | Where a name would be, the client prints *No name* | Accepted |
| [0059](ADR-0059-the-record-is-searched-when-the-player-submits.md) | The record is searched when the player submits, not while they type | Accepted |
| [0060](ADR-0060-the-record-is-its-own-screen-and-the-lobby-is-the-door.md) | The record is its own screen, and the first screen is the door | Accepted |
| [0061](ADR-0061-a-season-is-a-calendar-month-and-the-coin-never-resets.md) | A season is a calendar month, the ladder is a window over it, and the coin never resets | Accepted — builds on 0014; §3 amended by 0062 |
| [0062](ADR-0062-two-clocks-and-a-date-comes-from-java-time-clock.md) | The server has two clocks, and a date comes only from an injected `java.time.Clock` | Accepted — amends 0061 §3, 0027 §1, 0031 |
| [0063](ADR-0063-nothing-gates-a-place-and-the-farm-is-accepted-until-the-ladder-is-public.md) | Nothing gates a place on the ladder, and the farm is accepted until the ladder is public | Accepted — applies 0012's gate, narrows 0061 §4 by nothing, answers what 0058 parked |
| [0064](ADR-0064-tied-players-share-one-rank-and-row-order-is-not-a-ranking.md) | Tied players share one rank number, and the order rows sit in is not a ranking | Accepted — applies 0014; constrains 0061 §4's read and `DEC-061` |
| [0065](ADR-0065-the-ladder-hands-a-player-their-own-row.md) | The ladder hands a player their own row, and the profile strip keeps the all-time coin | Accepted — applies 0064 and 0063; settles what 0061 §6 parked; constrains `DEC-061` |
| [0066](ADR-0066-the-ladder-is-computed-per-request-and-a-walk-is-pinned.md) | The ladder is computed per request, and a walk is pinned to the instant it began | Accepted — applies 0061 §4, 0064 and 0065; extends 0057's cursor contract to a second ordering |
| [0067](ADR-0067-a-leaderboard-row-is-text-and-no-id-turns-into-a-profile.md) | A leaderboard row is text, and no id turns into a profile | Accepted — applies 0029 §7 in the opposite direction, with 0063 §2, 0039 and 0012; drops `STORY-0504` |
| [0068](ADR-0068-an-atomic-ticket-names-the-gate-that-forbids-splitting-it.md) | An atomic ticket names the gate that forbids splitting it | Accepted — amends 0007's three-file cap with one declared exemption, and 0047 §6's count; §§3 and 5 amended by 0069 |
| [0069](ADR-0069-the-blast-radius-is-probed-not-remembered.md) | The blast radius is probed, not remembered, and a ticket's size is its own Files table | Accepted — amends 0068 §3 (the twelve-file ceiling is deleted, not raised) and §5 (the enumeration becomes a probe); 0068 §§1, 2 and 4 stand; §3 replaced and §2 narrowed by 0070 |
| [0070](ADR-0070-a-blast-radius-is-complete-only-when-the-gates-are-green.md) | A blast radius is complete only when the gate set is green, and a merged gate may add the row that makes it so | Accepted — replaces 0069 §3 (one probe, the CI gate set by reference, run to green) and narrows 0069 §2 by one bounded exception; 0069 §§1, 4, 5 stand |
| [0071](ADR-0071-a-discriminator-is-its-kotlin-type-name.md) | A discriminator is its Kotlin type name, and the name moves rather than the gate | Accepted — amends 0028 §1 (the second subtype is renamed, type and `@SerialName` together); leaves `TASK-020210`'s 16-character gate unedited and unratified |
| [0072](ADR-0072-a-tab-remembers-its-room-until-the-player-leaves-it.md) | A tab remembers its room until the player leaves it, and the way back is what forgets | Accepted — deletes `TASK-031009`'s `DuelFinished` branch in `boot.ts`; makes `ADR-0044` §5 reachable; extends `ADR-0032` §2's `DuelClient` by one member and §3's event-handlers-only rule to it |
| [0073](ADR-0073-the-waiting-screen-says-back-to-the-lobby-and-the-room-stays-open.md) | The waiting screen says *Back to the lobby*, and the room stays open | Accepted — applies `docs/vision.md`'s *Positioning* and `ADR-0022`'s one way for a `WAITING` room to end; extends `ADR-0046`'s register by two strings and `ADR-0072` §5's way back to a second screen |
| [0074](ADR-0074-sign-in-is-ten-wrong-passwords-a-minute-reserved-before-the-hash.md) | Sign-in is ten wrong passwords a minute, reserved before the hash and refunded when right | Accepted — fixes the two numbers `ADR-0027` §6 left open and settles where the check sits; extends `ADR-0055` §2's `AttemptBudget` by one method (`refund`) and keeps §1's *over budget still counts* by shortening the window rather than forking the type |
| [0075](ADR-0075-the-mark-lives-as-long-as-the-absence-that-produced-it.md) | The server's mark lives as long as the absence that produced it | Accepted — settles the lifetime `ADR-0046` §4 left silent; applies `ADR-0046` §2's *never on a timer, never on a fade* and rejects `ADR-0043` §3's clear-on-`Snapshot` precedent on the server's own emission order |
| [0076](ADR-0076-a-screen-the-player-chose-has-an-address.md) | A screen the player chose has an address; a screen the server gave has none | Accepted — extends `ADR-0032` §3's `useSyncExternalStore` discipline to a second source and keeps its dependency refusal; collects on `ADR-0060` §4's navigation-ignorant screens and leaves `ADR-0072` §5's and `ADR-0073` §1's real navigations alone, so `ADR-0075`'s room-boundary hole stays unreachable |
| [0077](ADR-0077-no-sender-is-an-implementation-and-detachment-is-a-decorator.md) | No sender is an implementation, detachment is a decorator, and a test binds neither | Accepted — settles what `ADR-0031` §5 and §7 left open and applies `ADR-0025`'s scope rule to a second server-owned coroutine; transcribes `ADR-0031` §4's link rather than reinterpreting it, and raises `DEC-075` over its collision with `ADR-0076` §4 |
| [0078](ADR-0078-the-mail-is-the-only-real-check-on-an-address.md) | The mail is the only real check on an address, so the syntax rule refuses almost nothing | Accepted — fills the predicate `ADR-0031` §5's `400` promised and left blank, following `ADR-0048`'s shape; departs from `ADR-0048` §5 and `ADR-0029` §2 on canonicalisation, because an address is a delivery target rather than a string that is compared |
| [0079](ADR-0079-five-to-attach-ten-to-forget-and-the-attach-budget-is-the-only-mail-cap.md) | Five to attach, ten to forget, and the attach budget is the only cap on the mail it causes | Accepted — fixes the two pairs `ADR-0031` §5 left open and settles where each check sits; keeps `ADR-0055` §1's *over budget still counts* across a fourth caller, and records that §5's fifteen-minute suppression covers the reset path only |
| [0080](ADR-0080-the-password-is-judged-before-the-token-is-touched.md) | The password is judged before the token is touched, so a refusal costs no link | Accepted — amends `ADR-0031` §5's `reset-password` `422` (the status stays, its *"when the token was good"* precondition goes) and keeps §4's single `DELETE … RETURNING` and *"no read-then-write window"* byte-unchanged; applies `ADR-0048` §2's before-the-lookup placement and §3's pure-function policy at a third endpoint |
| [0081](ADR-0081-a-mailed-link-is-a-fragment-route-and-the-token-is-the-segment-behind-the-slug.md) | A mailed link is a fragment route, and the token is the segment behind the slug | Accepted — amends `ADR-0031` §4's URL shape only (its token, hour, single use and refusal of a query string stand byte-unchanged) and the two literals `ADR-0077` §6 transcribed; extends `ADR-0076` §1's address table by two rows, §5's `screen.ts` by one function, and answers the `DEC` §2 demanded for the first screen whose subject is a capability |
| [0082](ADR-0082-a-handle-is-read-from-a-proven-address-never-from-a-player-id.md) | A handle is read from a proven address, never from a player id | Accepted — sources the handle `ADR-0031` §6.2's `sendPasswordReset` requires and nothing could supply; **upholds** `Credentials.verifyCurrent`'s merged refusal of a reverse lookup and gates it for the first time; extends `RecoveryEmails` by one address-keyed member and leaves `PasswordResets.issue`'s `Boolean`, `verifiedOwnerOf` and `RecoveryMailer` byte-unchanged |
| [0083](ADR-0083-the-second-account-screen-is-sign-in-and-its-address-is-never-refused.md) | The second account screen is *Sign in*, and its address is never refused | Accepted — names the one screen `ADR-0076` §1 left unnamed and fills the last row of its address table for `STORY-0412`; coins nothing, widening §1's *"a word"* to a hyphenated compound the product already writes (`POST /api/auth/sign-in`) for that one word; applies `ADR-0060` §2's one-spelling-per-destination rule and `ADR-0076` §2's refusal to let an address claim entitlement; changes no server, wire, schema or ticket |
| [0084](ADR-0084-a-criterion-that-speaks-in-shell-belongs-in-verify.md) | A criterion that speaks in shell belongs in `verify:`, and one about an unbudgeted file may only refuse | Accepted — gives `CLAUDE.md` rule 3's *"done is an exit code"* and `tasks/README.md`'s *"checkable"* a mechanical meaning for the first time; applies `ADR-0070` §4's read/edit split to a ticket's own prose and `ADR-0068` §4's *two tickets* rule to a criterion; leaves `ADR-0068` §7 and `ADR-0070` §6 untouched |
| [0085](ADR-0085-not-again-is-this-browser-and-an-answer-spends-the-offer.md) | *"Not again"* is this browser, and an answer is what spends the offer | Accepted — amends `ADR-0036` §Consequences' *"It belongs on the profile"* (its premise does not hold: `pd.deviceId` shares the bucket, so a storage clear mints a fresh profile rather than resurrecting the prompt) and `ADR-0056` §5's *"and by nothing else"* parenthetical, whose holding — a `429` spends nothing — stands byte-unchanged; leaves `ADR-0036` §Decision, which was the human's `DEC-025` call, untouched; adds no column, field, endpoint or migration, and narrows `DEC-080` to the key and the module |
| [0086](ADR-0086-the-offers-answer-is-one-key-owned-beside-the-predicate-it-feeds.md) | The offer's answer is `pd.accountOfferSettled`, owned beside the predicate it feeds | Accepted — applies `ADR-0085` §1 and §2 and amends nothing; names the key, puts `web-client/src/result/account-offer-settled.ts` in sole possession of the literal, stores the sentinel `"1"` with an unrecognised value reading as *not settled*, exports no way to clear it, and writes the third row of `one-module-owns-each-storage-key.test.ts` verbatim. Fixes one clause for the wiring ticket — the accept-side write rides the anchor's click handler, `DuelResult`'s `onLeave` precedent — and records that `pd.roomCode` has no row in that gate |
| [0087](ADR-0087-forgot-your-password-is-a-door-on-the-sign-in-screen.md) | *Forgot your password?* is a door on the sign-in screen, not a screen of its own | Accepted — names the flow `ADR-0083` §6 deferred here and settles that it costs `ADR-0076` §1's address table no row and `screen.ts` no slug; **coins** one player-facing phrase, which `ADR-0083` did not have to, and records that as the cost; narrows `ADR-0081` §3's grant of a *forgot password* screen's address by leaving it unspent, and leaves `reset`, `verify` and every other sentence of §3 byte-unchanged; applies `ADR-0031` §5's unconditional `202` and `ADR-0078` §Consequences' *honest about a pending state* to the copy; changes no server, wire, schema or ticket |
| [0088](ADR-0088-the-two-browser-proof-is-a-written-hand-check.md) | The two-browser proof is a written hand-check, not a CI job | Accepted — closes `EPIC-03` with no browser runner; fixes the automated ceiling at `ADR-0032` §4's *"still jsdom, still no network"*; names the four things no test covers and accepts them; §1's **heading** amended by 0089 (*"here or in CI"* → in CI and between a pull request and `develop`), its body, §§2–5 and `DEC-024`'s answer byte-unchanged |
| [0089](ADR-0089-a-browser-drives-this-client-for-a-qa-round-never-for-a-gate.md) | A browser drives this client for a QA round, never for a gate | Accepted — amends `ADR-0088` §1's heading **only**, and preserves every cost term §1's ratio rested on as a standing condition: no browser dependency in any module, `build.yml` still two jobs, no pull request or `verify:` waiting on a case, and no citing a QA round as coverage; upholds `ADR-0002` at the test layer by letting the driver read anything and write nothing but `pd.roomCode`; adds the rule `EPIC-12` §Termination lacked — a failure that does not reproduce by hand is a **harness** defect, filed against `EPIC-12`, excluded from `B(N)`, and never repaired in production code; §2b's **heading and one sentence** amended by 0090 (*"No gate"* → *"No gate, and one caller"*; *"a human's command"* → *"the human's own message and nothing else"*), the rest of §2b and §§2a, 2c, 3, 4, 5, 6 byte-unchanged; permission extended to the `uat` focus by 0092, all six sections standing |
| [0090](ADR-0090-a-skill-may-write-the-catalogue-or-run-it-never-both.md) | A skill may write the catalogue or run it, never both in one turn | Accepted — amends `ADR-0089` §2b's heading and one sentence **only**, reading its *"not another skill invoking it as a step"* as a rule about **composition** rather than about automation alone: *"a human's command"* is a condition only if it means the **immediate caller**, and a human-typed composite is a cron whose clock is the length of its first half. Licenses `qa-cases`, an authoring skill that lands catalogue cases through ordinary reviewed PRs, runs no browser, dispatches neither `qa` nor `qa-manager`, and whose terminal act is a **report naming the command the human types next**; extends `ADR-0089` §5 where it was silent with a `source` column, so a case whose `expect` has no merged source is **not written** but registered as a `DEC` for the product owner; amends `docs/test-plan.md` §*Per-epic suites*' *"filled in when an epic is first tested, not before"* to admit an authored-then-tested path whose output is **provisional until its first round**, because a merged decision proves what was decided and not what shipped; gives §2b a one-command check over a declared three-file set and states which half of it no grep can catch; §2's declared three-file set grown to four by 0092 (`agents/uat.md`, mention-only) and to five by 0097 (`agents/audit.md`, mention-only) |
| [0091](ADR-0091-design-gets-no-agent-a-new-screen-owes-a-card.md) | Design gets no agent: a new screen owes a card, and adoption is gated where it is consumed | Accepted — applies `ADR-0024` and amends nothing, all five of its sections byte-unchanged; adds no agent and no skill to the roster; puts the one rule the undesigned screens lacked in the planner's split procedure — a story adding a `Screen` member names its card in `## Design notes` or its split's first ticket *is* the card — and generalises `EPIC-06`'s interactive deviation into mint-interactively / compose-by-dispatched-ticket with the human's visual verdict allowed to trail the merge; keeps `check-drift.sh` out of `web-client/` (adoption is gated in the consumer's own CI job, where three guards already live and a fourth — no raw length in a Tailwind arbitrary value, measured at 25 occurrences of four values — is added); reopens `EPIC-06` with the six-slug retrofit story and records that `ADR-0024`'s generator trigger has fired; §5's collection vehicle narrowed by the human's run-now call recorded in 0092 — UAT rounds may file the missing cards, the retrofit story covers the remainder — §5's register and §§1–4 byte-unchanged |
| [0092](ADR-0092-a-uat-round-files-what-a-source-settles-and-asks-the-rest.md) | A UAT round files what a merged source settles, and asks the product owner the rest | Accepted — extends `ADR-0089`'s permission to a `uat` focus of the **same** cycle with all six of its sections standing as written (a `shot` verb captures screenshots over CDP with Node built-ins only, never committed, **no image-diff tooling ever**; §4 transposed to *observable by a human looking*; §2c gains the corollary that no round may be cited as what made the product *ready*); amends `ADR-0090` §2's declared set from three files to four (`agents/uat.md`, mention-only) and nothing else in it; applies `ADR-0024` §3 and `ADR-0091` §§1–4 byte-unchanged, recording the human's run-now narrowing of §5's vehicle; adds the `uat` observer to the roster, keeps one manager, one skill and one ledger; and gives `B(N)` its second and third exclusions — missing cards and decision-born improvements — plus a baseline rule for newly-carded screens and a verdict line qualified inline. Registers `DEC-086` open for the product owner |
| [0093](ADR-0093-ready-for-real-users-is-said-of-the-shipped-artifact.md) | "Ready for real users" is said of the shipped artifact, and the bar is two facts | Accepted — resolves `DEC-086` for the product owner: the bar is **two merged, checkable facts, neither a test result** — the proofs of record (the `ADR-0088` §2 hand-check and the QA/UAT rounds) load the **built bundle** rather than `npm run dev` (mechanism registered as `DEC-087`, the architect's; until it is answered the bar is unmeetable, on purpose), and **recovery is completable by a real user in the offered deployment** (`ADR-0031` §7's *"valid state in development and tests"* applied: an offering to real users is neither). Meeting the bar is a **precondition on the phrase, never a certificate** — readiness stays the human's judgment made by reading, `ADR-0089` §2c and `ADR-0092` §2 standing; the bar gates the phrase, not the act of offering, which stays the human's. The event is `ADR-0063` §5's — strangers beyond *"people the author invited personally"* — so the founding moment is not gated, and that ADR's farm expiry falls due at the same event on its own terms. Five absences (history paging past 10, ladder paging, the third device, the hundred-way tie, real network) are **out** under a written rule: a gap enters the bar only when its failure falsifies the success condition at first contact or destroys what a player cannot get back |
| [0094](ADR-0094-opening-the-invite-is-taking-the-seat.md) | Opening the invite is taking the seat, and the two join cards are corrected to it | Accepted — §1 qualified by 0105 (an opener already in a running duel takes no seat) — resolves `DEC-092` for the product owner: the join path **ships as it is**, and the two cards that draw screens it does not have are what change. Applies `docs/vision.md`'s first success condition — five verbs, none of them *accepts* — with `ADR-0022` (*holding a room code is the invite*; joining is *"one click with zero friction for the invited"*) and `ADR-0060` §§1, 4 (the first screen carries *"the join form"* and *"the room code box"*, which a separate code screen would make false). Amends nothing: no client, server, wire or `PROTOCOL_VERSION` change, and no `docs/test-plan.md` `expect` — `SMK-05`, `CORE-02` and `CORE-05` stand. Constrains `TASK-120907` (now a `module: design` ticket), `design/screens/join-duel.html` and `enter-code.html`, and two rows of the screen table. Raises nothing for the architect: `TASK-120907`'s technical half was conditional on a *yes* |
| [0095](ADR-0095-the-table-states-who-took-the-pot-and-never-names-a-hand.md) | The table states who took the pot, and never names a hand | Accepted — resolves `DEC-095` for the product owner: **yes** to the banner, **no** to the made hand. When the street is `COMPLETE` the table replaces `Pot N` with one line — `You win 4,850` / `Your rival wins 4,850` / `Split pot — you win 2,425` — transcribed from a `PotAwarded` the client already receives, and **no hand is ever named** at the table, at any street, in text or in an `aria-label`. Applies `docs/vision.md`'s *On variance* — *"Luck decides a hand"* — for the banner, and *"Every hand is stored as an event log, so a match can be replayed and analysed **afterwards**"* with *Positioning* and the roadmap (replay is v0.4) for the refusal. Builds on `ADR-0046` §§0, 2, `ADR-0075` (lifetime is a fact, never a timer), `ADR-0008`, `ADR-0002` and `ADR-0094` (the card is corrected, not the product). Amends nothing: no engine, server, wire type, `PROTOCOL_VERSION` step or `docs/test-plan.md` `expect`. `no-derivation.test.tsx`'s `HAND_TALK` gate stands **byte-unchanged** — its fixture is mid-hand, so a correctly-triggered banner leaves it green, and a coder who finds it red has built the wrong trigger. Constrains `TASK-121101` and both banner frames of `design/screens/duel-table-states.html`, which lose their second line. Raises nothing for the architect: `TASK-121101`'s wire question was conditional on a *yes* |
| [0096](ADR-0096-the-audit-judges-a-whole-duel-against-a-frozen-rubric.md) | The audit judges a whole duel against a frozen rubric, and no round may grow it | Accepted — resolves `DEC-096` for the product owner: a **product audit** is a third focus of the one cycle (`/qa-cycle audit <scope>`), and its unit is the **beat**, not the screen — the walk is `docs/vision.md`'s first success condition read one clause at a time, eight beats from the opened link to the rematch, **including the all-in runout `docs/test-plan.md` has never walked**, both browsers observed, at **two shapes** — the whole walk at `phone` **390 × 664** and `R2`/`R3` again at `laptop` **720 × 900**, on the human's *"we have to support phone size"*, with **one bar checked twice rather than two bars**. `ADR-0092` §3's classifier is **amended for this focus only, by relocating the merged source rather than removing it**: a finding contradicts a **criterion** in a merged, closed, five-line **rubric**, and needs no other source — the human's *may file findings that contradict no merged source*, honoured without abolishing the rule §3 protects. §3 stands byte-unchanged for the `qa` and `uat` focuses. **Termination is the rubric**: `A(N)` counts *criteria* answered `not met`, so `A(N)` can never exceed the rubric's size, a ceiling known before the round starts; the rubric is **frozen for the invocation** (`EPIC-12` rule 1, one level up), a proposed criterion routes as `ADR-0092` §5 routes a question and reaches the list no earlier than the next invocation; `PASS` at `A(N) = 0` says the **list** is satisfied and never that the product is finished (`ADR-0089` §2c, `ADR-0093` §2). **No severity and no backlog under this focus** — a finding deferred by the eight-cap stays counted, which is the repair of the seventeen `status: backlog` rows — with `EPIC-12` rule 2 scoped to the other two focuses on the human's call. Applies `ADR-0092` §§6, 8 (one manager, one ledger, no second skill) and `ADR-0093` §2. Registers `DEC-097` — **the architect's**: the declared-file set and the two-viewport mechanism. Its one escalation — **is a phone a supported surface?** — the human answered the same day (*"we have to support phone size"*), so §4 walks **two shapes** and `DEC-097`(b) became the load-bearing half. **`docs/vision.md` is not amended**: *"She opens it in a browser"* and *"Two browsers, one room link"* name a browser and **no device**, so the call resolves a silence rather than contradicting a sentence, and §4 names what would change that. **Phone landscape was left open by §4 and is now answered** — the human's *"we are ok to support only one orientation for mobile form factor"*, recorded by [`ADR-0097`](ADR-0097-a-resize-is-two-numbers-and-the-observer-is-the-fifth-file.md) §5; **the rubric's home is settled by [0099](ADR-0099-the-rubric-is-the-adr-section-and-a-criterion-is-born-merged.md)** — §2 itself, grown only by an amending ADR that annotates this row, and no working document ever exists (`DEC-098`) |
| [0097](ADR-0097-a-resize-is-two-numbers-and-the-observer-is-the-fifth-file.md) | A resize is two numbers, and the audit's observer is the fifth declared file | Accepted — resolves `DEC-097` for the architect, both halves, and unblocks the first audit round. **(b), the load-bearing half: a viewport resize is an *act* under `ADR-0089` §3**, the third of its three categories — *click, type, navigate, reload, clear storage* — not a read and not an application-state write, so §3 stands byte-unchanged and is applied rather than widened. `scripts/qa/drive.mjs` gains one verb, `size <width> <height>`, sending `Emulation.setDeviceMetricsOverride` with **`width`, `height`, `deviceScaleFactor: 0`, `mobile: false` and no other field**, reading the viewport back from the page and **exiting 1 if it is not what was asked for**. *The classification is a property of the fields, not of the method name*: measured, `mobile: true` widens the layout viewport 390 → 520 by mobile shrink-to-fit and turns an `R2` failure into a **false pass**, fabricates `screen` and `devicePixelRatio`, and buys none of `pointer: coarse`, `hover: none` or `maxTouchPoints` — unchanged in all three states. The override **survives session detach and navigation** and is **per-target**, so a live tab keeps its JS context (hence its socket and its seat), fires one `resize` and re-resolves `100dvh` — §4's two shapes are two measurements of one tab, not two passes, and `ADR-0018`'s re-seat is never approached. **Both tabs move together and the walk restores itself to `phone`**, so the shape is the only variable. `Browser.setWindowBounds` is **rejected on measurement**: Chrome clamps a window to 500 px wide, so 390 × 664 yields a 500 × 577 viewport, and 87 px of chrome makes 720 × 900 yield 720 × 813 — §4's numbers are viewport numbers and window sizing cannot express them. **(a)** `ADR-0090` §2's declared-file set becomes **five**, `.claude/agents/audit.md` licensed to *mention* the cycle and never to invoke it, on `ADR-0092` §2's precedent and passing `ADR-0092` §8's own test — `ADR-0096` §2 froze `ADR-0092` §3 byte-unchanged for `qa` and `uat`, so one file cannot hold two classifiers; the check gains `audit` and was verified to exit 0 today, 0 at five and 1 at six. `ADR-0092` §8 otherwise applies unamended: one manager, one ledger, no new skill, no `Write` on the observer. **Portrait only** — the human's *"we are ok to support only one orientation for mobile form factor"* recorded where `ADR-0096` §4 left the question, so no rotation handling, no reflow decision, no second mobile shape, and `screenOrientation` is never sent. `ADR-0089` §§2a/2b/2c re-checked one at a time, with the corollary **a shape walked is not a surface supported**. Refines one stated reason in `ADR-0092` §2 and `ADR-0096` §4 without moving either decision: the ~500 px clip belongs to window *sizing*, not to capture — an overridden 390 × 664 captures faithfully at 390 × 664 |
| [0098](ADR-0098-the-wordmark-belongs-to-the-front-door-alone.md) | The wordmark belongs to the front door alone | Accepted — resolves `DEC-099` for the product owner: the coin-and-two-tone lockup renders on the **front door alone** — the first screen's pre-create branch, as `design/screens/create-duel.html`'s front-door frame draws it — and the unconditional `<h1>Poker Duels</h1>` leaves `App.tsx`, so no other screen or state carries on-page product naming. **Mostly a reading of merged sources, and recorded as one**: eleven card files hold one `.mark` between them; the four secondary-screen cards were composed under the shipped global heading and none drew it; even `create-duel.html`'s own waiting frame gives its top to the code. Confirmed by `docs/vision.md`'s *Positioning* (*"dark, quiet, fast, minimal"*) and the success condition, whose match is played on the screens the mark stays off; builds on `ADR-0033` (anatomy card-born, placement deliberately unsettled until now), `ADR-0060` §5 (chrome above the table already refused once) and `ADR-0094` — which makes the named cost real: **the invited half of the product is seated straight into a dealt hand and now never reads the product's name on-page**, only in the tab title. **No card is in arrears — the client was**; the follow-up ticket touches four files (`App.tsx`, `Lobby.tsx`, `App.test.tsx` — whose four plain-title assertion sites cannot stand — and `Lobby.test.tsx`) and §4 names them for the planner. Amends nothing on the wire and raises nothing for the architect |
| [0099](ADR-0099-the-rubric-is-the-adr-section-and-a-criterion-is-born-merged.md) | The rubric is the ADR section, and a criterion is born merged | Accepted — resolves `DEC-098` for the architect: **the audit rubric is `ADR-0096` §2 itself, and no working document ever exists.** The deciding force sat in neither reading's list — **where a criterion is born**: `ADR-0096` §3 routes every proposed criterion to a `DEC` whose answer is *a merged PR either way*, and a merged answer is an ADR (`CLAUDE.md` rule 5), so every future criterion arrives as merged ADR text whatever `DEC-098` said, and a working document could only ever have held a **copy** — a second register from its first line, able to disagree silently with the law it transcribes, guarded by nothing (*two copies of a rule drift*, `ADR-0092` §8, applied not re-argued). Growth is **one amending ADR** in the pattern `ADR-0092` §2 and `ADR-0097` §4 used on `ADR-0090` §2's declared-file set: ids sequential and never reused (`R6` next), the criterion stated in §2's own three-column form, the **resulting priority order restated as ids only — never another criterion's text**, and `ADR-0096`'s index row annotated *rubric grown to N* **in the same PR** — the same-PR clause priced against its precedent's one observed lag, `ADR-0090`'s row reading *grown to four* a day after `ADR-0097` made the set five, repaired in this ADR's PR. **A round record cites `R<n>` and the ADR section that states it — `R2` (`ADR-0096` §2) today — never a document path**; the freeze (`ADR-0096` §3) becomes a commit fact, the rubric in force for an invocation being the chain as merged at the commit its first round names; §6's metric counts from two dated registers, growth ADRs in this index and invocations in the round stories. `TASK-121203` and `TASK-121205` already cite the decided form and **change nothing**; the declared-file set stays at **five**, no file added anywhere; `ADR-0096` §7's *the rubric* deletion and *the ADR that says why* collapse into one act, so reversal got cheaper. Costs named: the rubric is never again one table once it grows (assembly on every invocation, with the reversal — found a document later, transcribe once at that day's size — priced for the day the chain is the demonstrated obstruction); the index annotation is a hand-maintained discipline, not a gate; a sixth criterion is never cheaper than a full merged ADR — slow on purpose |
| [0100](ADR-0100-the-driver-reaches-an-amount-by-pressing-what-a-player-presses.md) | The driver reaches an amount by pressing what a player presses | Accepted — resolves `DEC-100` for the architect: `web-client/src/e2e/drive-duel.tsx`'s `actThroughTheBar` stops setting a control's value and **presses the sizing row, then reads the action button before it clicks** — find the button by the recorded verb (unchanged), read the total it prints, click if it already matches, otherwise press each sizing control in document order re-reading after each press, and **throw** naming the recorded amount and every amount reached if none matches. That is strictly *more* than `fireEvent.change` proved: the old path set a value and trusted it, the new one proves the bar reached the amount before `whole-duel.test.tsx`'s `expect(actsSent).toEqual(recordedActs)` proves it encoded it. **The premise the question was raised on is false, and measuring it made the answer cheap** — the script is generated, not authored: `playDuel` draws every bet/raise amount from `minRaiseTo` or `allInTo` alone, and all four amount-carrying steps in the committed fixture land on a chip (three `allInTo`, one `minRaiseTo`), so **no frame is re-recorded and none of the four failing e2e files is edited**, which is the evidence nothing they prove was traded away. §4 settles a second breakage the two-file budget hid: the sizing row must set `to = allInTo` *without* sending (three recorded raises) **and** an `ALL_IN` button appears whenever the server names `ALL_IN` (one recorded `AllIn`, and `BettingRules.kt` allows `ALL_IN` with no `RAISE` for a short stack facing a bet) — so the card's three-button `.actions` row is one drawn state, not a law over all states, and no card is in arrears. §5 refuses **by name** a driver-only slider, any test-only prop or `data-testid`, and any reach into state or `actFrame`. §6 rules that **a named preset computes the quantity it is named for**: `floor × 4/3` labelled *⅓ pot* does not ship, and the pot reaches the control from `view.pot + both committedThisStreet` through `Lobby.tsx` — no wire change — while *which* quantity `pot` names is `DEC-101`, registered open for the product owner and answered the same day by [0101](ADR-0101-pot-means-a-pot-sized-raise-and-the-fractions-share-its-base.md). Costs named: the e2e can no longer exercise an **interior** amount at all (nothing lost today, foreclosed tomorrow); the bar can read all-in twice; `TASK-120908` becomes a six-file atomic ticket, blocked. Applies `ADR-0002` and reads `ADR-0033`; amends neither |
| [0101](ADR-0101-pot-means-a-pot-sized-raise-and-the-fractions-share-its-base.md) | `pot` means a pot-sized raise, and the fractions share its base | Accepted — resolves `DEC-101` for **the product owner**: every sizing chip sets a street total, and the three fractions are **one rule over one base**. The base is the pot as it will be *after the call* — `P = view.pot + both seats' committedThisStreet` (`ADR-0100` §6's pot, unchanged) plus `toCall = callTo − the acting seat's committedThisStreet` — and the chips set `min → minRaiseTo`/`minBetTo`, `⅓ → callTo + floor(base/3)`, `½ → callTo + floor(base/2)`, `pot → callTo + base`, `all-in → allInTo`, rounding **down** because chips are integers and under-committing is the side the label promises. With no bet outstanding `toCall` is 0 and the base is simply the pot, so one rule gives *half the pot* when betting and a half-pot raise when raising, with no second case. **Licensed by `docs/vision.md`'s *What it is*, first line — *"Heads-up Texas Hold'em. Two players. Never three."*** — the product is the game, so a control's word is the game's word; `ADR-0100` had already recorded in a merged sentence what a poker player pressing *pot* expects, and the two formulas the card admits are not it. **§3: a fraction chip is offered only when its own amount is legal** (`floor ≤ amount ≤ allInTo`), never clamped and never greyed — so `TASK-120908`'s *"a preset the stack cannot afford is not offered"* stands and gains the bottom end, hiding costs a shortcut and never a legal amount, and `min`/`all-in` are legal by construction and never absent, which is what keeps `ADR-0100` §3's *no frame is re-recorded* true of this answer too. **§5: `design/screens/duel-table.html` is in arrears on one number in two places** — the hero frame's stepper and its `Raise to` button read `3,250` with the `pot` chip selected and become **3,650**; 3,250 is the *base*, printed where the raise that sizes to it belongs. Costs named: a merged card is wrong and a design ticket exists that nobody planned; the row changes shape between turns; preflop the button gets `min` and `½` at the same 300 and **no `⅓` at all**, so an ordinary 2.2–2.5bb open is reachable from no chip; and the chips can no longer be defined from `LegalActions` alone. Registers **`DEC-102`** — the stepper's step, which `ADR-0100` mis-filed here and which gates none of §§1–5. `TASK-120908` keeps `ADR-0100` §7's six files |
| [0102](ADR-0102-a-hand-ends-in-steps-and-the-client-owns-the-clock.md) | A hand ends in steps, and the client owns the clock | Accepted — resolves `DEC-105` for **the architect**: a runout's pacing lives in **`web-client`**, and `poker-server`, `poker-engine` and the wire are untouched. When a `Snapshot` says the hand is over, the store paints it as a **sequence of steps** — one per `StreetDealt` in the `Events` frame that immediately preceded it, then a final step carrying the whole snapshot and `ADR-0095`'s award line — and **queues every frame that arrives while steps remain**, in arrival order, applying them when the last step has stood. At step *k* exactly two fields lag: the board is `view.board.cards.slice(0, n_k)` with `n_k = board.length − (cards carried by the steps after k)`, and the street label is that `StreetDealt`'s own `street`. A `Snapshot` that does not end a hand is applied at once, with no timer, so **ordinary play costs nothing**. **The ticket's source reading was confirmed on three points and wrong by omission on a fourth, which is what decides the answer**: `DuelAction.kt`'s `act` calls `advance` in the same call and `deliver` sends both hands' frames in one batch — the merged `DuelActionTest.afoldEndsTheHandAndOpensTheNext` asserts snapshots for hand 1 and hand 2 in one `outbound` — so a client that only sliced the board would be overwritten a millisecond into the first step, and `ADR-0095` §4's banner is today drawn and erased in the same delivery. §6 meets *the server is authoritative* head on and draws the line: **the client may choose when to paint a fact the server sent, never what the fact is** — every value painted is the server's own, the lag is monotone, bounded and self-terminating, it can never influence what the client sends, and `ADR-0095` §2 had already settled that rendering a server-stated value is not asserting a game fact. §3 fixes the invariant: **the only thing ever withheld is the tail of the board** — no card face comes from a `StreetDealt` payload, no hole card is held back, no control is shown early or retracted, and `no-derivation.test.tsx` stays byte-unchanged. §4 sets the step at **600 ms**, named once in `boot.ts`, with **`0` meaning synchronous** so `drive-duel.tsx` boots at `0` and the four recorded-frame e2e suites are not edited. §5: a **reconnecting or reloading client jumps to the end**, structurally — `resumeFrames` sends no `Events`, so there are no street steps to take. §7: `PROTOCOL_VERSION` does **not** move, and would not have moved for the server option either. Costs named: a runout's **stacks settle at the first step**, so the outcome is legible from the seat plates 1.8 s early; every hand ends 600 ms later; screen and server disagree by design for up to 2.4 s, and a queued `OpponentPresence` over-states its grace; the two seats pace independently; and `DEC-104`'s `Pot 0` is held on screen longer, unfixed and still the product owner's. `TASK-121301` becomes one atomic `web-client` ticket, re-cut whole |
| [0103](ADR-0103-the-table-fits-the-phone-and-the-cards-give-before-the-numbers.md) | The table fits the phone, and the cards give before the numbers | Accepted — §1 qualified by 0106 (a true residual under one CSS pixel reads as met) — resolves `DEC-106` for **the product owner**: the duel table **fits a 390 × 664 phone**, whole column, at every beat — `scrollHeight ≤ clientHeight`, no scroll to act — and it is **one table at two widths**, every element shown in the same order with the same words, its measurements narrowing **continuously with the column's own width** (the `100cqi` idiom already in the card), so no width shows a different table and `ADR-0096` §4's three second-surface tests are all missed. **The arm the question offered was already closed**: `ADR-0096` §2 rules scrolling to see the amount to call `R2` `not met` at either shape and forbids a relaxed phone bar by name, so *"a player scrolls"* would have amended a criterion and contradicted the human's *"we have to support phone size"* — not this ADR's to do. **§3, what gives, in order and exhaustively: whitespace → the rival's face-down hand → the player's hole cards (floor: never smaller than a board card) → the board, last of the card groups. Nothing else gives** — stacks, pot, bet lines, the amount to call, the sizing row and the action buttons keep their size, labels and place (`R3`). The bar **may grow** (its rows wrap at 390) but never truncates; running out of the list re-opens a decision rather than accepting a scroll. **§4: `design/screens/duel-table.html` carries it** — amended so the hardcoded `--w:96px` and `--w:40px` narrow like `--bw`, plus a **second frame at 390 × 664 with the same markup**, boxed in height as well as width; a separate phone card is refused, and the line is **markup identity, not file count**. **The card is in arrears and was already**: measured headless this run it is **732 / 664** at 390 × 664 and 900/900 at 720 × 900, so a client transcribing it perfectly still fails `R2` by 68 px — which is why `TASK-121302` could not close this by conformance. **§5: `TASK-121302` is rewritten, not amended, and blocked on the design ticket**; its height-budget half (`min-height: 100dvh`, `flex: 1`, one column not two) survives as necessary-but-not-sufficient, and its file set is measured. **§1 also settles that one frame answers every beat** — the card reserves every slot, so the table's height does not vary by beat. Costs named: the player's own hand is smaller on a phone; the table acquires a **size budget**, so the next thing added must buy its space or not ship; round 1's fix set grows a dependency and `R2` is counted `not met` again if round 2 runs first (`ADR-0096` §5 — filing does not reduce `A(N)`); two frames drift with no gate; and the give order becomes a merged constraint on design taste. Leaves `DEC-103`, `DEC-104` open by name and reopens no orientation question (`ADR-0097` §5). Applies `ADR-0096` §§2 and 4, `ADR-0091` §§1 and 3, `ADR-0024` §3; amends nothing |
| [0104](ADR-0104-a-frame-reaches-the-connection-in-the-room-it-is-about.md) | A frame reaches the connection that is in the room it is about | Accepted — resolves `DEC-107` for **the architect**: `deliver` resolves a seat to a player exactly as it does today and then asks for that player's writer **for this room**; a connection in another room, or in none, is skipped silently, the same idiom `deliver` already documents for a seat mid-reconnect. **§1: `ConnectionDirectory.writerFor(player)` is deleted**, replaced by `writerFor(player, room)`, so there is no unscoped lookup a later call site can reach for; `deliver`'s signature is unchanged and **all seven call sites are untouched**. **§2:** `RoomMembership` moves to `duels.poker.server.session` and is registered with the writer it belongs to, one object shared by reference, its `code` **`@Volatile`** — read by the ticker and the other seat's socket, and a stale read is invisible on one thread. **§3: production does not change** — `disconnect()` still builds the frame, `ADR-0028` §5's emission table stands, and the registry keeps knowing nothing of writers. **§6: `PROTOCOL_VERSION` does not move** — no declaration changes, `ADR-0047` §2's fingerprint is unchanged, no ledger row, `TASK-121403` is **not** `atomic:`. **The story's reading was confirmed and found to understate the defect**: the widening is in the composition `seat → PlayerId → writer-anywhere`, so it carries every frame `deliver` routes — nine of `ServerMessage`'s eleven subtypes (`Snapshot`, `Events`, `YourTurn`, `DuelFinished`, `Rejected`, `Failure`, `ActedForAbsent`, `RematchOffered`, `OpponentPresence`; only `Welcome` and `RoomJoined` are never routed) — and needs no race, because nothing on the wire says *leave* (`ADR-0072`) and a held seat's frames follow the player. No card can leak: the recipient is always the player the frame was projected for. **§4** scopes the client's own store — a `RoomJoined` naming a **different** room re-initialises it (counters carry over) — and is explicitly **not** load-bearing; **§5** answers the in-flight case in three windows and names the one no server check can close. **§9** treats the unreproduced fold as a mechanism that fits, never as a cause. Costs named: `ConnectionDirectory`'s documented ignorance is retracted; a `@Volatile` obligation nothing in the type system asks for; silent drops gain a second indistinguishable cause with no counter; membership-before-delivery becomes load-bearing ordering; and **the unscoped "send to a player wherever they are" is foreclosed**, deliberately and with no current victim — `writerFor` has one call site, and this is it. **Splits off `DEC-109`** — the product owner's: may one player hold seats in two live rooms at once? Upholds `ADR-0028` §1 rather than reopening it; leaves `DEC-108` untouched |
| [0105](ADR-0105-one-duel-at-a-time-and-the-refusal-hands-back-the-duel.md) | One duel at a time, and the refusal hands the player back their duel | Accepted — resolves `DEC-109` **for the human, who stated the call on 2026-09-02** after review found the vision does not settle it: **no**, a player holds at most one seat in a **running** duel. While they hold a seat in a `PLAYING` room, `CreateRoom` and `JoinRoom` from that player **take no seat** — the room they asked for is untouched, the seat and duel they hold are untouched, and no coin moves. **§2 scopes *running* to `PLAYING` alone**: a `WAITING` seat is not refused, because `ADR-0073` §3 told the host on screen that the room stays open and they may walk away; a `FINISHED` seat is not refused, because `Room.offerRematch` agrees only when **both** seats have offered. A seat inside `ADR-0013`'s grace window is refused with the rest — that is the grace period working. **§3: the refusal hands the player back the duel they are in** — the table as it stands, never a lobby, never a dialog — and repeating it does the same thing every time; both shipped routes in (`Back to the lobby`, and a second `?room=` link that `boot.ts` prefers over the memory) arrive holding no memory of that room. **§4 fixes two sentences and no third** — `You are already in a duel. Finish it to start another.` — with a table of what the copy may not say (a duration, anything about the code that was asked for per `ADR-0022`'s no-oracle rule, a promise the link will last, the casino's furniture); layout stays `EPIC-06`'s. **§5: the player whose invite was clicked sees nothing**, because nothing happened to them. **§6 states what must be true and writes no repair**: nothing moves, the client must be able to name the duel it returns to, and the refusal must not meter as a guess when `ADR-0022` §2's budget is built (`JoinLimits` and `TOO_MANY_ATTEMPTS` do not exist at `develop`). **§7 inherits `ADR-0104` §9**: this explains no fold. Costs named: **a player who wants out of a duel is stuck** — no resign, no turn clock, and a `PLAYING` room is never reaped, so their exit depends on their rival still acting; the product's primary action, *Create a duel room*, can now say no; the friend whose link was clicked and the player who clicked it are both left slightly in the dark, on purpose; one more string with nothing keeping it in voice; and **§2's hole stays open** — several `WAITING` rooms can each become a duel the player is not at, so *never in two duels at once* is what this aims at and not what it guarantees. Qualifies `ADR-0094` §1 by one case; registers `DEC-110` (the architect's — the mechanism) and `DEC-111` (the product owner's — more than one waiting room) |
| [0106](ADR-0106-a-sub-pixel-residual-is-a-fit-and-one-pixel-is-the-fence.md) | A sub-pixel residual is a fit, and one pixel is the fence | Accepted — resolves `DEC-112` for **the product owner**: after `TASK-121402`'s 48 px repair the duel-table document measures a true **664.90625 px against 664** (`scrollHeight` 665) with both action buttons at or above the fold, and *fits the phone* **admits it**. `ADR-0103` §1's `scrollHeight ≤ clientHeight` stays the contract; at the one boundary the integers cannot express — a reading of exactly one over — the judgment moves to the true geometry, and **a true excess strictly under one CSS pixel is met**, one pixel or more is `R2` `not met`, a two-integer excess files with no second read. **§2 fences the tolerance at the instrument's own unit and forecloses its widening** — a tolerance that can hold painted content is the *"relaxed phone bar"* `ADR-0096` §2 forbids by name, so widening is the human's, reachable by no vision-derived ADR. **§3: `TASK-121402` merges as scoped** — the implementer's stop is the conduct `ADR-0103` §3 commands, and the 664 / 664 prediction was unreachable from the day it was written (the defect table's 712 was a rounded 712.90625; the integers were never the geometry). **§4: the last 0.90625 px is re-filed once**, ordinary backlog, buying headroom (the column stands 0.09375 px from the fence) and retiring the second read; its closer spends §3.1 whitespace and nothing further down the give list. **§5 is the QA clause**: a sub-pixel overflow may not be filed — not as a finding, a criterion, a `DEC` or precedent — and shields nothing else; clipped content, imperceivable events, unmeasured beats and any full-pixel excess walk the ordinary path. Costs named: the one-`eval` contract is spent at the boundary (two reads and a rule, until a residual ticket nothing forces to land); a citable tolerance now exists; the product ships 0.09375 px from the fence; a criterion merges undischarged as literally written, carried by the PR body's readings plus this ADR; and a classic-scrollbar window at phone width can grow a scrollbar track for a rounded pixel. Qualifies `ADR-0103` §1 by one boundary case; upholds `ADR-0096` §2 |

## Open decisions

Questions deliberately left open are marked `DEC-NNN` in the document they affect.

**Who answers one** — see [`docs/workflow.md`](../workflow.md#who-answers-a-dec):

| Kind | Answered by |
| --- | --- |
| **Technical** — where a type lives, schema shape, wire format, concurrency, failure semantics | the `architect` agent |
| **Product** — what a player sees, what a duel *is*, which risks inside the software are acceptable | the `product-owner` agent, deriving from [`docs/vision.md`](../vision.md) |
| **Vision** — money; what the product *is* or *is not*; the roadmap's shape; risk with consequences outside the software | the human, and nothing else |

A `DEC-NNN` can be listed in **four** places: this file, [`tasks/BOARD.md`](../../tasks/BOARD.md),
and the `## Open decisions` table of any epic under `tasks/epics/`. The PR that answers a decision
**strikes every row it answers, in the same PR.** A strike deferred to somebody else's next PR is a
strike nobody makes — `DEC-035` sat listed open and answered simultaneously for weeks that way.

| ID | Question | Where | Due |
| --- | --- | --- | --- |
| DEC-002 | What performance budget does the hand evaluator carry, how is it measured, and does `HandRank` become a packed integer? | `../../tasks/stories/STORY-0103-hand-evaluator.md` | before STORY-0108 |
| DEC-008 | Is the full `MatchLog` persisted in v0.1, and where — a column, a table per hand, or object storage? | `../../tasks/stories/STORY-0209-postgres-schema-and-migrations.md` | before EPIC-08 |
| DEC-032 | **The architect's** — how does the client test browser storage, and is the developer Node version pinned? Node 24+ defines an inert `localStorage` global (absent `--localstorage-file`) that shadows jsdom's under Vitest, so `typeof localStorage === "undefined"` while `sessionStorage` works. `TASK-030304` sidesteps it by injecting a `Storage`, which its own contract already required — but `TASK-030307`, `TASK-030309` and `STORY-0311` each touch storage, and `STORY-0311` sends the same value as an HTTP header. Open: does anything ever exercise a *real* `Storage`, and does the repo pin a developer Node version rather than only CI's (`.nvmrc` says 24, this workstation runs 26)? `DEC-022` foresaw the split; this is the first time it has bitten. | [`TASK-030304`](../../tasks/tasks/TASK-030304-the-device-id-lives-under-one-key-this-module-owns.md) | before STORY-0311 |
| DEC-060 | **The product owner's** — does a **finished** season ever become reachable from a screen, and how is one chosen? Raised by [`ADR-0061`](ADR-0061-a-season-is-a-calendar-month-and-the-coin-never-resets.md) §7, which settles that a finished season is never *gone* — its standings recompute exactly from rows nothing rewrites — and that v0.3 ships no way for a player to ask for one. So on the first day of a new season the previous month's ladder is computable and unreachable, and **nothing records who won it**. Settle whether the product ever shows a past season, and if so how one is named and chosen (a selector, a *last season* line, a single remembered winner, or nothing), given that a selector is a control on a screen [`ADR-0060`](ADR-0060-the-record-is-its-own-screen-and-the-lobby-is-the-door.md) already said will crowd. *Never* is a complete answer and needs saying out loud rather than falling out. Blocks nothing today | [`ADR-0061`](ADR-0061-a-season-is-a-calendar-month-and-the-coin-never-resets.md) | before the first season boundary after the ladder ships |
| DEC-087 | **The architect's** — by what mechanism do the proofs of record load the built bundle a real user would receive: what serves `dist/` (the Ktor server, a static server, a `scripts/qa/stack.sh` mode), on what origin, and what supersedes [`ADR-0088`](ADR-0088-the-two-browser-proof-is-a-written-hand-check.md) §2 step 3's `npm run dev` — through that ADR's own §5 route, since its §2 is immutable prose — so the hand-check describes the same artifact? Raised 2026-08-30 by [`ADR-0093`](ADR-0093-ready-for-real-users-is-said-of-the-shipped-artifact.md) §1a, whose readiness bar is unmeetable until this is answered. Blocks nothing running today — QA and UAT rounds continue against the dev server and their records say so | [`ADR-0093`](ADR-0093-ready-for-real-users-is-said-of-the-shipped-artifact.md) | before anything is offered to real users |
| DEC-088 | **The product owner's** — when a UAT round's promotion gate has **more than three screens** each offering a question that clears the bar, which screens' questions take the three slots? Raised 2026-08-30 by `TASK-120708`, whose coder declined to invent the rule and named it instead. `ADR-0092` §5 sets the caps — at most three per round, one per screen — but supplies no ordering, and neither `.claude/agents/qa-manager.md` nor `.claude/agents/uat.md` does. Step 4's fix-set ordering (*`blocker` before `high`, then by how much of the product is blocked*) does **not** generalise: every promoted question has already cleared the same single bar, so there are no severity layers to sort by, and no merged source ranks one screen against another. Until it is answered, `qa-manager` improvises — and a UAT round walks 13 screens for 3 slots, so the first round hits this. *The three sharpest, by the manager's own judgment, recorded in the round story* is a complete answer. Blocks nothing — the cycle runs, it just promotes an unordered three | [`ADR-0092`](ADR-0092-a-uat-round-files-what-a-source-settles-and-asks-the-rest.md) | before the first UAT round |
| DEC-093 | **The architect's** — does [`ADR-0092`](../docs/adr/ADR-0092-a-uat-round-files-what-a-source-settles-and-asks-the-rest.md) §6's **baseline rule** extend to a round in which a screen-state becomes conformance-judgeable for the first time through **any** merged instrument, or only through a **card** merged in the previous round's repairs? Both §6 and `EPIC-12` §Termination rule 4 are written about a card. Round 3 met the other case: two frames of `design/screens/duel-table-states.html` and `rematch-states.html` were unreadable in rounds 1 and 2 — round 2 said so in as many words, *"check (a) on two of `duel-table-states.html`'s three frames is unreachable by any round with the verbs `drive.mjs` has"* — and became readable in round 3 only because `TASK-121008`'s `record`/`frames` verbs merged in round 2's repairs. The rule's stated **purpose** reaches that case exactly (*"the two rounds measured differently-sized judgeable sets"*); its **text** does not. Raised 2026-08-30 at round 3's triage, which **refused to extend the exemption itself**: a manager who widens a rule in the round that rule would save cannot prove it would have widened it anywhere else, and `STORY-1208` is the precedent for repairing this machinery by a merged sentence rather than by a generous reading at triage. **Not one of `ADR-0092` §5's three promotion slots** — those are the product owner's, and this is `CLAUDE.md` rule 5 routing. **Blocks nothing**: round 3's fix set is empty, `B(3) = 0` so rule 4 could not fire at any reading, and the cycle ended `PASS`. Answering it costs a future invocation a wrong `STOP_DIVERGING` or nothing at all. Recorded in [`STORY-1211`](stories/STORY-1211-round-3-uat-the-count-fell-to-zero-and-the-cycle-ends.md) | [`STORY-1211`](stories/STORY-1211-round-3-uat-the-count-fell-to-zero-and-the-cycle-ends.md) | before the next `/qa-cycle` invocation |
| DEC-094 | **The product owner's** — should a control that **no card draws** wear the client's control vocabulary, or is a bare control the intended treatment for navigation the cards do not draw? Concretely: the lobby's `Your duels`, `Leaderboard` and `Account` doors, and the `Back` on each secondary screen, all render with `className: ""` — computing as body text — beside a room-code input, `Create a duel room`, `Join the duel` and `Set my name` that carry the full recipe. No merged source is contradicted, which is why it is a question and not a finding: `design/tokens/tokens.css` is a `:root` sheet with **no selectors**, so an unclassed button contradicts nothing in it; `create-duel.html`'s front-door frame draws neither control and notes *"nothing else on the door — no lobby noise, no tables list"*; and `ADR-0060` §§2–4 settle the doors' **word**, **element** and **placement** and the way out's word, and say nothing about treatment. Raised 2026-08-30 at round 3's triage and promoted by `qa-manager` under [`ADR-0092`](../docs/adr/ADR-0092-a-uat-round-files-what-a-source-settles-and-asks-the-rest.md) §5 — **one of the three slots that round had, and the only one spent**, on the `first` screen, so `DEC-088`'s ordering question did not bite. It clears both halves of the bar: a concrete choice answerable in one sentence, and it bears on whether a player can tell that three words under a form are things they may activate. **Two rounds have now spent a finding on it** — the four `Back`s in round 2, the three doors in round 3 — and only a merged answer closes it mechanically, as `ADR-0094` closed the join path. **Blocks nothing** — round 3's fix set is empty and the cycle ended `PASS`; an answer becomes a merged source either way, and any ticket it yields enters the **ordinary backlog now the cycle has ended**, never the round that asked (`EPIC-12` §Termination rule 1). A *no* earns a row in `docs/test-plan.md` §*Settled, and not a finding*, so a later round re-raising it would itself contradict a merged source. Recorded in [`STORY-1211`](stories/STORY-1211-round-3-uat-the-count-fell-to-zero-and-the-cycle-ends.md) | [`STORY-1211`](stories/STORY-1211-round-3-uat-the-count-fell-to-zero-and-the-cycle-ends.md) | before the cycle's next triage |
| DEC-089 | **The product owner's** — on the `result` screen, should a post-verdict account-claim nudge share the verdict's own type weight, or should the verdict stay the single largest thing on the screen? `web-client/src/result/AccountOffer.tsx:25` renders the nudge's headline — *Your duel coins are only in this browser* — at `text-display leading-tight font-bold`, the same weight `DuelResult` gives *Victory* itself, and `design/screens/duel-end.html` draws nothing after *Rematch* to compare against (the card predates the offer by thirteen days; `ADR-0091` §5 registers the accretion as debt). No merged source ranks the two, so it is a question, never a finding. Raised 2026-08-30 at round 1 of `/qa-cycle uat regression`'s triage and promoted by `qa-manager` under [`ADR-0092`](ADR-0092-a-uat-round-files-what-a-source-settles-and-asks-the-rest.md) §5 — one of the three slots that round had, on one of three distinct screens, so `DEC-088`'s ordering question did not bite. It clears both halves of the bar: a concrete choice answerable in one sentence, and it bears on a player's ability to tell what is going on or what they may do. **Blocks nothing** — it gates no member of round 1's fix set, so the cycle continues (`ADR-0092` §5); an answer becomes a merged source either way, and any ticket it yields enters the **earliest subsequent** round's triage or the ordinary backlog, never the round that asked (`EPIC-12` §Termination rule 1). Recorded in [`STORY-1209`](../../tasks/stories/STORY-1209-round-1-uat-the-front-door-was-never-dressed.md) | [`STORY-1209`](../../tasks/stories/STORY-1209-round-1-uat-the-front-door-was-never-dressed.md) | before the cycle's next triage |
| DEC-090 | **The product owner's** — on the `account` screen, should *Attach a recovery address* — which asks for a *Current password* — appear on a device that has no password yet, or only once one exists? The section is offered on an unclaimed profile, where the field it requires cannot be filled. `ADR-0050` §4 settles what the screen **reads** (`deviceRouteLive`, and *no `ProfileResponse` field*) and no merged source settles what it **offers**, so the shipped state contradicts nothing and this is a question. Raised 2026-08-30 at round 1 of `/qa-cycle uat regression`'s triage and promoted by `qa-manager` under [`ADR-0092`](ADR-0092-a-uat-round-files-what-a-source-settles-and-asks-the-rest.md) §5 — one of the three slots that round had, on one of three distinct screens, so `DEC-088`'s ordering question did not bite. It clears both halves of the bar: a concrete choice answerable in one sentence, and it bears on a player's ability to tell what is going on or what they may do. **Blocks nothing** — it gates no member of round 1's fix set, so the cycle continues (`ADR-0092` §5); an answer becomes a merged source either way, and any ticket it yields enters the **earliest subsequent** round's triage or the ordinary backlog, never the round that asked (`EPIC-12` §Termination rule 1). Recorded in [`STORY-1209`](../../tasks/stories/STORY-1209-round-1-uat-the-front-door-was-never-dressed.md) | [`STORY-1209`](../../tasks/stories/STORY-1209-round-1-uat-the-front-door-was-never-dressed.md) | before the cycle's next triage |
| DEC-091 | **The product owner's** — on the `sign-in` screen, should *Back* on the sign-in screen return to the account screen it was opened from, or to the lobby? `ADR-0083` §2 settles where a **successful sign-in** lands (`#/account`) and nothing settles where a refusal or a retreat lands. The shipped *Back* returns to the lobby, two steps from where the player was. No merged source is contradicted. Raised 2026-08-30 at round 1 of `/qa-cycle uat regression`'s triage and promoted by `qa-manager` under [`ADR-0092`](ADR-0092-a-uat-round-files-what-a-source-settles-and-asks-the-rest.md) §5 — one of the three slots that round had, on one of three distinct screens, so `DEC-088`'s ordering question did not bite. It clears both halves of the bar: a concrete choice answerable in one sentence, and it bears on a player's ability to tell what is going on or what they may do. **Blocks nothing** — it gates no member of round 1's fix set, so the cycle continues (`ADR-0092` §5); an answer becomes a merged source either way, and any ticket it yields enters the **earliest subsequent** round's triage or the ordinary backlog, never the round that asked (`EPIC-12` §Termination rule 1). Recorded in [`STORY-1209`](../../tasks/stories/STORY-1209-round-1-uat-the-front-door-was-never-dressed.md) | [`STORY-1209`](../../tasks/stories/STORY-1209-round-1-uat-the-front-door-was-never-dressed.md) | before the cycle's next triage |
| DEC-102 | **The product owner's** — what does one press of the sizing row's stepper (`+`/`−`) move the dialled total by, and therefore which legal totals can the bar reach at all? Registered open 2026-08-31 by [`ADR-0101`](ADR-0101-pot-means-a-pot-sized-raise-and-the-fractions-share-its-base.md) §6. `ADR-0100`'s alternatives filed the step size under `DEC-101` (*"the step size is undecided and belongs with `DEC-101`'s control design"*); `ADR-0101` §6 declines it, because the question registered was what the **named presets** compute and a stepper is not a named preset — nothing in that ADR implies a step, and neither `docs/vision.md` nor `docs/duel-rules.md` names one, so an answer would be invented rather than derived. **It gates none of `ADR-0101` §§1–5** — the chips' arithmetic, their offer rule and `ADR-0100` §1's driver all stand without it, and the merged `whole-duel.test.tsx` gate needs only `min` and `all-in` — so the rewritten `TASK-120908` is **not** blocked by it. The constraint whoever answers it must weigh: `docs/duel-rules.md` §*Betting* says **No-limit**, the shipped slider reaches every legal total and five chips reach five, so whether the bar must keep a way to reach any legal total — and therefore whether the stepper may ship after the chips or must ship with them — is part of this question. Raised by an ADR, not promoted at a triage, so **not** one of `ADR-0092` §5's three slots | [`ADR-0101`](ADR-0101-pot-means-a-pot-sized-raise-and-the-fractions-share-its-base.md) §6 | before the sizing row's stepper ships |
| DEC-108 | **The product owner's** — when the table says *The duel is paused.*, may the action bar stay enabled? Raised 2026-09-01 by [`STORY-1214`](../../tasks/stories/STORY-1214-a-duel-played-by-hand-deadlocked-on-presence.md). Measured: under that notice every control read `disabled: false` — `Fold`, `Call 100`, `Raise to 200`, `All in 10,000` and all four sizing chips — and the human obeyed the sentence and waited while the duel was in fact playable. A script clicking through sees nothing wrong; a person reading the screen stops. **This is not filed as a defect, and deliberately so**: [`ADR-0046`](ADR-0046-the-table-says-away-timed-out-and-back.md) §6 declines the question **by name** — *"Whether the action bar's controls look disabled while the duel is paused"* — recording that `ADR-0028` §6 keeps `YourTurn` standing with `DUEL_PAUSED` as the refusal and that `STORY-0313`'s criteria *"already assume a live bar and an explained refusal"*. A live bar therefore contradicts no merged source, so a bug ticket would contradict a merged ADR and a QA case asserting the opposite would have no source (`ADR-0092` §5). What is genuinely open is whether *wait* and *you may act* should be sayable at the same moment at all — and, if not, whether the bar dims or the notice changes its words. **Blocks nothing**; `TASK-121401` writes `CORE-23` against the half that *is* merged (a client may never assert a game fact — the screen said paused while the server accepted the action) and leaves this untouched. The case that checks this answer is written when the answer merges | [`STORY-1214`](../../tasks/stories/STORY-1214-a-duel-played-by-hand-deadlocked-on-presence.md) | before the next `/qa-cycle audit` invocation |
| DEC-110 | **The architect's** — by what mechanism is a player who asks for a second room while holding a seat in a `PLAYING` room refused, and returned to the duel they are in? Registered open 2026-09-01 by [`ADR-0105`](ADR-0105-one-duel-at-a-time-and-the-refusal-hands-back-the-duel.md) §6, which fixes what must be true and deliberately writes no repair. The product half is answered: the request takes no seat, nothing moves, the player lands back on their own table, and the words are `ADR-0105` §4's. What is open is the carrier — a new `ProtocolError` value and the `PROTOCOL_VERSION` step [`ADR-0047`](ADR-0047-a-protocol-version-is-claimed-in-a-ledger.md) prices, or an answer assembled from frames that already exist (`replyToJoinRoom`'s `ALREADY_SEATED` branch already answers a seated player with `RoomJoined(code, seat)` *"exactly as a fresh seating would"*, derived fresh from the registry). Three constraints the mechanism must satisfy are `ADR-0105` §6's, not this decision's: nothing is vacated or forfeited; the client must be able to **name** the room it puts the player back in, including a tab that has never heard of it; and the refusal must not meter as a guess when `ADR-0022` §2's failed-join budget is built — nothing implements it at `develop` (`JoinLimits` and `TOO_MANY_ATTEMPTS` do not exist). **Blocks the ticket that implements `ADR-0105` §1**, and nothing else. The wire break is free until a client is deployed | [`ADR-0105`](ADR-0105-one-duel-at-a-time-and-the-refusal-hands-back-the-duel.md) §6 | before `ADR-0105` §1 is cut into a ticket |
| DEC-111 | **The product owner's** — may one player hold more than one `WAITING` room at once? Registered open 2026-09-01 by [`ADR-0105`](ADR-0105-one-duel-at-a-time-and-the-refusal-hands-back-the-duel.md), which answered `DEC-109` for the **running** case and names this hole in its own Consequences rather than quietly covering it. Reachable in three presses of shipped controls: *Create a duel room*, `Back to the lobby` (`ADR-0073` §1, which forgets the room and keeps the seat), *Create a duel room* again. Each waiting room a player holds can be joined by a rival and become a duel that player is not at — `ADR-0013`'s window, `ADR-0023`'s absent seat, `ADR-0014`'s coin — so `ADR-0105` §1 refuses a **second seat taken while a duel runs** but does not stop two duels **starting** in rooms whose seats were taken earlier. `ADR-0073` already accepted that cost for **one** waiting room, with eyes open and in as many words; whether it survives being multiplied is the question, and it is `docs/vision.md`'s *"Not a multi-table poker room"* against `ADR-0073` §3's shipped promise that *"The room stays open"*. `ADR-0105`'s Alternatives records the shape this will have to weigh (refusing the **rival's** join, which lands a refusal on a blameless stranger). **Blocks nothing** | [`ADR-0105`](ADR-0105-one-duel-at-a-time-and-the-refusal-hands-back-the-duel.md) §Consequences | before the first public link |
| DEC-114 | **The product owner's** — what does the table's `Pot` name: the **collected** pot, or the **total including this street's commitments**? Raised 2026-09-02 by the human after playing a duel end to end — *"pot size is not correct; it shoud include all bets including the latest one that was just made"* — and registered by [`EPIC-13`](../../tasks/epics/EPIC-13-the-living-table.md). **The shipped behaviour is deliberate, not a defect**, which is why this is a `DEC` and not a bug ticket under `EPIC-12`: `PotStrip.tsx` prints `view.pot` and says so in as many words (*"the pot is `view.pot` and not a sum of what the seats put in"*), so at 50/100 preflop the strip reads `Pot 0` while both blinds sit in front of the seats. **Nothing mechanical stands in the way of either answer** — `PlayerView` already carries each seat's `committedThisStreet` (`PlayerView.kt:108`) and `web-client/src/lobby/Lobby.tsx:154` already sums it into `potIncludingStreet`, so **no wire change, no `PROTOCOL_VERSION` bump and no engine change** is implied by saying *total* (`GameState.potTotal` exists and is not on the view). The sharp fact is that **the same screen already shows both numbers**: the sizing row's `pot` preset is a fraction of the total per [`ADR-0101`](ADR-0101-pot-means-a-pot-sized-raise-and-the-fractions-share-its-base.md) §1 while the strip prints the collected pot, so a player pressing *pot* gets an amount they cannot derive from what they can see. Answering *collected* is complete and needs saying out loud, but then owes the same screen an account of that gap. Bears on `EPIC-13` item 2 and gates it | [`EPIC-13`](../../tasks/epics/EPIC-13-the-living-table.md) | before EPIC-13 is split |
| DEC-115 | **The product owner's** — what happens when a player's clock runs out, and how does the **timebank** meet [`ADR-0013`](ADR-0013-disconnect-grace-period.md)'s **disconnect grace window**? Raised 2026-09-02 by the human — *"player shoud have 30seconds for move + 3m timebank; timebank also work for disconnection case; clock shoud visibly change each second"* — and registered by [`EPIC-13`](../../tasks/epics/EPIC-13-the-living-table.md). **The two numbers are given and are not the question.** What is open is the consequence: on expiry does the player **fold**, or **check when checking is free and fold otherwise** — the second is the near-universal online convention and the first is simpler to state — and does a timeout end the duel by forfeit or only the hand? And whether the timebank **replaces** `ADR-0013`'s grace window, **runs concurrently** with it, or is a second budget a disconnected player spends after it, given that `ADR-0013`'s window and this timebank now both answer *"the player is not acting"* with different durations and different screens (`presence-countdown.ts` is the only countdown on `develop` and is `ADR-0013`'s). It bears directly on a cost [`ADR-0105`](ADR-0105-one-duel-at-a-time-and-the-refusal-hands-back-the-duel.md) accepted by name — *"a player who wants out of a duel is **stuck** — no resign, **no turn clock**"* — which `ADR-0105` §1 sharpened by letting *Create a duel room* refuse. **It moves a duel coin**, which is what makes it the product owner's rather than the architect's. The mechanism is a separate architect's `DEC` the answering ADR should register, following `ADR-0105` §6: it is the only item in `EPIC-13` that moves the wire, so it is an `atomic:` ticket sized by `ADR-0070`'s probe and serialised by `ADR-0047`'s lock. Bears on `EPIC-13` item 4 and gates it | [`EPIC-13`](../../tasks/epics/EPIC-13-the-living-table.md) | before EPIC-13 is split |
| DEC-116 | **The product owner's** — does creating a duel land the host **at the table** rather than on the waiting screen, what stands in the rival's seat until they arrive, and what becomes of [`ADR-0073`](ADR-0073-the-waiting-screen-says-back-to-the-lobby-and-the-room-stays-open.md) §3's promise? Raised 2026-09-02 by the human — *"when player create a duel it shoud be redirected to room immidietly; copy link/invite btn shoud be where the table is(on the table, it shoud be drawn) and opponent icon shoud say:'waiting for oppent', after opponent join duel starts"* — and registered by [`EPIC-13`](../../tasks/epics/EPIC-13-the-living-table.md). **The waiting screen is where two shipped promises are made**, so moving off it is not only a layout change: `ADR-0073` §3 tells the host *"The room stays open"* and that they may walk away, and [`ADR-0105`](ADR-0105-one-duel-at-a-time-and-the-refusal-hands-back-the-duel.md) §2 leans on that exact sentence to justify **not** refusing a `WAITING` seat. If the host now sits at a table, settle whether `Back to the lobby` survives, where it goes, and whether the room-stays-open promise is still said and where. Also open: what the empty seat shows — the human says *waiting for opponent* — and whether the invite on the table is the link, a copy control, the room code, or all three, given [`ADR-0022`](ADR-0022-the-room-code-is-the-invite.md)'s no-oracle rule about what may be said of a code. **Explicitly not this decision**: `DEC-111`, whether a player may hold several `WAITING` rooms — item 5 will be tempting to widen into it and must not be. Bears on `EPIC-13` item 5 and gates it | [`EPIC-13`](../../tasks/epics/EPIC-13-the-living-table.md) | before EPIC-13 is split |
| DEC-117 | **The product owner's** — what does the table say about **the act just made**, and how long does it stand? Raised 2026-09-02 by the human — *"opponent last action shoud be visible on the screen; probably some icon plus info(like for check icon will be ok, for raise/bet icon plus size info)"* — and registered by [`EPIC-13`](../../tasks/epics/EPIC-13-the-living-table.md). Open: which acts get a mark (all six, or only those a player could miss), whether an amount rides along with `BET`/`RAISE`/`CALL` and in what words, whether the mark is the rival's alone or both seats', and **when it clears** — on the next act, at the end of the street, on a timer, or never within a hand. The duration half has a near precedent that does **not** reach it: [`ADR-0075`](ADR-0075-the-mark-lives-as-long-as-the-absence-that-produced-it.md) settles how long *the server's own action* stays on screen by tying it to the absence that produced it, and an act mark has no absence to outlive, so the rule has to be stated rather than inherited. `web-client/src/table/action-text.ts` already renders act text and is the surface this lands on. Bears on `EPIC-13` item 3 and gates it | [`EPIC-13`](../../tasks/epics/EPIC-13-the-living-table.md) | before EPIC-13 is split |
| DEC-118 | **The product owner's** — when a player **types** a bet amount that is not a legal raise, what does the table do: refuse the press, clamp to the nearest legal amount, or send it and let the server's rejection land? Raised 2026-09-02 by the human — *"player shoud be able to make bet using raw text input"* — and registered by [`EPIC-13`](../../tasks/epics/EPIC-13-the-living-table.md). **That a player may type an amount is the instruction, not the question**; the question is only the illegal case, which the sizing row cannot currently produce because every preset computes a legal quantity ([`ADR-0101`](ADR-0101-pot-means-a-pot-sized-raise-and-the-fractions-share-its-base.md) §1) and `ActionBar.tsx` renders `<button>` alone. A typed field is the first control that can express an amount the rules refuse — below `minRaiseTo`, above the stack, or not a number — and `rejection-text.ts` already exists for the third path. Note for whoever answers: [`ADR-0100`](ADR-0100-the-driver-reaches-an-amount-by-pressing-what-a-player-presses.md) §5 refuses **by name** a driver-only **slider**, any test-only prop and any `data-testid`; a typed field is a real player control and is welcome, but the e2e driver must keep **pressing what a player presses** and may not start setting its value, which is the guarantee `ADR-0100` bought. Bears on `EPIC-13` item 7 and gates it | [`EPIC-13`](../../tasks/epics/EPIC-13-the-living-table.md) | before EPIC-13 is split |

## Answered decisions

| ID | Question | Answered by |
| --- | --- | --- |
| DEC-112 | **The product owner's** — does *fits the phone* admit a sub-pixel residual? Raised 2026-09-02 by [`TASK-121402`](../../tasks/tasks/TASK-121402-the-duel-table-column-fits-the-phone-it-is-nested-in.md), whose 48 px repair left the duel-table document at a true **664.90625 px against 664** (`scrollHeight` 665) with both action buttons at or above the fold and every other criterion met — the ticket's acceptance criterion demanded `≤` and predicted 664 / 664, its Out of scope ordered the implementer to stop short of the next give, and the implementer stopped and said so, which is [`ADR-0103`](ADR-0103-the-table-fits-the-phone-and-the-cards-give-before-the-numbers.md) §3's stop rule operating at its first live boundary. Registered and answered in the same PR (the `DEC-039` path — it never appeared in an open table). **Answered: yes, at exactly one boundary — a reading of one integer over is judged on the true geometry, and a true excess strictly under one CSS pixel is met**; one pixel or more, or a two-integer excess, is `R2` `not met` as ever. **The fence is the instrument's own unit and cannot widen**: a tolerance that can hold painted content is the *"relaxed phone bar"* `ADR-0096` §2 forbids by name, so widening belongs to the human. **`TASK-121402` merges as scoped**; **the last 0.90625 px is re-filed once** as ordinary backlog, its closer confined to `ADR-0103` §3.1 whitespace; and a future round **may not file a sub-pixel overflow as anything** — while clipped content, unmeasured beats and full-pixel residuals stay filable on the ordinary path. Derived from the vision's first success condition — *"Send a link. She opens it in a browser. We play a full heads-up match."* — read as `ADR-0103` read it: *fits* is for playing unscrolled, and nothing a player must read or press can hide below a fold no glyph fits under | [ADR-0106](ADR-0106-a-sub-pixel-residual-is-a-fit-and-one-pixel-is-the-fence.md) |
| DEC-109 | **The human's** (routed to the product owner, escalated on review) — may one player hold seats in **two live rooms at once**? Registered open 2026-09-01 by [`ADR-0104`](ADR-0104-a-frame-reaches-the-connection-in-the-room-it-is-about.md) §10, which split it off rather than deciding it. **Answered: no — a player holds at most one seat in a *running* duel.** Derived from `docs/vision.md`'s *What it is not* — *"Not a multi-table poker room. No 6-max, no 9-max, no tournaments, no sit & go, no cash games."* — with the **shape** of the answer taken from *What it is*: *"One duel coin per win… A counter of duels won."* While a player holds a seat in a `PLAYING` room, `CreateRoom` and `JoinRoom` from them **take no seat**: the room asked for is untouched, the seat and duel held are untouched, and **no coin moves**. **Scoped to `PLAYING` alone** — a `WAITING` seat is not refused (`ADR-0073` §3 promised the host on screen that the room stays open) and a `FINISHED` seat is not refused (`Room.offerRematch` agrees only when both seats have offered), while a seat inside `ADR-0013`'s grace window **is** refused, which is the grace period working. **The refusal hands the player back the duel they are in** — the table, never a lobby, never a dialog — and is safely repeatable. **Two sentences and no third**: `You are already in a duel. Finish it to start another.` The player whose invite was clicked sees nothing, because nothing happened to them. **No repair is written**: §6 states what must be true and registers `DEC-110` for the architect; §7 inherits `ADR-0104` §9 and explains no fold. **Vacating the first seat was rejected by name** — every coin in the merged tree is settled by a duel the engine ran to a chip holder (one `sink.record` call site, reached only with an outcome), so a forfeit-on-navigation would be the product's first, would be the least reversible option in the set, and is a commitment `docs/vision.md` does not make. Costs named: a player who wants out of a duel is **stuck** — no resign, no turn clock, and a `PLAYING` room is never reaped, so their exit depends on their rival still acting; *Create a duel room* can now say no; two people are left slightly in the dark about a clicked link, on purpose; and **the multiple-waiting-rooms hole stays open as `DEC-111`** | [ADR-0105](ADR-0105-one-duel-at-a-time-and-the-refusal-hands-back-the-duel.md) |
| DEC-107 | **The architect's** — where is a presence frame scoped to the room it is about, so that a client cannot apply one about a room it has left? Raised 2026-09-01 by [`STORY-1214`](../../tasks/stories/STORY-1214-a-duel-played-by-hand-deadlocked-on-presence.md) out of a duel the human played by hand and could not play, and measured on the wire: `OpponentPresence(AWAY, 60000)` arriving **320 ms before the recipient sends `CreateRoom`**, with a roomless controlled negative that was clean in both directions. **Answered: a frame is delivered to the connection that is in the room it is about.** `deliver` resolves a seat to a player exactly as it does today and then asks for that player's writer **for this room**; a connection in another room, or in none, is skipped silently — the idiom `deliver` already documents for a seat mid-reconnect. `ConnectionDirectory.writerFor(player)` is **deleted** so no unscoped lookup survives for a later call site to reach for, `RoomMembership` moves to `duels.poker.server.session` and is registered with its writer with a **`@Volatile`** `code`, and **all seven `deliver` call sites are untouched**. **`PROTOCOL_VERSION` does not move** — no declaration changes, `ADR-0047` §2's fingerprint is unchanged, no ledger row, and `TASK-121403` is **not** `atomic:`. **Production is unchanged**: `disconnect()` still builds the frame whether or not the other seat is connected to that room, because the registry knows nothing of writers by design — that half of the question is answered *yes, unchanged*. **The story's reading was confirmed and understates the defect**: the widening lives in the composition `seat → PlayerId → writer-anywhere`, so it carries every frame `deliver` routes and needs no race at all; no card can leak, because the recipient is always the player the frame was projected for. The client's store is scoped too — a `RoomJoined` naming a **different** room re-initialises it — and the ADR says in as many words that this half is **not** what makes the system correct. Costs named: a documented ignorance retracted, a `@Volatile` obligation nothing in the type system asks for, a second indistinguishable cause of a silent drop with no counter, load-bearing ordering, and and **the unscoped *send to a player wherever they are* foreclosed** — deliberately, and with **no current victim**: `writerFor` has one call site and this decision is it, so the cost is prospective. Splits off `DEC-109` for the product owner; leaves `DEC-108` untouched | [ADR-0104](ADR-0104-a-frame-reaches-the-connection-in-the-room-it-is-about.md) |
| DEC-106 | **The product owner's** — what does the duel table look like on a phone: does it get a phone treatment at all, what gives at 390 wide, and which card carries it? Raised 2026-09-01 out of round 1 of `/qa-cycle audit smoke` (run 2026-08-31; [`STORY-1213`](../../tasks/stories/STORY-1213-round-1-audit-three-criteria-unmet-and-a-table-that-names-the-wrong-winner.md), `R2` `not met` at four decision beats — 885/664 preflop, `bottom: 820.578` facing a raise, 868/664, 866/664) and by [`TASK-121302`](../../tasks/tasks/TASK-121302-the-decision-fits-a-390-by-664-screen.md), which asserted *"No `DEC` is needed here — this is conformance to a merged card"* and could not close it: `min-height` is a floor, `flex-grow` distributes slack, and **the merged card itself measures 732 against 664 at 390 × 664**, so the card describes no phone and a coder conforming to it still fails. **Answered: the table fits the phone, it is one table at two widths, and the cards give before the numbers** — whitespace, then the rival's face-down hand, then the player's hole cards (never smaller than a board card), then the board; nothing else gives. `design/screens/duel-table.html` is amended and gains a same-markup 390 × 664 frame; `TASK-121302` is rewritten and blocked behind that design ticket. Registered and answered in the same PR (the `DEC-039` path); it never appeared in an open table | [ADR-0103](ADR-0103-the-table-fits-the-phone-and-the-cards-give-before-the-numbers.md) |
| DEC-105 | **The architect's** — how is a runout's pacing produced, and what does a step cost? Raised 2026-09-01 by [`TASK-121301`](../../tasks/tasks/TASK-121301-the-runout-arrives-street-by-street-on-the-screen-too.md) against `R1` `not met` at beat 5 of round 1 of `/qa-cycle audit smoke`, and routed by [`ADR-0096`](ADR-0096-the-audit-judges-a-whole-duel-against-a-frozen-rubric.md) §2 — *"how a beat is paced is settled by the ticket that repairs it… with the architect where [a still] cannot [hold it]"*. One question in two halves: **where the pacing lives** — `poker-server` sending one `Snapshot` per street, or `web-client` holding the `Snapshot` it already has — and **what a step costs**, in hold time and in what a reconnecting client does. **Answered: a hand's ending is painted as a sequence of steps, and the client owns the clock.** The store paints a hand-completing `Snapshot` as one step per preceding `StreetDealt` plus a final step, queues every frame behind them in arrival order, and lags exactly two fields — a **prefix of the server's own board**, and that `StreetDealt`'s own street. A step is **600 ms**, named once in `boot.ts`, with `0` meaning synchronous so the four recorded-frame e2e suites are untouched; a **reconnecting client jumps to the end** because `resumeFrames` sends it no `Events` to pace against. **The ticket's reading was confirmed on three points and incomplete on a fourth that decides the answer**: `act` calls `advance` in the same call and both hands' frames are delivered in one batch, so board-slicing alone would be erased a millisecond in. `PROTOCOL_VERSION` does not move, and would not have moved for a server-paced answer either — the server option lost on the engine handing out one state per runout, a wall clock in the authoritative path, and its inability to fix the batch it creates. Costs named: the stacks settle at the first step, every hand ends 600 ms later, and the screen trails the server by design. Registered open by `TASK-121301`, answered the day after | [ADR-0102](ADR-0102-a-hand-ends-in-steps-and-the-client-owns-the-clock.md) |
| DEC-101 | **The product owner's** — what amount does each named sizing preset set: what does a chip labelled `pot` (and `⅓`, `½`) promise a player in a heads-up duel? Registered open 2026-08-31 by [`ADR-0100`](ADR-0100-the-driver-reaches-an-amount-by-pressing-what-a-player-presses.md) §6, which settled the technical half — a named preset **computes the quantity it is named for**, and the pot reaches `ActionBar` through `Lobby.tsx` with no wire change — and refused this one, because the card's single worked example (`Pot 2,450`, rival `committed 400`, `Call 400`, `pot` selected, `Raise to 3,250`) is satisfied by **two** formulas that disagree the moment the hero has committed on the street, and excludes the textbook pot-sized raise, 3,650. **Answered: `pot` means a pot-sized raise, and `⅓` and `½` are fractions of the same base** — the pot as it will be after the call — so `pot → callTo + base`, `½ → callTo + floor(base/2)`, `⅓ → callTo + floor(base/3)`, `min` and `all-in` unchanged as the server's own numbers, and a fraction chip whose amount falls outside `[floor, allInTo]` is **not offered** rather than clamped. `TASK-120908`'s `floor × 4/3` does not ship, its file set stays at `ADR-0100` §7's six, and `design/screens/duel-table.html` is in arrears by one number in two places. The stepper's step was split off as `DEC-102` and refused | [ADR-0101](ADR-0101-pot-means-a-pot-sized-raise-and-the-fractions-share-its-base.md) |
| DEC-100 | **The architect's** — how does a scripted duel reach a bet amount once the table's sizing control is the card's discrete presets rather than a range slider? Raised 2026-08-31 by [`TASK-120908`](../../tasks/tasks/TASK-120908-the-tables-sizing-control-is-the-cards-presets.md), whose coder replaced the range input, got all three of its new tests and the whole of `ActionBar.test.tsx` green at 25, then hit **24 failures across four merged e2e files** for a reason outside its two-file budget and routed rather than widening scope (`CLAUDE.md` rules 4 and 5): `web-client/src/e2e/drive-duel.tsx:125` reaches an arbitrary amount with `fireEvent.change(getByRole("slider"))`, and removing the input makes that query throw in `whole-duel.test.tsx`, `duel-secrecy.test.tsx`, `claimed-here-recovered-there.test.tsx` and `drive-duel.test.tsx`. **Answered: the driver presses the sizing row and reads the action button before it clicks**, throwing by name when no press reaches the recorded total — no test-only door, no driver-only slider, no re-recorded frame, and none of the four files edited. Its product half — what `pot`, `⅓` and `½` each compute — was split off as `DEC-101` and refused. Registered and answered in the same PR (the `DEC-039` path); it never appeared in an open table | [ADR-0100](ADR-0100-the-driver-reaches-an-amount-by-pressing-what-a-player-presses.md) |
| DEC-099 | **The product owner's** — where does the product's wordmark belong: on every screen, or on the front door alone? Raised 2026-08-31 by [`TASK-121004`](../../tasks/tasks/TASK-121004-the-front-door-finishes-the-card-it-started.md), whose third scope item quotes `<h1 class="text-title">Poker Duels</h1>` and directs dressing it as [`design/screens/create-duel.html`](../../design/screens/create-duel.html)'s front-door frame draws — but the markup lives in `web-client/src/App.tsx` above `<Lobby />`, not in `Lobby.tsx` (the ticket's declared file), and renders unconditionally on every surface the client has, so the dressing would have put the front door's wordmark on all of them: a change to what a player sees everywhere, not a dressing change on one screen. The coder shipped the ticket's other two scope items (PR #1234) and refused to guess this one (`CLAUDE.md` rule 5). **Not** one of `ADR-0092` §5's three promoted slots — raised by a ticket, like `DEC-092` and `DEC-095`. Registered and answered in the same PR (the `DEC-039` path) — it never appeared in an open table | [ADR-0098](ADR-0098-the-wordmark-belongs-to-the-front-door-alone.md) |
| DEC-098 | **The architect's** — is the audit rubric [`ADR-0096`](ADR-0096-the-audit-judges-a-whole-duel-against-a-frozen-rubric.md) §2 **itself**, grown by an amending ADR the way [`ADR-0092`](ADR-0092-a-uat-round-files-what-a-source-settles-and-asks-the-rest.md) §2 and [`ADR-0097`](ADR-0097-a-resize-is-two-numbers-and-the-observer-is-the-fifth-file.md) §4 both grew [`ADR-0090`](ADR-0090-a-skill-may-write-the-catalogue-or-run-it-never-both.md) §2's declared-file set, or a **working document elsewhere** that §2 founds — and what does a round cite when it records a criterion `not met`: an ADR section, or a document path? Raised 2026-08-31 by the planner splitting [`STORY-1212`](../../tasks/stories/STORY-1212-the-audit-focus-the-observer-the-resize-and-what-a-criterion-costs.md); blocked nothing, and was answered the same day — before any round cited a criterion and before a sixth criterion arrived, the window its own *Due* named. **Answered: the rubric is §2 itself, and no working document ever exists.** Every future criterion is *born* merged — `ADR-0096` §3 routes a proposal to a `DEC` whose answer is *a merged PR either way*, and a merged answer is an ADR (`CLAUDE.md` rule 5) — so a document could only ever have held a **copy**: a second register from its first line, able to disagree silently with the law it transcribes, guarded by nothing (*two copies of a rule drift*, `ADR-0092` §8). Growth is one amending ADR — ids sequential (`R6` next), the criterion in §2's own three-column form, the priority order restated as ids only, `ADR-0096`'s index row annotated in the same PR — and a round cites `R<n>` with the ADR section that states it, never a path. `TASK-121203` and `TASK-121205` already cite the decided form and change nothing | [ADR-0099](ADR-0099-the-rubric-is-the-adr-section-and-a-criterion-is-born-merged.md) |
| DEC-097 | **The architect's** — by what mechanism does [`ADR-0096`](ADR-0096-the-audit-judges-a-whole-duel-against-a-frozen-rubric.md)'s **audit focus** run under [`ADR-0089`](ADR-0089-a-browser-drives-this-client-for-a-qa-round-never-for-a-gate.md) §2's three conditions? Two halves, both about merged conditions on the browser permission. **(a) The declared-file set** — is `ADR-0090` §2's set amended a second time for an audit observer, or does the focus run with no new agent file? **(b) Two viewports inside one live duel** — `ADR-0096` §4 walks the whole duel at 390 × 664 and re-answers `R2`/`R3` at 720 × 900, and relaunching mid-duel re-seats the device (`ADR-0018`) and deals a different board, so the shapes cannot be two passes over the same beat: either the harness resizes a **live tab** — and is `Emulation.setDeviceMetricsOverride` a read, or the application-state write §3 forbids? — or §4 is unbuildable as written and its second shape returns to the product owner. Registered 2026-08-31 by `ADR-0096`; **(b) restated the same day** after the human's *"we have to support phone size"* turned one viewport into two. **Answered without returning anything to the product owner**: the mechanism exists, so §4 stands as merged. Blocked the first audit round | [ADR-0097](ADR-0097-a-resize-is-two-numbers-and-the-observer-is-the-fifth-file.md) |
| DEC-096 | **The product owner's, with three halves settled by the human** — what process catches a product that is *raw*, and what authority does it need? Raised 2026-08-31 by the human after playing the product in two browsers immediately following the `/qa-cycle uat regression` that ended `PASS` with `B(3) = 0`: the round **ends immediately after an all-in** with no runout, *"a lot of times it is hard to understand what is going on"*, the pot and bet amounts are not legible, **scrolling** is required to see the opponent's actions, weird copy and text without spacing. Three merged rules made every one of those unreportable, and the ADR diagnoses each: `ADR-0092` §3's classifier has **no merged source of the general kind** to contradict, so a judgment about legibility could only ever be a question; `EPIC-12` §Termination rule 2 puts everything that makes a product feel unfinished in the `medium`/`low` band that is *"never scheduled by this cycle"* — **seventeen such tickets are in the backlog**; and coverage is **per-screen**, so pacing, a property of a sequence, has no row to be reported in. The human settled three halves and they are recorded verbatim rather than chosen: the audit **may** file findings contradicting no merged source, the benchmark is category quality with the vision's aesthetics, and audit findings are repaired **regardless of severity**. Registered and answered in the same PR (the `DEC-039` path) — it never appeared in an open table. Registers `DEC-097` for the architect. Its one escalation — **is a phone a supported surface?** — the human answered the same day (*"we have to support phone size"*), so §4 walks **two shapes** and `DEC-097`(b) became the load-bearing half. **`docs/vision.md` is not amended**: *"She opens it in a browser"* and *"Two browsers, one room link"* name a browser and **no device**, so the call resolves a silence rather than contradicting a sentence, and §4 names what would change that. **Phone landscape was left open by §4 and is now answered** — the human's *"we are ok to support only one orientation for mobile form factor"*, recorded by [`ADR-0097`](ADR-0097-a-resize-is-two-numbers-and-the-observer-is-the-fifth-file.md) §5 | [ADR-0096](ADR-0096-the-audit-judges-a-whole-duel-against-a-frozen-rubric.md) |
| DEC-095 | **The product owner's** — when a hand ends, does the table state the pot's winner and amount in place of the pot line, as [`design/screens/duel-table-states.html`](../../design/screens/duel-table-states.html) draws it — and **does the product name the made hand at showdown at all**, given that no wire field carries one? The card draws both halves of a two-line banner; only one is buildable. `PotAwarded` carries `seat` and `amount`, so *You win 4,850* is a transcription, while *Two pair, aces and sevens* exists on no `GameEvent` and could only be computed by the client — forbidden by `CLAUDE.md`'s non-negotiables and `ADR-0002`, and gated by `web-client/src/table/no-derivation.test.tsx`'s `HAND_TALK` matcher. Raised 2026-08-30 by [`TASK-121101`](../../tasks/tasks/TASK-121101-the-table-says-who-won-the-hand-it-just-finished.md), whose triage refused to guess it (`CLAUDE.md` rule 5) and which is unstartable until this lands; **not** one of `ADR-0092` §5's three promoted slots, so round 3's cap is untouched. Registered and answered in the same PR (the `DEC-039` path) — it never appeared in an open table | [ADR-0095](ADR-0095-the-table-states-who-took-the-pot-and-never-names-a-hand.md) |
| DEC-092 | **The product owner's** — does the product build the two screens its merged cards draw — the *offered seat* of [`design/screens/join-duel.html`](../../design/screens/join-duel.html) (*ImKate challenges you*, a stakes line, a room-code chip, *Take the seat*) and the dedicated code-entry screen of [`design/screens/enter-code.html`](../../design/screens/enter-code.html) — or are those two cards corrected to the join path that shipped, where an invite link seats the player straight into a dealt hand and a code is typed inline on the first screen? Raised 2026-08-30 by [`TASK-120907`](../../tasks/tasks/TASK-120907-the-join-path-ships-neither-screen-its-cards-draw.md), which routed it rather than guessing (`CLAUDE.md` rule 5) and is unstartable until it lands; **not** one of `ADR-0092` §5's three promoted slots, so round 1's cap is untouched. Registered and answered in the same PR (the `DEC-039` path) — it never appeared in an open table. Three consecutive UAT rounds (`STORY-1205`, `STORY-1209`, `STORY-1210`) filed the same `high` conformance finding against these cards, and would have gone on doing so: `ADR-0092` §3a judges the shipped screen against its merged card, and nothing said which artefact was the product | [ADR-0094](ADR-0094-opening-the-invite-is-taking-the-seat.md) |
| DEC-086 | **The product owner's** — what, if anything, is the written bar for *"ready for real users"*? Raised 2026-08-30 by the human's UAT request, whose stated goal is that after the pass the *"product should look like ready for real users"*. [`ADR-0089`](ADR-0089-a-browser-drives-this-client-for-a-qa-round-never-for-a-gate.md) §2c forbids citing any round as coverage, and [`ADR-0092`](ADR-0092-a-uat-round-files-what-a-source-settles-and-asks-the-rest.md) §2 extends the corollary by name: no round record — a `PASS` included — may be cited as the thing that made the product ready, so the bar, if one exists, must be **decided**, not measured into existence; *there is no written bar; the human judges by reading the round records* was registered as a complete answer | [`ADR-0093`](ADR-0093-ready-for-real-users-is-said-of-the-shipped-artifact.md) — **the phrase is said of the shipped artifact, and the bar is two facts, neither a test result.** Answered by the product owner on 2026-08-30, deriving from the success condition's second clause — *"She opens it in a browser"*: what she opens is the artifact the product serves her, and today no proof of record describes it — every layer, unit to UAT, loads `npm run dev` on `localhost` while `dist/` is opened by nothing (`ADR-0088` gap 3, still true). **Fact a: the artifact under test is the artifact shipped** — the proofs of record (the `ADR-0088` §2 hand-check and the QA/UAT rounds the human reads) load the built bundle; the serving mechanism is **`DEC-087`, the architect's**, registered open by the same PR, and until it is answered the bar is unmeetable — correctly, since a phrase with no referent should be unavailable. **Fact b: recovery is completable by a real user in the offered deployment** — a mail transport bound, never `NoRecoveryMailer` — because `ADR-0031` §7 scoped the senderless build's validity to *"development and tests"* and an offering to real users is neither, `ADR-0087`'s acknowledgement promises a link that must be able to arrive, and `ADR-0031`'s Consequences price the failure as the product's one total, permanent loss. **Meeting the bar does not make the product ready**: it is a precondition on the phrase, never a certificate — readiness stays the human's judgment made by reading (the escape hatch's kernel, kept), no record may ever be cited as the bar being met, no proof is required to have *passed* (that would cite a record), and the bar gates the **phrase**, never the **act** of offering, which stays the human's own with no ADR scheduling it. The event is `ADR-0063` §5's — strangers beyond *"people the author invited personally"* — so the founding moment (*send a link, she opens it*) is not gated, and that ADR's farm expiry falls due at the same event, on its own terms, named beside this bar rather than collected into it. **Five absences are out under a written rule** — a gap enters the bar only when its failure would falsify the success condition at first contact or destroy something a player cannot get back: history paging past 10, ladder paging (`05-04`), the third device (`CORE-03`), the hundred-way tie (`ADR-0065` §1) and real network each stay known absences a round reports without them bearing on the bar, every one decided in a merged ADR and proved at the layer that computes it, with the reopening trigger named (a real user losing something unrecoverable through one of the five means it was misclassified, and the correction is a superseding ADR). **No machine enforces the bar and none may be built** — a `verify:` asserting *ready* is `ADR-0024` §3's opinion-in-a-verify-block one abstraction up. Costs named: the ADR tells the human their UAT pass cannot end at the phrase they aimed it at; the bar waits on `DEC-087` and a transport (a twenty-first pointer at `EPIC-07`, but a **blocking** one, which fails loudly rather than collecting nothing); §1b removes the senderless-deployment freedom and may cost a mail bill; and enforcement is prose plus the review gate, which is weaker than a gate |
| DEC-085 | **The architect's, with two halves settled by the human mid-decision** — may a UAT round exist, what may it judge, and what may it file? Raised 2026-08-30 by the human's request for a second testing pass after `/qa-cycle`, over the same catalogue, focused on UX and design conformance — *"is the main info properly highlighted? is it clear to user what is going on? are all options accessible?"* — with a missing design *"reported as bug → addressed (according to workflow in build-epic) → then testing should resume"* and the goal that the product *"should look like ready for real users"*. Registered and answered in the same PR (the `DEC-039` path). Two things genuinely blocked it: [`ADR-0089`](ADR-0089-a-browser-drives-this-client-for-a-qa-round-never-for-a-gate.md) §2's permission is *"exactly their conjunction"* — three conditions written for a **QA** round, and a UAT round is a round-kind it never contemplated, where covering it *by analogy* would make §2 satisfiable by relabelling; and [`ADR-0024`](ADR-0024-design-follows-the-code-workflow.md) §3 (*"a verify block cannot hold an opinion, so it does not try"*) with [`ADR-0091`](ADR-0091-design-gets-no-agent-a-new-screen-owes-a-card.md) reserve visual judgment for the human, while the request asks an agent to question exactly that. Mid-decision the human settled the routing — **a UX question raised during UAT is delegated to the `product-owner`, whose decision may create a bug ticket through `build-epic`** — and the sequencing: **run UAT now and let it file the six missing cards** rather than wait for `ADR-0091` §5's retrofit story | [`ADR-0092`](ADR-0092-a-uat-round-files-what-a-source-settles-and-asks-the-rest.md) on 2026-08-30 — **UAT is a second focus of the same cycle: it files what a merged source settles and asks the product owner the rest.** `/qa-cycle uat <scope>` is the human's own message and chains from nothing — the QA focus never runs the UAT focus, neither report prints the other's command, and a preceding QA cycle is practice, never a checked precondition, because the check would cite a round as a gate (§2c). **The three conditions are checked one at a time and hold**: a `shot` verb captures screenshots over CDP with Node built-ins into the round's temp directory, never committed, **no image-diff tooling ever** (§2a); §2c gains the corollary that no round — a `PASS` included — may be cited as what made the product *ready*, with the bar itself registered as `DEC-086`, the product owner's; a screenshot and a rendered card are **reads** under §3; and §4 transposes to *observable by a human looking* — at the screen, and the rendered card beside it, by eye. **The classifier at the boundary is the merged source**: an observation files as a finding only when it contradicts something merged — a card, a token, an owned literal, an ADR section, a vision sentence — and a judgment with no merged source is a **question**, `ADR-0090` §4 transposed from authoring to observing. Three checks per screen-state: conformance to the merged card (transcription, not taste, and not pixel equality), reachability (*"found N matches, all invisible"* is already mechanical), and copy against merged sources. **A missing card is `high`, outside `B(N)`** — registered debt being collected, not decay — its repair **is** the card (composed per `ADR-0091` §3, verdict trailing), its dedupe key is the card's own path, and the screen is walked, not parked: only conformance reads `BLOCKED — no card`. **Questions go through a bounded gate**: the `uat` agent asks at most three per screen and answers nothing; `qa-manager` — the only promoter — registers at most three `DEC`s per round, one per screen, each a concrete choice answerable in one sentence and bearing on a player's ability to tell what is going on; below the bar is recorded unanswered, never a ticket. **An answered question becomes a merged source** either way — a change makes the shipped state a finding at the next triage, a blessing closes the question mechanically. The frozen set survives: decision-born tickets enter the earliest **subsequent** triage or the backlog, and are the **third `B(N)` exclusion** beside harness defects and missing cards, because counting improvement as decay trips `STOP_DIVERGING` on a product getting better. A round that unlocks newly-carded screens is a **baseline round** — rule 4 skips its comparison, rule 5's three-round budget still binds — and every UAT verdict is qualified inline (`PASS (conformance unjudged on 6 of 7 screens)`) so a round that mostly authored cards says so in the one line anyone reads. `STOP_BLOCKED` is scoped to an escalation that **gates the current fix set**; otherwise `notify.py blocked` carries it and the cycle continues. **One new `uat` observer** (opposite refusal lists cannot share a file with `qa.md`, which stands byte-unchanged); one manager, one skill, one ledger — dedupe spans focuses. `ADR-0090` §2's declared set grows three→four (`agents/uat.md`, mention-only, by the clause's own licensed route). Costs named: round 1's conformance is near-tautological until the pane verdicts land on cards composed from the very screens they judge; up to nine Opus dispatches per invocation; three prose exclusions in a count that used to be simple |
| DEC-084 | **The architect's** — does design work need its own agent, or does it stay on the `coder`/`reviewer` workflow? Raised 2026-08-30 by the human; registered and answered in the same PR (the `DEC-039` path — it never appeared in an open table). The tension: the workflow demonstrably ships design — 42 merged commits under `design/`, 43 tickets, four stories `done` — yet `design/screens/` holds only the v0.1 duel flow, `EPIC-04` and `EPIC-05` shipped six of the seven `Screen` members (`duels`, `leaderboard`, `account`, `sign-in`, `verify`, `reset`) with no design card, no rule anywhere makes a new screen ask for one, and `check-drift.sh` scans `design/` against itself and never reads `web-client/` | [`ADR-0091`](ADR-0091-design-gets-no-agent-a-new-screen-owes-a-card.md) on 2026-08-30 — **no designer agent and no design skill; the taste carrier is the card, and a new screen owes one before its build.** An agent is added here when a decision needs an owner, and this one would own none: `ADR-0024` §3 already gave taste to the human at the render and structure to the `light` review, so an agent between them is a hop — and it would have prevented neither recorded failure, since nothing dispatched any design work at all (an agent nobody dispatches designs nothing) and the drift tax was mechanical. The trigger the epics lacked becomes **the plan-story rule, in `.claude/agents/planner.md` alone**: a story whose split adds a `Screen` member (`web-client/src/routing/screen.ts`) names the card it implements in `## Design notes`, or the split's first ticket **is** the card; a new surface behind an existing slug — the account offer was one — is the planner's judgment under the same rule. `EPIC-06`'s interactive deviation generalises instead of expiring with the epic: **minting is interactive with the human, composing is an ordinary dispatched ticket**, and the human's visual verdict may trail the merge — the named price is a screen shipping a look the human later rejects, bought so unattended runs never stall at a pane. The adoption evidence was **corrected before being acted on**: the zero-token grep measured direct references, but `app.css`'s `@theme static` block rebinds every Tailwind namespace to `var(--pd-*)` under three merged client guards (byte-identity, theme references, colour literals), so the real leak is raw lengths in arbitrary values — `380px` ×21, `560px` ×2, `460px`, `1.5em`, fifteen files — and **`check-drift.sh` therefore stays out of `web-client/`**: adoption is gated where consumption happens, by a fourth client guard refusing raw lengths in arbitrary values, while the shell gate keeps guarding `design/` against itself in `tickets.yml`. The debt is registered, not forgiven — `EPIC-06` reopens with a retrofit story for the six slugs, its 2026-08-15 metrics standing as history — and `ADR-0024`'s generator trigger (*"cards multiply or a grep actually fires"*) is recorded as fired, its promised ticket owed. A deliberately deferred piece is named with its own trigger: a slug↔card coverage gate becomes a ticket only if a screen ships undesigned with the rule in force. Nothing in it was the product owner's: that every player-facing screen carries the identity is the vision's *Positioning* sentence and `EPIC-06`'s merged goal already |
| DEC-083 | **The architect's** — may a single human-typed skill both author catalogue content and then run the QA cycle over it, or must authoring and running stay two separate human commands? Raised 2026-08-29 by the human's request for *"one skill"* that writes the missing cases for existing functionality and then runs a full cycle. [`ADR-0089`](ADR-0089-a-browser-drives-this-client-for-a-qa-round-never-for-a-gate.md) §2b — one of the three standing conditions the browser permission is the **conjunction** of — says a cycle is started by *"a human's command — not a merge, not a cron, **not another skill invoking it as a step**"*, and a composite is literally the third while being plainly the first. §2 says a condition that stops holding *"returns the question as a new `DEC-NNN`"*, so this is that `DEC` rather than an argument around the clause. **Blocks the authoring of every suite `docs/test-plan.md` lists as *not yet written*** — `EPIC-04`, `EPIC-05`, `EPIC-06` | [`ADR-0090`](ADR-0090-a-skill-may-write-the-catalogue-or-run-it-never-both.md) — **a skill may write the catalogue or run it, never both in one turn.** Answered by the architect on 2026-08-29. **The composition reading is taken and the automation reading is rejected by name.** Four things decide it: *"a human's command"* forbids nothing unless it means the **immediate caller**, since every automated trigger has a human behind it at some distance; `ADR-0089` §Consequences gave §2b a future job — *"the sentence to point at"* when a nightly is proposed — which a clause satisfiable by relabelling cannot do; §2's own closing sentence pre-refuses the argument form (*"condition **b** failing, not a refinement of it"*); and the composite's **sole** value is that the cycle begins while the human is elsewhere, making it a cron whose clock is the length of its first half, so it fails even the attended/unattended test the permissive reading proposes. **§2b's heading and one sentence are amended in the open** — *"No gate"* becomes *"No gate, and one caller"*, and the trigger sentence becomes *a cycle is started by the human's own message and nothing else*, with `/qa-cycle` the first act of the turn it starts; the rest of §2b and §§2a, 2c, 3, 4, 5 and 6 stand byte-unchanged, as do `ADR-0088` §1's body and §§2–5. **The human types two commands**, and the expensive half is still one: `qa-cases` is licensed to plan, write and land the missing suites through `build-epic` and the ordinary review gate, and may not bring the stack up, start a browser, dispatch `qa` or `qa-manager`, or invoke the cycle by any route — **its terminal act is a report naming, verbatim, the command the human types next.** **On the author-and-grade worry**: the `qa`/`qa-manager` separation survives a composite untouched, so the blunt objection is answered by the reviewed PR; what a diff-versus-ticket review cannot answer is whether a case's `expect` is a claim the product ever made, and a composite hands `ADR-0089` §4 — *"a rule an agent follows, not an exit code"* — a whole catalogue of **never-executed** cases in the same unattended stretch that step 4 is merging production diffs. So every case `qa-cases` writes carries a `source` column, and **a case whose expectation has no merged source is not written**: it becomes a `DEC` for the **product owner**. **And what that grounding does not buy is stated**: a merged decision proves what was *decided*, not what *shipped*, so a suite `qa-cases` writes is **provisional until its first round** — `docs/test-plan.md`'s *"filled in when an epic is first tested, not before"* is amended in the same PR to admit the authored-then-tested path, and the honest expectation for that first round is a pile of harness tickets that can end `PASS` while a dozen of its own cases were wrong. Checkable by one command over a declared three-file set, with the half no grep can catch stated rather than claimed. Chosen in the direction whose **mistake announces itself** — forbidding costs a visible wait every round and generates its own evidence for reversal, while permitting costs an invisible precedent that no `git rm` undoes |
| DEC-082 | **The architect's** — may a browser-driving QA harness live in this repository? [`ADR-0088`](ADR-0088-the-two-browser-proof-is-a-written-hand-check.md) §1 refuses one by name: *"No browser drives this client, here or in CI"*, no browser runner, and no Playwright, Puppeteer, Selenium, WebDriver or Cypress dependency in `web-client/package.json`. [`EPIC-12`](../../tasks/epics/EPIC-12-quality-and-defect-repair.md) proposes something narrower than what §1 refused — **no CI job, no `package.json` dependency, no pull-request gate**, an agent-run harness driving Chrome over the DevTools protocol that a human invokes by command — and the question is whether that is inside §1's refusal or outside it. Raised 2026-08-29 by the human's instruction to build a QA cycle. **Blocked all of `EPIC-12`** | [`ADR-0089`](ADR-0089-a-browser-drives-this-client-for-a-qa-round-never-for-a-gate.md) — **a browser drives this client for a QA round, never for a gate.** Answered by the architect on 2026-08-29. **It was inside §1's words, and the answer says so rather than reading them narrowly**: *"here or in CI"* says *here*, so the only legal route was to amend the clause in the open. **§1's heading is amended and nothing else is** — it becomes *no browser drives this client in CI, and no browser stands between a pull request and `develop`* — while §1's body, §§2–5 and `DEC-024`'s answer stand byte-unchanged: `EPIC-03` still ships no fifteenth story, §2's eleven-step hand-check is still the proof of record, `build.yml` keeps its two jobs, no browser dependency enters `web-client/package.json`, and `ADR-0032` §4's *"still jsdom, still no network"* still holds of every test suite, because the harness is in none of them. **The permission is the conjunction of three conditions, not a general licence**: no browser dependency in any module; **no gate** — no pull request, `verify:` block or ticket waits on a case, and a human's command starts a cycle, not a merge or a cron; and **no coverage claim** — a QA round is a dated record and may not be cited in an epic's `Metrics`, a Definition of done or a `verify:`. Any one of the three failing returns the question as a new `DEC`. **§1's reasoning was a ratio and none of its cost terms are incurred here**, while §2 of that same ADR already has a person driving two browsers through this client: the difference between them is the **position** — a hand-check that leaves a receipt, not a gate between a diff and `develop` — and not the browser. **The driver reads anything and writes nothing** but `pd.roomCode`, because a case that seeds state to reach a screen is a client asserting a game fact (`ADR-0002`). **And it adds the clause `EPIC-12` §Termination lacked**: a failure that does not reproduce by hand is a **harness** defect — filed against `EPIC-12`, excluded from `B(N)`, and never repaired in production code — which is what stops a stale catalogue from reading as a product getting worse, or from being satisfied by a merged diff |
| DEC-024 | **The architect's** — does `EPIC-03` ship an automated browser end-to-end test: two real browser contexts against a running server and database, playing a duel to a winner? If so, what runs it and in which CI job, given `./gradlew check -PrequireDocker=true` already carries the socket-level end-to-end suite. If not, the two-browser proof is a manual check in v0.1 and `STORY-0312`'s frame-driven test is the automated ceiling. Open since `EPIC-03` was written; the last thing standing between the epic and its close, and the reason its two registers contradicted each other. | [`ADR-0088`](ADR-0088-the-two-browser-proof-is-a-written-hand-check.md) — **the two-browser proof is a written hand-check, not a CI job.** Answered by the architect on 2026-08-28. **No browser runner, no fifteenth story, no third CI job**: no Playwright, Puppeteer, Selenium, WebDriver or Cypress dependency enters `web-client/package.json`, `build.yml` keeps its two jobs, and the automated ceiling stays where `STORY-0312` and `poker-server/.../e2e/` put it — `ADR-0032` §4's *"still jsdom, still no network"* stands. **`EPIC-03` closes on its stories, fourteen of fourteen**, and its table's *"decides whether a fourteenth story exists"* is answered **no**. **The gap is named rather than waved past, and it is four things measured on `develop`**: `main.tsx`'s root render — seven nested providers into a `#root` no test document has — is executed by no test and `index.html`'s `<div id="root">` is asserted nowhere (delete either and the suite stays green while the page renders blank); `new WebSocket(socketUrl(window.location))` is never executed — `index.test.ts` stubs the constructor and asserts the *string*, `dev-proxy.test.ts` asserts `vite.config.ts`'s *values*, and nothing forces the two to agree — so **no test in this repository has ever opened a TCP connection to the duel server**, the JVM suite included, which runs on `testApplication` in process; `npm run build`'s exit code is the whole claim made about `dist/`; and two storage partitions are something no jsdom or JVM test has any notion of. **The manual proof is eleven numbered steps with a named failure condition each** (§2), covering `docs/vision.md`'s success condition in order, and it is in the ADR rather than a new `docs/*.md` because a document nothing points at is a document nothing runs — twenty ADRs already name `EPIC-07`, which is not written. **The receipt** — date, commit SHA, both browsers, room code, winner, both balances — is one line appended to `EPIC-03`'s Definition of done, written by the human who runs it alongside the `Metrics` table; it gates no merge and no ticket, and no agent can write it. **Costs accepted and stated**: the regression window is a release rather than a pull request; a failure has no bisect; the built bundle stays unproven by anything including the hand-check, because §2 runs `npm run dev` and nothing here serves `dist/`; the check decays with repetition; and this is now the merged precedent for `EPIC-04` and `EPIC-05`, which are in the same position. **Chosen because it is the cheapest to reverse** on thin evidence, with no deadline in either direction — reversal is a superseding ADR plus one story, and nothing here makes it harder |
| DEC-081 | **The product owner's** — what does the product call the flow a player uses when they have **forgotten their password**, is it a screen with its own address or a form on `#/sign-in`, and where is its door? Raised on 2026-08-28 when `STORY-0417` was split. This is `DEC-077`'s question one screen later: [`ADR-0076`](ADR-0076-a-screen-the-player-chose-has-an-address.md) §1 permits only *"a word the product already says to a player"* and coins none itself, and **this product says nothing to a player about forgetting a password** — the phrase exists only in the endpoint `POST /api/auth/forgot-password` and in ADR prose, so a ticket that picked a heading would be coining player-facing vocabulary a ticket may not coin. [`ADR-0083`](ADR-0083-the-second-account-screen-is-sign-in-and-its-address-is-never-refused.md) §6 deferred *"a **forgot password** door"* to `STORY-0417` by name; [`ADR-0081`](ADR-0081-a-mailed-link-is-a-fragment-route-and-the-token-is-the-segment-behind-the-slug.md) §3 granted the story *"the ***forgot password*** screen's"* address without saying what it is | [`ADR-0087`](ADR-0087-forgot-your-password-is-a-door-on-the-sign-in-screen.md) on 2026-08-28 — ***Forgot your password?* is a door on the sign-in screen, not a screen of its own.** Answered by the product owner, licensed by the vision's *Positioning* sentence — *"The reference points are **Lichess** and **Chess.com**, not PokerStars. Dark, quiet, fast, minimal"* — the same sentence that licensed `ADR-0083`. **Unlike `ADR-0083`, it coins**, and says so: *sign in* was merged player-facing text before that ADR named a screen with it, while *forgot your password* is player-facing text nowhere here, so the search that answered `DEC-077` returns nothing and `ADR-0076` §1's *"only a word the product already says"* stops describing every string in `recovery-text.ts`. **The word is `FORGOT_PASSWORD_LABEL = "Forgot your password?"`**, with `FORGOT_PASSWORD_SUBMIT = "Send a link"`, `FORGOT_PASSWORD_ACKNOWLEDGED` (*"If that address is verified on an account here, a link is on its way. Follow it to set a new password."*) and `FORGOT_PASSWORD_FAILED` (*"That did not go through. Try again."*, a third literal holding `SIGN_UP_FAILED`'s and `ATTACH_FAILED`'s words) — four constants in `recovery-text.ts`, with `ADDRESS_LABEL` and `CANCEL` **imported, not re-authored**. **It is not a screen**: `Screen` gains no member, `hashForScreen` no case, `ADR-0076` §1's table no row, and **no slug is minted** — the count `ADR-0076` §1 left to the story is settled at zero for this flow, and `ADR-0081` §3's grant is left unspent while `reset`, `verify` and every other sentence of §3 stand byte-unchanged. **The door is on the sign-in screen, below the sign-in form**, offered to everyone who can see that screen and conditional on nothing — not on the first screen (`ADR-0083` §3), not on the account screen — and it opens a one-field form **in place of** the sign-in form, so there are never two forms in view and no password field anywhere in the flow. **The door's words are the form's heading, from one literal**, which departs from `ADR-0083` §3's two-constant shape *with the reason stated*: there is no screen and therefore no name, both utterances are the same utterance seen one at a time, and a divergence would be a defect rather than a freedom. **An address the product does not hold gets exactly what everybody else gets** — same sentence, same controls, same layout, no second state — and the acknowledgement **states a rule rather than reporting an outcome**, which is what makes it honest across all five situations `ADR-0031` §5 refuses to distinguish and discharges `ADR-0078` §Consequences' *"honest about a pending state rather than congratulatory about a `202`"*. **The form survives its own success**, with what was typed still in it, because a player who mistyped has no other route back and a second request inside fifteen minutes sends nothing (`ADR-0031` §5, `ADR-0079`). **Costs named rather than discovered**: the first coined player-facing phrase in this product and the first question it asks a player, loosening the discipline the next ticket will cite; **the browser's *Back* cannot close the form** — it leaves the sign-in screen for `#/account` — which is `ADR-0076`'s own harm in miniature and the strongest argument for the screen that was refused, paid rather than answered; recovery still two navigations deep and advertised nowhere; **a mistyped address is told the same thing as a correct one and nothing detects it**, with the form staying visible as the whole mitigation; a mode added to the one screen a player types a password into; and a two-sentence string fixed before `EPIC-06` letter-fits anything. **Forecloses** any feedback about the address by rule rather than omission, and a *forgot your handle* flow for as long as `ADR-0031` §6.2 keeps the handle in the reset mail; **`#/forgot-password` is left open, not shut** — one table row and one branch, additive — and the phrase is fixed so no second `DEC` is needed for the word. Chosen as the direction undone by adding rather than removing. **Unblocks `STORY-0417`'s three held tickets**, changes no server, wire, schema, `PROTOCOL_VERSION` or written ticket, and **nothing in it was the human's** |
| DEC-080 | **The architect's** — which storage key holds `ADR-0085`'s browser-local answer, which module owns it, and therefore the third entry `web-client/src/protocol/one-module-owns-each-storage-key.test.ts` gains. Raised on 2026-08-27 when `STORY-0415` was split, as the technical half of `DEC-079`, and **narrowed on 2026-08-27** by [`ADR-0085`](ADR-0085-not-again-is-this-browser-and-an-answer-spends-the-offer.md) §1 and §5, which closed its wire, column, endpoint, server-half and *first-win fact* branches and left only the key and the module | [`ADR-0086`](ADR-0086-the-offers-answer-is-one-key-owned-beside-the-predicate-it-feeds.md) on 2026-08-28 — **the offer's answer is `pd.accountOfferSettled`, owned beside the predicate it feeds.** The key is `pd.accountOfferSettled`; `web-client/src/result/account-offer-settled.ts` is the only production file naming that literal, exporting `ACCOUNT_OFFER_SETTLED_STORAGE_KEY`, `readOfferSettled(storage)` and `markOfferSettled(storage)` over the injected `Storage`; the stored value is the sentinel `"1"` and **anything unrecognised reads as *not settled***, which inherits `ADR-0085` §Consequences' own tie-break — *"the side that risks asking twice over the side that risks never telling them"*. **The module exports no way to clear the bit**, which makes `ADR-0085` §2's *"nothing ever clears it"* mechanical, and `signOut` is unchanged, so the signed-out-account-holder case stays where `ADR-0085` left it. **Three measurements decided it, run in a worktree with throwaway files and reverted.** The gate scans with `String.includes`, so a file holding `"pd.accountOfferSettled"` and a file holding `"pd.accountOffer"` are **both** returned by a scan for the shorter string — which is why the obvious short name is refused and why the `pd.` namespace now carries an unenforced shape rule: no key may contain another. A second writer in `src/result/` reddens a row whose owner sits in `src/protocol/`, so the scan is whole-`src` and the owner's directory is free — placing it in `result/` beside `account-offer.ts`, `account-offer-text.ts` and `AccountOffer.tsx` rather than in `protocol/`, whose three keys are each a wire fact this one is not. And a literal only the test file holds returns `[]`, so the `.test.ts` exclusion does the self-exclusion work and the row may carry the literal verbatim. **The third row is written out verbatim**, with the three properties that stop it being green and vacuous: presence, self-exclusion, and three keys resolving to three different modules. **One clause is fixed for the wiring ticket**: `markOfferSettled` runs from the accept anchor's click handler before the page load `ADR-0076` §3 forces, on `DuelResult`'s merged `onLeave` precedent — *"Storage operations are synchronous, so a handler that forgets has finished before the page leaves"* — and **not** on the account screen's load, which the lobby's own control also reaches. **Costs named**: a row that outlives its meaning with nothing able to remove it; a fourth key in a `pd.` namespace that is a convention, not a mechanism, now carrying a shape rule nothing enforces; a storage clear that re-offers, made mechanical by there being exactly one copy; a magic sentinel no other module here uses; one more literal that must stay a single line forever; an unguarded `setItem`; and a modified click that settles the offer without leaving the page. **Forecloses** any local record of *which* control was pressed, any count or timestamp, and `protocol/` as the one place to ask what this browser stores. **Reversible**: one file and one test row, no wire, no schema, no migration. **Records that `pd.roomCode` has no row in the gate** — three production keys, two rows — which is the evidence for its deadline: the row ships in the same ticket as the module. **Unblocks `STORY-0415`'s persistence, wiring and arc tickets.** Nothing in it was the product owner's |
| DEC-079 | **The product owner's** — is `ADR-0036`'s *"not again"* a fact about the **player** or about **this browser**, and what **spends** the offer? Raised on 2026-08-27 when `STORY-0415` was split, because the story and the ADR it applies say opposite things and only one can be built. [`ADR-0036`](ADR-0036-an-account-is-offered-never-required.md) §Consequences: *"Dismissal is state that must survive. 'Permanently dismissed' cannot live in `localStorage` alone… **It belongs on the profile**"* — so it follows the player to a second browser and survives a storage clear. [`STORY-0415`](../../tasks/stories/STORY-0415-the-offer-after-a-first-win.md) §Design notes: *"it is stored under a key this module owns"*, asserted *"through the injected storage"* — so it does neither. Second half, deciding the same tickets: `DEC-049`/[`ADR-0056`](ADR-0056-a-throttled-sign-up-says-so-and-keeps-what-was-typed.md) §5 records that a `429` spends nothing, *"including `ADR-0036`'s offer, **which only 'Not now' dismisses**"*, which reads as *accepting does not spend it*; `STORY-0415`'s criterion *"It does not appear a second time after a second win"* reads as *being shown spends it* | [`ADR-0085`](ADR-0085-not-again-is-this-browser-and-an-answer-spends-the-offer.md) on 2026-08-27 — ***"not again"* is this browser, and an answer is what spends the offer.** **Derived, not chosen**: the roadmap's `v0.1` row — *"Two browsers, one room link, one complete duel, rematch. **No accounts.**"* — is the vision counting a player as a browser at exactly the stage the offer addresses, since it is shown only to a player holding no credential; and *Positioning*'s *"Dark, quiet, fast, minimal"* is what refuses a prompt that returns after the player answered. **Three measurements decided the first half.** `ADR-0036`'s stated reason does not hold on the shipped client: `device-id.ts` owns `pd.deviceId` in the same bucket `main.tsx` injects, so clearing storage takes the device id too, `ADR-0049` §4's `resolve` finds no live binding and **mints a fresh, empty profile** — the prompt is not resurrected, the profile is gone, and the returning browser sees no offer until *it* wins. The two answers are **indistinguishable for every player who can see the offer**, because it requires *no credential* and `ADR-0049` §1's `device_binding_live_player` gives such a player exactly one live browser; the only route to a second browser is signing in, which switches the offer off. And **two of `offerAccount`'s three terms are already browser-local** (`signedIn` is *"whether this browser holds a session token"*), so a travelling third term would give one predicate two ideas of who the player is. **The bit is written and read through the injected `Storage` and never sent**: no column, no `GET /api/me` field, no endpoint, no wire change, no migration. **Second half: an answer spends it, and nothing else does.** Both controls are answers — taking the offer and *"Not now"* — and each is permanent; a `429`, an abandoned sign-up, a rematch, a reload and the mere fact of having been rendered spend nothing; **an offer shown and never answered is made again after the next win**, because the prompt shares a screen with *Rematch* and `ADR-0036` §Alternatives refused silence for the reason *"a player who has never been told their coins are device-bound learns it by losing them"*. **Costs named rather than discovered**: the player who accepts and abandons is never re-asked, and it is the player most likely to convert; the offer **reappears on a second browser** and nothing can know it is the same human; a mis-tap on *Not now* is final by design; a player who tapped past it is asked twice; **one more storage key**; and `signedIn`'s browser-scoped meaning leaves a signed-out account holder offerable — **named, not solved**. **Forecloses** any server-side record that a player was ever offered an account — no funnel, no *asked-and-declined* metric — which is named because it is the first thing a growth argument asks for. **Amends** `ADR-0036` §Consequences' storage clause and `ADR-0056` §5's *"and by nothing else"*, leaving §5's holding and `ADR-0036` §Decision — the human's `DEC-025` call — byte-unchanged. **Chosen as the reversible direction**: a column is additive while `EPIC-07` hosts nothing, a shipped field is not. **Unblocks `STORY-0415`'s fifth, sixth and seventh tickets**; leaves `TASK-041501`–`TASK-041504` unchanged; hands the planner one clause for the story's third criterion; and **narrows `DEC-080`** to the key and the module. **Nothing in it was the human's** |
| DEC-078 | **The architect's** — what mechanically counts as a **gated** acceptance criterion, and what does `lint_tickets.py` do about the ones already written? Raised on 2026-08-26 after a run produced two families of defect a linter was expected to catch, with **both** candidate checks measured against the live backlog before being proposed and **both failing as written**: (a) *a criterion naming a file the `## Files` table excludes* — `TASK-041628`'s shape and six others — flags 194 tickets raw and 29 once narrowed to demand verbs, of which 28 are correct; (b) *a shell command named in a criterion must appear in `verify:`* — `TASK-041210`'s criterion 5, `TASK-041215`'s `grep -cE`, `TASK-041634`'s Scope — flags 25 unsettled tickets across 12 files and cannot land green without a twelve-file repair `TASK-000106` refuses | [`ADR-0084`](ADR-0084-a-criterion-that-speaks-in-shell-belongs-in-verify.md) on 2026-08-26 — **a criterion is gated when a `verify:` command exits non-zero if it is false, and nothing else gates anything.** Face (b) becomes a `lint_tickets.py` check scoped to `ready`/`in-progress`/`in-review`, matching by **word-subset of one `verify:` line** rather than substring — which is what dissolved the twelve-file blocker, since re-measuring showed the five spelling false positives were an artefact of the relation and the true count is **18 criteria across 11 files**, `backlog` × 16 and `ready` × 2. The sixteen retire at each ticket's own `backlog` → `ready` flip; `TASK-041219`'s two are repaired in the check's PR. Face (a) is **not** mechanised — re-measured at 206 raw, and every narrowing leaves the legitimate refusals dominant, because *"`CardSecrecyTest` passes with no change to the file"* is a refusal in a demand's grammar — and becomes a written rule in `tasks/README.md`: a criterion may demand new content only in a file the *Files* table edits, and one that cannot be met without a forbidden edit **is the next ticket**. The named cost is that `TASK-041628` would happen again today |
| DEC-077 | **The product owner's** — what does the product call the screen a player opens to reach an account from a browser that does not hold it, and therefore what is that screen's permanent slug? Raised on 2026-08-26 when `STORY-0412` was split. [`ADR-0076`](ADR-0076-a-screen-the-player-chose-has-an-address.md) §1 makes a slug *"the lowercase ASCII form of a word the product already says to a player"* and states that the ADR **coins no player-facing vocabulary**, so a screen needing a word the product does not yet say is a product question. The story settled the count `ADR-0076` §1 left to it — **two** — and found the first name already merged (`ADR-0050` §3, `ADR-0036` and `ADR-0056` §2 each say *account* to a player), so `TASK-041222` ships `#/account` with nothing invented. The second screen had only a **verb** in the merged record | [`ADR-0083`](ADR-0083-the-second-account-screen-is-sign-in-and-its-address-is-never-refused.md) — **the second account screen is *Sign in*, and its address is never refused.** Answered by the product owner on 2026-08-26, and **nothing is coined**: `ADR-0050` §3's merged player-facing text already says *sign in* to a player twice, and the vision's *Positioning* sentence (*"Lichess and Chess.com… Dark, quiet, fast, minimal"*) is what licensed the plain word over a themed one. **The heading is `SIGN_IN_HEADING = "Sign in"`** in `account-text.ts`, golden like every other string there. **The slug is `sign-in`** — a literal in `screen.ts`, never derived from the heading — and **the hyphen is the product's own spelling**, since `POST /api/auth/sign-in` is already written that way, so the client's address and the server's path now read the same character for character; `signin` and `sign_in` are this product's spelling of nothing. That **widens `ADR-0076` §1's *"a word"* to a hyphenated compound for this word only**, with the rule that survives it stated: `[a-z-]` only, never leading or trailing, and still the lowercase form of something the product already says. **The word is said twice, from one constant** — the screen's heading and the single door to it on the account screen, offered only when `signedIn` is false (`ADR-0060` §2's one-spelling-per-destination rule); no fourth door on the lobby; and `SIGN_IN_LABEL` stays a separate constant, because a control's verb is not a screen's name. **The address is refused to nobody**: a browser already holding a session token that opens `#/sign-in` gets the screen, the fragment is not replaced, and **holding a token is not a fourth branch** — `ADR-0076` §3's three store-owned branches still outrank it exactly as at `#/account`. The reason is that `signedIn` means *this browser holds a string*, nothing in this client reacts to a `401` (`TASK-041209`), and `ADR-0050` §3 ends every **other** device's session while those devices keep their dead token — so the browsers most in need of the screen are the ones a bounce would turn away, which is `ADR-0076` §2's forbidden shape, an address making a second claim about entitlement. **A successful sign-in lands on `#/account`**, not back on the sign-in screen, because `account-text.ts` authors no *you are signed in* sentence and the account screen's routes statement is the only confirmation the product has. Costs recorded rather than discovered: ***Sign in* is said three times in one flow and twice on one screen**, two constants holding one string, so tests must query by role and a screen reader hears it twice; **the first slug with a character outside `[a-z]`**, which makes `TASK-041226`'s `^[a-z]+$` criterion wrong and leaves every later screen the hyphen question; **the slug and the endpoint agree by two literals and nothing mechanical**, a fragment crossing no wire; **a signed-in browser can reach a working sign-in form** and swap identity at an address rather than behind a hidden door (legal, no coin moved — `ADR-0030` §6); **every sign-in pays one extra click** before the create and join controls; and **the word is fixed before `EPIC-06` letter-fits anything**. Chosen partly as the cheapest to unwind — two literals and a constant, in no mail, on no wire, bookmarked by nobody. **Unblocks `TASK-041226` and `TASK-041227`**, which need three changes folded in: the `^[a-z]+$` criterion widened, §5's landing rule owned by `TASK-041227` (which reaches `main.tsx`, outside its three-file budget), and the *Sign in* heading queried by role. **Folded in on 2026-08-26**, with one of the three landing differently: the criterion widened to `^[a-z]+(-[a-z]+)*$`, the heading is queried by role in `TASK-041227` — `TASK-041226` renders no DOM and says so rather than carrying a vacuous grep — but §5's landing rule became **`TASK-041229`** rather than a fourth file, because `ADR-0070`'s probe found the client gate green with either half alone, so no merged gate refuses the intermediate state and `atomic:` would have been a false claim. **Raises no `DEC`, and nothing in it was the human's** |
| DEC-076 | **The architect's** — where does the login handle `RecoveryMailer.sendPasswordReset(address, token, handle)` requires come from? **Nothing in this codebase can obtain a handle from a `PlayerId`**: `PasswordResets.issue` answers a `Boolean`, `RecoveryEmails.verifiedOwnerOf` answers a `PlayerId?`, `PostgresPlayerDirectory` resolves device ids only, and `Credentials` declares exactly `verify`, `verifyCurrent`, `create` and `holdsCredential` — with `verifyCurrent`'s own KDoc recording that a reverse lookup was deliberately **not** added, *"for no other reason than this one check."* `sendPasswordReset` has **no production caller** — the only call anywhere is `NoRecoveryMailerTest`'s literal `"handle"` — and `TASK-041626` is the first real one, with no later ticket in `STORY-0416` filling the gap, since `TASK-041630` passes `handle` through its decorator without sourcing it. The product half is settled and not reopened — [`ADR-0031`](ADR-0031-an-optional-verified-recovery-email.md) §1 names the reset mail as the single exception to *the handle appears in no response body, ever*. **Registered and answered in the same PR**, because an implementation attempt raised it rather than a planner (`DEC-064`/`DEC-065`'s pattern). Raised and answered on 2026-08-26 | [`ADR-0082`](ADR-0082-a-handle-is-read-from-a-proven-address-never-from-a-player-id.md) — **a handle is read from a proven address, never from a player id.** Answered by the architect on 2026-08-26. **`RecoveryEmails` gains one member keyed by an address** — `resetRecipientOf(address: EmailAddress): ResetRecipient?`, answering `ResetRecipient(playerId, handle)` — and **there is no `PlayerId` overload and must never be one**. It is one statement joining `recovery_email` to `credential`, whose `WHERE` clause is `SELECT_VERIFIED_OWNER_SQL`'s **character for character** (the pinned `und-x-icu` fold, for the reason that constant's own comment already gives) and whose `c.kind = 'password'` matches `REWRITE_CREDENTIAL_SQL` verbatim. **The fence is the argument type, not the member name**: to get a handle you must already hold a **verified** recovery address, which is the exact secret the endpoint refuses to disclose — `ADR-0031` §5's own test for `verify-email`'s `409`, applied to a lookup. **A `JOIN`, never a `LEFT JOIN`**: unknown address, pending-only address and a verified address whose owner holds no `password` credential all answer `null`, so the third — unreachable under §3 — is answered rather than handed to the route as a null handle. **`verifyCurrent`'s refusal is upheld, not overturned**, and promoted from a KDoc sentence to a build failure: a test asserts **`Credentials` declares no member returning `String` or `String?`**, green today and reddening on exactly `handleOf(playerId): String?` in the one file anybody would write it. **`PasswordResets.issue` keeps its `Boolean`** — `ADR-0031` §5's two outcomes, seven merged assertions and four test doubles all untouched — which was the closest call: `Issued(handle)`/`Suppressed` is what `ClaimPendingResult`'s KDoc recommends, and it lost because `issue(playerId, newResetToken())` **is** a `PlayerId → handle` function with a side effect, and because the credential-less player leaves `Issued` no honest `String`. **The read is not inside `issue`'s transaction and need not be**: verified in source, the only `UPDATE credential` sets `secret_hash`, so a handle cannot go stale — the precise difference from §5's suppression window, which must share a connection. **A handle-less owner mints no token**, the route returning before `issue` so no row spends a window for a mail that cannot go. Costs recorded rather than discovered: **a read whose product is a login handle now exists and did not yesterday**, fenced by an argument type and a KDoc rather than by an impossibility proof; **`RecoveryEmails` reads a third table**, so its *"two recovery tables"* charter and its *"no member returns a `String` that could be one"* sentence are both amended; **`ResetRecipient` is a plain `data class`, so `"$recipient"` prints the handle** — deliberately not redacted, since §6.3 protects the address and inventing a rule the ADR did not make would also make every `assertEquals` failure unreadable, **with the trigger named** (the first log line anywhere on the reset path); **two reads of `recovery_email` now carry one `WHERE` clause in two constants** with nothing keeping them in step; the join adds a third caller to an unindexed `credential (player_id)` scan, with the index named as one later ticket; and **`kind = 'password'` as a literal becomes ambiguous** the day `DEC-027` admits a second kind carrying an identifier. **The gate is a tripwire, not a proof** — a handle read on another type, or one wrapped in a value class (`@JvmInline` reports the wrapper, not `String`), passes it. Chosen partly as the cheapest to unwind: one member and one `data class`, deleted in a commit. **No migration, no index, no wire change, no `recoveryRoutes` parameter, and `RecoveryMailer` byte-unchanged.** **Unblocks `TASK-041626`**, which gains the port read, a fixture whose verified owner holds a `password` credential, and a `theMailCarriesTheOwnersOwnHandle` test over **two** players with different handles; **`TASK-041630` needs one criterion widened** from two distinct argument strings to three. **Raises no `DEC`, and nothing here is the product owner's or the human's** |
| DEC-075 | **The architect's** — does the mailed recovery link survive a static host with no rewrite rule, and if it becomes a fragment route, what is the slug? [`ADR-0031`](ADR-0031-an-optional-verified-recovery-email.md) §4 mails `<baseUrl>/reset#token=…` — a **path segment** — and was right about the fragment for the token, which is never sent to any server. [`ADR-0076`](ADR-0076-a-screen-the-player-chose-has-an-address.md) §4 has since made the opposite call for **every** client address, on the express ground that *"a fragment never 404s… no host, proxy or rewrite rule has an opinion about it"*, which is the same call already merged in `room-link.ts`'s comment — and `EPIC-07` still has no file in `tasks/epics/`, so no host and no rewrite rule exists to depend on. A recovery link is the one URL in this product whose failure is unrecoverable: a `404` on `/reset` means the player never reaches the client at all, and `ADR-0031`'s Consequences make no recovery a **total, permanent** loss of the account. The verification link, `<baseUrl>/verify#token=…`, has the same shape and the same exposure. Raised by [`ADR-0077`](ADR-0077-no-sender-is-an-implementation-and-detachment-is-a-decorator.md), which transcribed §4 rather than reinterpreting it and put both links in **one function** | [`ADR-0081`](ADR-0081-a-mailed-link-is-a-fragment-route-and-the-token-is-the-segment-behind-the-slug.md) — **a mailed link is a fragment route, and the token is the segment behind the slug.** Answered by the architect on 2026-08-25. **Both links become fragment routes on the client's single address**: `RecoveryLinks` returns `"$baseUrl/#/reset/$token"` and `"$baseUrl/#/verify/$token"`, so nothing depends on a rewrite rule from a host nobody has chosen. **The token has not moved out of the fragment** — it is now the second segment of one, which is *not* a URL path segment and is still never transmitted, never logged, never in a `Referer`; `ADR-0031` §4's entropy, hash at rest, one hour, single use and refusal of a query string stand **byte-unchanged**, and `TASK-041620`'s *token in a body, never in a URL* is untouched because the server still decodes it from a body and contains no `queryParameters`. **A recovery link contains no `?` at all**, kept absolute rather than conditional, which is why a query section inside the fragment (`#/reset?token=…`) lost the close call: the rule *a `?` is fine, but only after the first `#`* is broken by deleting two characters from a string that still looks ordinary. **The slugs are `reset` and `verify`, fixed here rather than left to `STORY-0417`** — the register's premise cut the other way, because these two addresses are **minted by the server into a mail** and a slug one module writes and another parses cannot be chosen later without reaching back into `RecoveryLinks`; neither word is coined here, `ADR-0031` §4 and `ADR-0077` §6 already wrote them, and `STORY-0417` keeps every other address `ADR-0076` §1 gives it. **`verify` and `reset` are answered the same way**: the failure is deterministic, so a second attempt mails the same dead link, and a failed verification is what *creates* the total-loss state under §3. **A stale or spent link is a screen that renders and a `400` on submission, never a routing outcome** — the client never inspects the token and must not, since `ADR-0080` deliberately left no liveness oracle; a missing token is an empty input, not an unknown address; and the sentence shown is `STORY-0417`'s, already owned. `screen.ts` gains `tokenFromHash`, matches on the **first fragment segment**, and the token is read once at mount before the address is replaced with `hashForScreen(screen)`. Costs recorded rather than discovered: **the two ends agree by two literals in two modules and nothing mechanical**, since a fragment crosses no wire and `protocol.gen.ts` cannot carry it — a divergence lands every mailed link on the lobby, silently; `#/duels/anything` now renders the record, a real widening of `ADR-0076` §7; **a reset link opened in a tab already seated is destroyed** by §3's store-outranks-the-address rule, at fifteen minutes' cost; `/reset` and `/verify` become dead addresses that a host with a rewrite rule will happily serve; and two player-facing words are fixed in a URL before `STORY-0412` names the screens. **Erred toward the `404` being unacceptable**: `ADR-0076`'s six costs are legible and survivable, while a `404` here is silent, deterministic, invisible to every test in this repository and permanent. **Blocks nothing today** — no sender is configured — and it is a one-function change, exactly as `ADR-0077` §6 promised. **Constrains `TASK-041633` (two literals) and `STORY-0417`; frees `EPIC-07` of any rewrite rule** |
| DEC-074 | **The architect's** — does a good reset token survive a `422`, and if so by what mechanism? [`ADR-0031`](ADR-0031-an-optional-verified-recovery-email.md) §5 gives `POST /api/auth/reset-password` a **`422` when the token was good and the new password fails policy**, which presupposes the endpoint knows the token was good *before* the policy is judged; §4 makes consumption **one** `DELETE ... RETURNING` inside the transaction that writes the password, with *"no read-then-write window"* — under which knowing the token was good **is** spending it. A conflict between two sections of a merged ADR, **not** in its *What this does not settle* | [`ADR-0080`](ADR-0080-the-password-is-judged-before-the-token-is-touched.md) — **the password is judged before the token is touched, so a refusal costs no link.** Answered by the architect on 2026-08-25. **§5's precondition gives way and §4 stands byte-unchanged.** The handler runs three steps and no others: decode ⇒ `400`; `passwordIsLongEnough` **and** `passwordIsWithinTheWorkBound` ⇒ `422` with **no connection taken and no statement executed**; then `consume` ⇒ `204`/`400`. **A `422` never touches the row** — same `token_hash`, `issued_at` and `expires_at` — so the same link works on the next submission while it lives, and §5's fifteen-minute suppression still sees it, meaning a `forgot-password` pressed in frustration is still the complete no-op §5 designed. **The order is not new**: `ADR-0048` §2 already puts the maximum *"before Argon2 runs and before the identifier is looked up"*, and at this endpoint the token **is** the identifier; the minimum joins it because §2 makes reset one of the two endpoints it applies at and splitting the pair across the lookup would give one published rule two positions. **The register's disclosure worry was checked and runs the other way**: because the branch is chosen entirely by the caller's own password, the `422` is byte-identical for a live token, an expired one and a string the caller invented, so `400`-versus-`422` reports **nothing** about `password_reset` — every other order makes it a liveness report. `ADR-0048` §3 makes the policy *"a pure function of the presented secret"* and §7 publishes the rule, and `docs/protocol.md` already documents sign-up's identical `422`, so the two endpoints now mean one sentence — `ADR-0048` §6's *one meaning per code*, finally true. **The endpoint stays unbudgeted and this makes its cheapest request cheaper** (one `codePointCount`, no round trip); a token probe still costs one `DELETE … RETURNING` and is still spent by the finding, which is why `ADR-0031` needed no counter. **`consume(token, secret): Boolean` is unchanged** and needs no third value. Costs recorded rather than discovered: **a `422` no longer proves the link is alive and nothing else does either**, so a player can be refused twice for one attempt and `STORY-0417`'s form must move from *password refused* to *link expired* without contradicting itself; **a stranger holding no token can make this endpoint answer `422`**, which is licensed only while the policy stays a published pure function — **the day a breach corpus or any row-reading rule joins it** (`ADR-0048` leaves that open) this order becomes an unbudgeted oracle and the endpoint must be budgeted or the rule moved behind the lookup; a caller with both faults needs two round trips in a fixed order; and `ADR-0031` §5 now reads wrong on its own. **`TASK-041620` is unchanged and needs no re-cut** — the step lands in *front* of `consume` — with one fixture constraint: every request in `ResetPasswordRouteTest`, including the two expecting `400`, must carry a `newPassword` of 8–128 code points. **`TASK-041629` gains the check and loses one named test**: `aBadTokenStillAnswersFourHundredNotFourHundredAndTwentyTwo` asserts the reversed order and cannot be written, and what it defended is better asserted the other way — that the `422` for a fabricated token and for a live one are indistinguishable; `aRefusedPasswordLeavesTheTokenAsTheDecisionSays` resolves to a second request answering `204`. `TASK-041617` transcribes the corrected sentence, not §5's. **The losing branch needs no clock and cannot race**; the one hazard is a fixture pinning `java.time.Clock` (`ADR-0062` §5) far from the database's `now()`, which expires every token it mints. No migration, no config, no protocol version, no new type. **Unblocks `TASK-041629`. Raises no `DEC`, and nothing here is the product owner's or the human's** — the words a form uses are already `STORY-0412`'s under `ADR-0031` and `ADR-0048` §7 |
| DEC-073 | **The architect's** — what are the two numbers for each of the `POST /api/auth/recovery-email` and `POST /api/auth/forgot-password` attempt budgets, and does an over-budget attempt still count against its own window? Exactly `DEC-069`'s shape one endpoint pair later: [`ADR-0031`](ADR-0031-an-optional-verified-recovery-email.md) §5 fixes the mechanism, the key (remote address) and the answer (`202`, identical to success) and fixes **no numbers**, and neither shipped pair is obviously right here. `ADR-0074` chose `ADR-0022` §2's ten-a-minute for sign-in on an argument that does **not** transfer — it turned on shared-address collateral being *visible*, and here over budget is indistinguishable from success, so a throttled player is told nothing and cannot pace. `ADR-0031` §5 also already carries a **second, durable** limiter (no mail if a live token was issued in the last 15 minutes), so the answer must say what the address budget is still for once that exists | [`ADR-0079`](ADR-0079-five-to-attach-ten-to-forget-and-the-attach-budget-is-the-only-mail-cap.md) — **five to attach, ten to forget, and the attach budget is the only cap on the mail it causes.** Answered by the architect on 2026-08-25. **`forgot-password`: `10` / `60000` ms. `recovery-email`: `5` / `60000` ms**, as `auth.forgotPasswordMaxAttempts` / `AUTH_FORGOT_PASSWORD_MAX_ATTEMPTS`, `auth.forgotPasswordWindowMillis` / `AUTH_FORGOT_PASSWORD_WINDOW_MILLIS`, `auth.recoveryEmailMaxAttempts` / `AUTH_RECOVERY_EMAIL_MAX_ATTEMPTS` and `auth.recoveryEmailWindowMillis` / `AUTH_RECOVERY_EMAIL_WINDOW_MILLIS`, with `forgotPasswordLimits()` and `recoveryEmailLimits()` beside the two existing pairs and **a separate `AttemptBudget` instance each** (`ADR-0074` §1's reason, verbatim). **The register's premise was checked and then cut the other way**: invisible collateral is a reason to be *generous*, since a limiter nobody can perceive is one nobody can work around. **What decided the numbers is that §5's fifteen-minute rule covers one of the two mail paths.** `TASK-041613` puts it in `PasswordResets.issue`; `TASK-041607` fixes `RecoveryEmails.claimPending` as returning `Unit` and `TASK-041608` writes unconditionally, so **a verification mail goes out on every successful attach, for ever**. Hence: on `forgot-password` the address budget **adds nothing** against bombing one victim — the durable rule already caps that at four an hour across *every* source address — and its only unique job is an aggregate cap across distinct victims, which a second source address buys back and which is hard to aim, since causing one mail needs an address already verified here and nothing discloses which those are. It is therefore set where it cannot bite the one player in this product whose refusal is unrecoverable. On `recovery-email` the budget is **the only cap on outbound mail to a caller-chosen recipient**, and a **second door to the current-password guess** `ADR-0074` priced at ten a minute — so five, no refund, keeps the front door the cheaper one by two. **Sixty seconds on both**, because the window is the recovery time and `ADR-0074`'s burst-becomes-lockout interaction is worse where the lockout is invisible; it also holds `AttemptBudget`'s per-key list at a fifteenth of what sign-up's window accepts. **An over-budget attempt still counts** — one rule for every limiter in this system, no fork of a shared type — and here it is also the rule that *works*: `202` gives a sprayer no reason to pace, so counting caps a hammerer at one window's worth in total where not counting would hand them one window's worth every minute for ever. **Placement is per endpoint and was the half of the question that is not a number**: `recovery-email` admits **after** the `401`, the decode and `ADR-0078`'s syntax `400` and **before** the Argon2 verify (`ADR-0074` §2's reason; budgeting before identity would let unauthenticated traffic spend a signed-in player's budget); `forgot-password` admits **after the `202` is written**, the only budget in the system consulted after the response, because `admit` takes a `Mutex` and `TASK-041626` makes that ordering the timing defence. **The key is §5's and none of it is the architect's** — `origin.remoteAddress` alone, no `X-Forwarded-*` until `EPIC-07` installs the plugin. Costs recorded rather than discovered: **an attacker can switch off password recovery for everybody behind one address, silently and indefinitely, while each of them is told mail is on its way**; five is a per-person number on an aggregating key, so a classroom setting recovery up together waits a minute; `202` on the attach path now means one more thing; 300 verification mails an hour from one source is still a relay; the per-key list still grows with rate × window; and an operator now holds four limiters and eight numbers with nothing reporting which refused. **Leaves one residual, named and conditioned rather than deferred: the attach path has no per-account resend suppression**, which on the best reading of §5 is a defect against `TASK-041607`/`TASK-041608`/`TASK-041625` rather than a new question — a ticket for the planner, due before `EPIC-07` configures a sender. **Unblocks `TASK-041628`. Raises no `DEC`, and nothing here is the product owner's or the human's** |
| DEC-071 | **The product owner's** — which strings does `POST /api/auth/recovery-email` accept as an address, and what is the player told when one is refused? [`ADR-0031`](ADR-0031-an-optional-verified-recovery-email.md) §5 answers `400` for *"an address that is not syntactically an address"* and states no rule; §2 already settles storage and the fold, so only the acceptance predicate was open. That `400` is the **only** thing the endpoint ever tells a player about their address, and the cost ran one way: a rule that refuses a real mailbox denies that player recovery, which `ADR-0031`'s Consequences make a total, permanent loss of the account, its coins and its ladder place | [`ADR-0078`](ADR-0078-the-mail-is-the-only-real-check-on-an-address.md) — **the mail is the only real check, so the syntax rule refuses almost nothing.** Answered by the product owner on 2026-08-25, deriving from `docs/vision.md`'s *"One duel coin per win… a counter of duels won"* (which fixes the proportion: nothing here is worth a corpus, a blocklist, a network call or a bill) and its *Positioning* sentence (which decides what fills the gap), the same pair `ADR-0048` used one decision earlier. **The asymmetry was resolved on `ADR-0031`'s own text rather than on taste**: §3 requires verification before an address does anything and §2 notes that *"a player whose address is stored has, by construction, received mail at it"* — so the system already contains an exact deliverability check, and a syntax rule can only report earlier and can only be wrong in the expensive direction. **The rule is four clauses**: at least one `@`; the first code point is not `@`; the last is not `@`; no ASCII control character; at most **254** code points (RFC 5321's path limit, in the house unit `ADR-0029` §2 and `ADR-0048` §1 fixed). No separate minimum — `a@b` is the shortest thing that passes. **Clause four is not a syntax rule**: no `addr-spec` holds a control character in any position, so it denies no mailbox, and it is there because a line terminator inside an address is the one thing this predicate could hand `EPIC-07`'s unwritten transport that would harm somebody who is not a player. **The rule runs where an address enters and nowhere else** — `forgot-password` keeps its unconditional `202` and consults no predicate, which is `ADR-0048` §2's *never at the lookup path* one endpoint pair over, and is the single property that keeps a future tightening free. **Nothing is canonicalised**: `emailAddressOrNull` returns the input unchanged, departing from `ADR-0048` §5 and `ADR-0029` §2 because an address is a **delivery target** and §2 fixed the stored form as what the player typed. **Deliberately not checked**: deliverability, DNS/MX, whether the domain is spelled right, disposable-address lists, role addresses, plus-address stripping, unicode conversion, a dot in the domain, quoting, and any whitespace that is not a control character. **The refusal is `400` with an empty body**, indistinguishable from a failed decode, and the client says one sentence that mentions no mailbox, no domain and no other account; the one constraint on the silent path is that **a `202` may not be reported as recovery being on**, since §3 leaves `hasRecoveryEmail` false. Both fixture tables are in §6, each carrying the entry that distinguishes this answer from `.+@.+\..+`. Costs recorded rather than discovered: **the endpoint's only feedback now fires almost never**, so nearly every mistake is answered `202` and silence; **`Bob Smith <bob@example.com>` and a trailing space are accepted** and are undeliverable; a player can believe recovery is on when it is not; the predicate is **never an invariant over `recovery_email`**, which is the price of the reversibility; plus-addressing and normalisation variants let **two rows be one mailbox**; and a permissive rule hands `EPIC-07` a higher bounce rate. **`TASK-041601`'s parked catalog assertion now has its condition met** — the answer admits non-ASCII — and becomes a ticket for the planner. **Unblocks `TASK-041624` and `TASK-041625`** (the latter still needed `DEC-072`, since answered). **Nothing was the human's**: no clause needs a paid service, the one alternative that would is refused on that ground, and nothing is sent under any answer. Raises no `DEC` |
| DEC-072 | **The architect's** — what is the shape of the mail seam, and what are its failure, lifetime and observability semantics? `ADR-0031` §7 declared *"a build with no sender configured is a valid state"* without saying what the wiring holds in it; §5 required the `202` before any mail work with delivery *"on a detached coroutine"* and settled nothing about that coroutine's parent, a failed send after the token row is committed, retries, what is logged, or **what a test can await** — which decides whether *a mail was sent* is assertable at all. Also where `baseUrl` lives and what an unconfigured one does | [`ADR-0077`](ADR-0077-no-sender-is-an-implementation-and-detachment-is-a-decorator.md) — **no sender is an implementation, detachment is a decorator, and a test binds neither.** Answered by the architect on 2026-08-25. **No sender configured is `NoRecoveryMailer`, a `public object` with two empty bodies — never a null**, so no route branches on configuration and the property `TASK-041627` exists to prove belongs to the type rather than to three handlers each remembering the same `if`. **Detachment is `DetachedRecoveryMailer(delegate, scope, log)`, a decorator over the same port**, so no route file contains a `CoroutineScope`, a `launch` or a `Job`; the two compose outermost-first. **The delivery scope is a supervisor child of the application's job**, built in `duelServer` beside `scheduleSweeps` — `ADR-0025`'s argument applied unchanged, so shutdown cancels every in-flight send, one failure cancels no sibling, and it is *not* the application scope itself, which also carries a ticker that never completes. **The server may exit with mail pending**; pending mail is cancelled, not drained, because a drain timeout is a number about a transport nobody has measured. **A failed send is logged once and nothing else happens**: the member name and `failure::class.simpleName`, with **no message and no stack trace** — a transport's exception is the likeliest place a recipient address ever reaches a log, and §6.3 admits no exception — **no `player_id`**, since the port deliberately carries none and §6.4's ceiling is not a floor, and **no success line**. **Nothing above the port is retried and no row is compensated**: retry policy is transport-shaped and stays `EPIC-07`'s *inside* the port, and a compensating delete would destroy live tokens for mail that actually arrived, since a relay that times out its acknowledgement throws what a relay that never delivered throws. **`baseUrl` is a `ServerConfig` field** — `server.baseUrl`, `BASE_URL`, defaulting to the Vite dev origin — **absent is the default, present-but-malformed refuses to start** like every other field, and **`RecoveryLinks` is the only place either URL is constructed**. **What a test can await, which was the load-bearing clause: the test binds an undecorated recording double**, so the send is an ordinary suspend call in the handler and both `assertEquals(1, mailer.sent.size)` and `assertEquals(emptyList(), mailer.sent)` are decidable with no join, no channel and no timeout — **absence is what forced the shape**, since no await proves a negative and four criteria in this story assert one. The Ktor test-engine mechanism was **measured, not assumed**. **No test asserts about a mail through `duelServer`**, the decorator is unit-tested on its own, and **§5's ordering is not gated and cannot be** — it stays a review criterion, as `TASK-041626`'s Proof already predicted. Costs recorded rather than discovered: **a lost reset mail costs the player fifteen minutes of silence they cannot distinguish from anything else**, the direct price of no retry; the failure log names a class and nothing else, so a broken relay must be reproduced outside the server; nothing in `STORY-0416` exercises the real composition; and `TASK-041627` is now bigger than its Files table. **Chose no transport and named the one clause that could not be answered provider-independently** — the retry policy — leaving it to `EPIC-07` rather than guessing. **Unblocks `TASK-041625`, `TASK-041626` and `TASK-041627`. Raises `DEC-075`** |
| DEC-054 | **The architect's** — does the web client grow URL-addressable routes and a working browser *Back*, and what carries them: the History API behind the store, a router library, or neither for now? Raised by [`ADR-0060`](ADR-0060-the-record-is-its-own-screen-and-the-lobby-is-the-door.md), which makes the duel record a screen with **no address**: nothing links to it, a reload lands on the first screen, and *Back* leaves the client. The answer had to settle what owns *which screen is showing*, what a route does to `ADR-0032`'s out-of-tree boot and single socket, what the Vite dev proxy and whatever `EPIC-07` deploys must serve so a deep link is not a `404`, and whether `?room=` becomes a route | [`ADR-0076`](ADR-0076-a-screen-the-player-chose-has-an-address.md) — **a screen the player chose has an address; a screen the server gave has none.** Answered by the architect on 2026-08-25. **Yes, the client gets addresses**, and the address is a URL **fragment**: `/` for the first screen, `/#/duels`, `/#/leaderboard`, and one slug for each of `STORY-0412`'s screens — however many that story turns out to have, which is the story's call and not the ADR's. A slug is the lowercase ASCII form of a word the product **already says to a player**, written as a literal so a restyled heading cannot break a link, so the ADR coins no player-facing vocabulary. **The waiting screen, the table and the result screen get no address, ever**: all three are chosen by frames — `RoomJoined`, the first `Snapshot`, `DuelFinished` — and an address claiming a seat is a client asserting a game fact, false the moment the room is reaped; a reload while seated lands on `/` and the frames put the player back, per `ADR-0072`. **The store outranks the address**: `Lobby.tsx` keeps its branch order, a player whom a frame seats is shown the duel (`ADR-0060` §5) and the fragment is replaced with `/` so the address does not lie. **The fragment beats a path segment on a sentence already merged in `room-link.ts`** — a path segment 404s on a static host with no rewrite rule and `EPIC-07` has no file in `tasks/epics/` — and it beats a query parameter because a query reaches the server's access logs and would contaminate the invite link a host copies. `#/duels` rather than `#duels`, because a bare fragment is an element identifier the browser will scroll to. **What carries it is two owned files and no dependency**: a pure `screen.ts` and a `use-screen.ts` over `useSyncExternalStore`, the primitive `ADR-0032` §3 already chose; `pushState`/`replaceState` fire neither `popstate` nor `hashchange`, so a push is an assignment to `location.hash` and a replace notifies its own subscribers — named because that bug is silent. ***Back* is defined at every boundary**: a chosen screen pushes, so browser *Back* returns to the first screen **in the same document**; `ADR-0060` §4's in-page control **replaces**; *Back* on the first screen or on a typed-in address leaves the client, correctly; and **`DuelResult`'s `<a href="/">` and the waiting screen's *Back to the lobby* stay real page loads**, because they are store boundaries and routing them would ship `ADR-0075`'s four-field presence leak. **There is no address a player is not entitled to**: an address selects a screen, grants nothing and is not a capability — `#/duels` in another browser shows that browser's record — and an unknown fragment renders the first screen with no error. Costs recorded rather than discovered: the destination now has **two spellings**, undoing the tidiness of `ADR-0060` §2's single constant; **the screen survives a reload and its paging, filter and search do not** (`ADR-0057`), so the address promises more than it delivers; **two navigation authorities** held apart by prose and a branch order; a **second hand-rolled `useSyncExternalStore` source** to keep correct; the in-page and browser *Back* **coexist and are not the same operation**, so `ADR-0073` §5's collision is defined rather than removed; and **every screen story from here owes an address**. Chosen partly because it is the cheapest to unwind — two files and two `useState` flags, no host, no build, no dependency. No Kotlin, no frame, no protocol step, no deployment requirement. **Unblocks `STORY-0412`'s split.** Raises no `DEC` |
| DEC-070 | **The product owner's** — how long does the most recent *action the server took* stay on a player's screen, and what takes it off? [`ADR-0046`](ADR-0046-the-table-says-away-timed-out-and-back.md) §4 settles the six sentences and settles **which** mark is shown — *“showing the most recent one is enough”* — and says nothing about its lifetime. Four answers were each defensible and each a different product: it never clears; it clears on the next `Snapshot`, the way [`ADR-0043`](ADR-0043-a-rejection-closes-no-decision-point.md) clears a rejection; it clears when the rival's presence returns to `PRESENT`; it clears on the next `YourTurn` | [`ADR-0075`](ADR-0075-the-mark-lives-as-long-as-the-absence-that-produced-it.md) — **the mark lives as long as the absence that produced it.** Answered by the product owner on 2026-08-24, deriving from `docs/vision.md`'s *Positioning* sentence — the same one `ADR-0046` derived its string set from — and from `ADR-0046` §2's already-shipped rule that a presence line clears on a frame and *never on a timer, never on a fade*. **Two of the four candidates were eliminated on the server's own code, not on taste**: `AbsentSeats.kt` returns `listOf(Addressed(0, mark), Addressed(1, mark)) + next.outbound`, and `act` composes that outbound through `framesFor` = `broadcast + turnFor`, where `broadcast` emits a `Snapshot` to **both** seats for every applied action — so the mark and the `Snapshot` describing the mark's own action are consecutive frames in one delivery, and *clears on the next `Snapshot`* (or on the next `YourTurn`, which rides the same delivery) would clear the mark microseconds after it is set and `ADR-0046` §4 would never be read by a human being. `ADR-0043` §3's precedent does not transfer because neither `rejection` nor `Your rival is back.` arrives welded to a `Snapshot`. **The mark is a status, not a notice**: while a seat is absent `foldAbsent` replaces it at nearly every decision point, so it can only go stale when the server stops acting. **Exactly two frames take it off** — an `OpponentPresence` carrying `PRESENT`, and `DuelFinished` — as two keys in two existing reducer cases; every other frame leaves it untouched, and there is **no timer, no fade and no dismiss control**. It clears on the **frame**, not on a transition, so unlike `rivalReturned` it needs no bookkeeping: a `PRESENT` at a resuming client whose rival never left still clears it, because the mark is about whether the server is acting and not about whether a return happened. **The words do not change** — `ADR-0046` §4's six sentences stand, no seventh string is added. Costs recorded rather than discovered: **a mark can be older than the hand on screen** (the present player folds the button pre-flop and the turn never reaches the absent seat), which is the price of §4's *no action log* and is the cost being chosen; **a mark naming this client's own seat has no clearing frame but `DuelFinished`**, because `OpponentPresence` is recipient-relative and the wire has no *you are present again*, and a protocol step was deliberately not requested for a race whose window is one delivery; **`serverAction` clears on `DuelFinished` as a boundary guard**, not because that frame says anything about absence, so that no mark can survive into a rematch without the correctness resting on a three-link argument; and **the store still has no room boundary** — `rivalPresence`, `graceRemainingMillis` and `rivalReturned` are cleared by nothing at either boundary, unreachable today only because the way back to the lobby is a real navigation that rebuilds `initialState()`, and a ticket rather than a decision the day `DEC-054` lands. Forecloses the *shape*: if an action log ever ships the mark attaches to its line by `(handNumber, actionSequence)` and takes that line's lifetime, superseding this rather than extending it — which is why the whole frame stays in the store. **Unblocks `TASK-031314` and `TASK-031315`**, both now `backlog` behind `TASK-031313` like the rest of the chain. No Kotlin, no frame, no protocol step, no stored data. Raises no `DEC` |
| DEC-069 | **The architect's** — what are the two numbers of the **sign-in** attempt budget, and does an over-budget attempt still count against its own window? `ADR-0027` §6 fixed the mechanism, the key and the answer and fixed no numbers; `ADR-0055` §2's pair is sign-up's alone | [`ADR-0074`](ADR-0074-sign-in-is-ten-wrong-passwords-a-minute-reserved-before-the-hash.md) — **ten failed sign-ins per remote address per rolling sixty seconds**, `AUTH_SIGN_IN_MAX_ATTEMPTS` `10` and `AUTH_SIGN_IN_WINDOW_MILLIS` `60000`, on a second `AttemptBudget` instance. The budget is **reserved before the hash and refunded when the password was right**, because metering failure and protecting the pool cannot both be met by one `admit` call and the obvious peek-then-record shape lets concurrent requests from one address burst unbounded Argon2 work. An over-budget attempt **still counts**, as `ADR-0022` §2 and `ADR-0055` §1 both have it — the sixty-second window, not a forked type, is what keeps that from compounding into a lockout. Over budget answers exactly as a wrong password does; no `429`, no `Retry-After`, no wire change |
| DEC-068 | **The product owner's** — does the *waiting for your rival* screen offer a way out, and what does it say? A host who creates a room is remembered in it (`pd.roomCode`, written on `RoomJoined`) and has no control that leaves it: `RoomRegistry.resume` declines a `WAITING` room, `Room.join` answers a player who already holds a seat with `ALREADY_SEATED`, and `DuelSocket.replyToJoinRoom` answers that *exactly as a fresh seating would* — so a reload lands back on the same screen. The routes out are a rival joining, the room being reaped as idle after ten minutes (`ADR-0022`), or clearing browser storage | [`ADR-0073`](ADR-0073-the-waiting-screen-says-back-to-the-lobby-and-the-room-stays-open.md) — **the waiting screen says *Back to the lobby*, and the room stays open.** Answered by the product owner on 2026-08-23, deriving from `docs/vision.md`'s *Positioning* sentence — the same one `ADR-0046` derived its string set from — and from `ADR-0022`, which already gives a `WAITING` room one way to end and no other. **The screen gains a way out**, calling `ADR-0072` §4's `forgetRoom()` from an event handler. **The control reads `Back to the lobby`, byte-identical to the string `DuelResult.tsx` already renders** for the same action on the same memory: one phrase for one action, no new vocabulary. **It does nothing to the room** — the room stays `WAITING`, the host keeps seat 0, the code keeps resolving, and the idle timeout is still the only thing that ends it — and **exactly one line says so**: *The room stays open. That link still works for your rival, and it brings you back.* Every clause was checked in source: `Room.join`'s `WAITING` branch seats a rival, and the host's own follow of the link is `ALREADY_SEATED` answered as `RoomJoined(code, seat)`. **The line names no duration**, because the client owns no clock against a server window (`ADR-0072` §6); when the room is reaped, the already-shipped *No duel room has that code.* is the correction. **No confirmation of any kind** — the action destroys nothing, and a dialog would assert that it does. **Two strings are the whole addition**, and a third needs a new ADR, which is what makes `STORY-0314`'s *the words are the ADR's* criterion enforceable. Refused by name, each with its reason: *Cancel* / *Close the room* / *Delete the room* (claims the room is gone; `Room.join` will still seat a rival at that code), *Leave* (claims the seat is vacated; `Room.seatOf` still names the host, and `ADR-0072` §4 refused `leaveRoom` for a function nobody sees), *Back* alone (the lobby's two panel swaps say it and change nothing), *forfeit* / *give up* / *stand up* / *sit out*, and any duration. **`design/screens/create-duel.html`'s waiting frame gains the control and the line verbatim**, as `EPIC-06`'s work; `STORY-0314` does not wait on it. Costs recorded rather than discovered: **a host pulled into a duel after pressing this has a store that never saw `RoomJoined`** — `deliver` addresses by player id through `ConnectionDirectory.writerFor`, so the lobby tab gets the opening `Snapshot` and renders the table, but `duel-state.ts` sets `mySeat` and `roomCode` only on `RoomJoined`, so the result screen loses its seat and a reload does not rejoin; **a rival opening the link after the host walked away gets a duel with an absent seat**, and the host can lose a coin without seeing a card — both predate this control, and it makes neither worse except by making the route one press; and **two components must now be reworded together**, with nothing enforcing it. Forecloses one thing on purpose: **this control never quietly becomes a room-closing control** — an invite that dies with its host is a frame, Kotlin in `EPIC-02` and a protocol step, and it must arrive with different words. **Unblocks `STORY-0314`** and nothing else. Raises no `DEC` |
| DEC-067 | **The architect's** — after a `DuelFinished`, does this tab still remember the room it was seated in, and what forgets it so the lobby stays reachable? `boot.ts` forgets the code on that frame (`TASK-031009`), because the way on from the result is a reload (`TASK-030807`) and a tab that remembered would rejoin, be handed the same `DuelFinished`, and never reach the lobby. `ADR-0044` §5 needs the opposite — a reopened socket must re-`JoinRoom` to be handed the standing offer — so as merged a rematch survives no reconnect and the player's own press answers `UNKNOWN_ROOM` from an empty `RoomMembership` | [`ADR-0072`](ADR-0072-a-tab-remembers-its-room-until-the-player-leaves-it.md) — **a tab remembers its room until the player leaves it, and the way back is what forgets.** Answered by the architect on 2026-08-23. **The client half moves, not the server's**: `boot.ts`'s `DuelFinished` branch is **deleted**, `pd.roomCode` means *the room this tab is seated in* and never *the screen this tab should show*, and exactly two things clear it — the player leaving, and `Failure(UNKNOWN_ROOM)` answering this tab's **own** rejoin, with `TASK-031010`'s `rejoining` guard kept verbatim. Not `DuelFinished`, not a timer, not the store. **The asymmetry was verified, not inferred**: `boot.test.ts`'s merged, green *sends no JoinRoom on the Welcome after a duel has finished* is a standing assertion that no socket this tab opens ever rejoins after a finish — a second `Welcome` being exactly what `reconnecting.ts` produces per attempt — so `DuelSocket.replyToJoinRoom`'s `for (player in resumed.room.rematchOffers)` loop is code that runs for nobody, and the case `ADR-0044` §5 was written for (an offer made while the opponent is inside their grace window) is the one that stays broken. **The surface is one member**: `DuelClient` gains `forgetRoom(): void` beside `store` and `send`, `DuelProvider` an **optional** third prop, `useForgetRoom()` sits beside `useSend()`, and `ADR-0032` §3's *event handlers only* extends to it verbatim — an effect would fire twice under StrictMode and would fire on the mount a rejoin just produced. Named for the mechanism on purpose: `leaveRoom` would be a lie, because nothing is sent, no seat is vacated and the open socket keeps its room — **the memory is about the next socket, never the current one**. **The way back stays an `<a href="/">`** and gains one `onClick`; `removeItem` is synchronous, so no `preventDefault` and no `window.location` are needed and `TASK-030807`'s reason for a navigation survives intact, with `DuelResult` still a function of its props (`onLeave`, handed down by `Lobby.tsx` exactly as `TASK-030910` hands down `onOffer`). **A stale code cannot strand a tab**: a boot holding a reaped code is refused, forgets under the `rejoining` guard, and lands at the lobby with *No duel room has that code.* — one round trip and one of `ADR-0022`'s ten budgeted joins, and **no client-side clock**, because a client never acts on a window the wire does not carry. Costs recorded rather than discovered: **a merged ticket is reversed and two green tests are deleted** — `TASK-031009` stays `done` and unrewritten on `ADR-0071`'s precedent, and §9 fixes the four replacement assertions so this is a reversal and not a weakening; **the way back is no longer a plain link**, so a modifier-click forgets the room in a tab that stays on the result screen; **a tab closed on the result screen comes back to it** until the player presses the way back or the room is reaped; **the two-tab adoption fight widens** from the duel to the whole finished-room lifetime; and the optional prop means a `main.tsx` that omitted it would compile and ship this exact defect, a hole bought to keep fourteen unrelated render sites out of the change and closed by review alone. Forecloses **the client deciding when a room has expired**. Rejected, each with its strongest case: shipping the rematch live-sockets-only (leaves `TASK-030913` asserting frames no client can receive); re-remembering on `RematchOffered` (makes the trap **intermittent** and misses every drop that follows `DuelFinished`); forgetting on every `UNKNOWN_ROOM` (re-opens the mistype bug, and a *live* finished room refuses nothing, so the lobby stays unreachable); a client timeout; a server auto-resume on `Welcome` (no way to say *not that room*); a `?lobby=1` marker in the invite's query string; and `sessionStorage`. **Blocks and unblocks no ticket** — `STORY-0309`'s fourteen apply frames to the store — but the transport half of its fourth acceptance criterion is now decided, and the story may not be called done without it. **Names one gap it does not close**: the *waiting for your rival* screen still has no way out, which is a screen affordance and a ticket of its own. Raises no `DEC` |
| DEC-066 | **The architect's** — `ADR-0028` §1 specifies `@SerialName("ActedForAbsentSeat")` verbatim; `ProtocolDiscriminatorTest.everyDiscriminatorIsShortAndUnique` has failed the build on any discriminator over **16** characters since `TASK-020210`, and that name is 18. Found by the `ADR-0070` §2 probe, not by reading: `Discriminator longer than 16 characters: [ActedForAbsentSeat]`. `OpponentPresence` is exactly 16 and passes. Shorten the `@SerialName`, rename the type, or raise the limit — three edits with different consequences on the wire and on a merged gate | [`ADR-0071`](ADR-0071-a-discriminator-is-its-kotlin-type-name.md) — **a discriminator is its Kotlin type name, and the name moves rather than the gate.** Answered by the architect on 2026-08-23. **The type is renamed, not the annotation and not the gate**: `ActedForAbsentSeat` becomes **`ActedForAbsent`** (14), Kotlin type and `@SerialName` together, with `ADR-0028` §1's four fields, both `require` blocks, both recipients and every §5 emission point standing exactly as written — *the word that goes is the one the payload already supplies*, since the frame carries `seat`, exactly as its sibling `OpponentPresence` carries none and names none. **The invariant is stated for the first time**: a `ClientMessage`/`ServerMessage` subtype's `@SerialName` equals its Kotlin simple name character for character, and a divergence is a **defect** — `@SerialName` is not a licence to diverge but what makes moving the wire name deliberate rather than a refactor's side effect. It is **not gated and cannot cheaply be**: a descriptor carries no Kotlin name and `TASK-020210` forbade reflection by name, so it holds by review. **Divergence was rejected on a measured fact** — `typeNameOf` reads `descriptor.serialName`, so a shortened annotation renames the *client's* generated interface, `frames.ts`'s key, `docs/protocol.md`'s row and the ledger fingerprint, leaving the long name alive only in the server's own sources; and it would be this protocol's **first** divergence in sixteen types, which is the expensive one. **The 16 stands, unedited and explicitly unratified.** Looked for rather than assumed: `TASK-020210` gives no reason, `TASK-020718` restates it as *"stay short"*, the test's KDoc is silent on length, no ADR mentions it, and `TASK-020213`'s frame limits are in kilobytes — and the same JSON already carries the engine's `BettingRoundEnded` (17) and `UncalledBetReturned` (19) nested inside `Events`, so **16 is not a property of the wire** but a house rule on the envelope's fourteen names, which is the set every client switches on. It is a **style** gate, not a correctness one: the fully-qualified-name check already catches every defaulted discriminator. It is nevertheless not moved here, because *"the name I already wrote is two over"* is the one evidence that must never move a threshold — `ADR-0070` §4 forbids a weakened assertion as propagation, and a name renames again on any version step while a raised limit cannot come back down without renaming whatever grew past it. The trigger is written down: a frame whose clearest name **cannot** fit 16 without losing meaning, alternatives written out and rejected on what each costs a reader. Costs recorded rather than discovered: **twelve settled documents now name a type that will not exist** and are deliberately left as written (`ADR-0028`, `ADR-0044`, `ADR-0045`, `ADR-0046`, two answered rows here, two `BOARD.md` paragraphs, and six `done` tickets and stories), with only `ADR-0028` amended and `ADR-0071`'s Consequences §1 acting as the index a grep ends at; **eight live documents are corrected** where the rejected alternative would have cost none; **§1's invariant is enforced by review alone**; the name is **worse than the one `ADR-0028` chose**, by one word, on purpose; **16 is now defended without ever having been justified**; and `OpponentPresence` sits exactly on it, so this corner has no headroom left. Forecloses the divergence family permanently, *including* its legitimate use — renaming a Kotlin type while holding the wire still now costs a version step. **Unblocks `TASK-021402`** (`files_touched` unchanged at 13, `ProtocolDiscriminatorTest.kt` is **not** a fourteenth *Files* row, `status: ready`, `TASK-021401` having landed) **and with it `STORY-0214`, `STORY-0313` and `STORY-0405`**. Names no ticket and raises no `DEC` |
| DEC-065 | **The architect's** — `ADR-0069` §3's probe missed two more files on the same ticket, at the same place, one day later: probe **(b)** runs `compileTestKotlin` and `tsc` while §3's own justification sentence claims *"`:poker-server:check` compiles test sources and `npm run check` runs `vitest`"*, and its edit says *"each sealed hierarchy"* so an enum entry trips nothing. Are **(a)** and **(b)** two probes at all if both run full commands; what makes an enumeration **complete** rather than merely longer; and may a coder that has *empirically demonstrated* the whole set amend the *Files* table itself, or does that hand away `ADR-0069` §2's growth protection? Registered and answered in the same PR, because an implementation attempt raised it rather than a planner | [`ADR-0070`](ADR-0070-a-blast-radius-is-complete-only-when-the-gates-are-green.md) — **a blast radius is complete only when the gate set is green, and a merged gate may add the row that makes it so.** Answered by the architect on 2026-08-23. **The obvious repair is not the fix.** Making **(b)** full would still miss both files: a red run of the gate set names a **prefix** — the probe's own `ServerMessage` variant breaks `DuelSocket.kt` in *main* sources, so `check` dies at `compileKotlin`, never reaches `test`, and never runs `ServerMessageHandshakeTest` or `TypeScriptDeclarationsTest` — which is exactly what happened in round one, where side **(a)** was already full. And **(b)**'s *edit* excludes `ProtocolError` by wording, so file 16 is invisible at any command depth. **Four procedures, one mechanism: every one was a copy** — a list of four files, of five, of twelve read out of two directories, and now of commands and of language constructs. A copy drifts from what it copies. So: **one probe**, every stub edit applied together, no hand-computed union; **its commands are the ones `.github/workflows/build.yml` runs, by reference and verbatim** — today root `./gradlew check -PrequireDocker=true`, `npm ci`, `npm run check`, `npm run build`, recorded as a dated observation and not as the rule — so a check added to CI joins every probe with no ADR edited and **no document may publish a narrower probe command**; **the probe is a loop that ends on `exit 0`**, each red run's paths becoming *Files* rows and each getting the minimal propagation before the next run, because **there is no prefix of green**; and **the stub edit is the story's declared surface** — every declaration added, removed, renamed or re-valued, an enum entry included, carrying the fields the story states. `ADR-0069` §2 stands with **one bounded exception**: a coder may add a *Files* row and continue when all four hold — a merged gate fails naming the path (quoted in the PR), the ticket's own authorised edits are what make it fail, the edit is **propagation not decision** (no behaviour, no new test, **no assertion weakened or derived away**), and the full gate set then exits 0. Anything else is still a `DEC`. Both files added here hold **golden** assertions — nine hard-coded `ProtocolError` names, and the exact string `export type ClientMessage = …` — and deriving them instead of updating them would turn each into `x == x`, so the ADR forbids it explicitly. Costs recorded rather than discovered: **the probe becomes the most expensive step in planning** (a root `check` with Docker plus `npm ci`/`check`/`build`, iterated, before any design exists) and the pressure to narrow it is answered only by a rule; **the coder gains a unilateral edit of its own contract**, whose third condition is judgement read by a cheap reviewer; `files_touched` becomes as-implemented rather than as-planned; a probe run without a Docker daemon is quietly narrower than CI and the remedy is a declaration; and **four documents now answer one question in three days**. **Unblocks `TASK-021301`** (`files_touched: 17`, `status: ready`, the fifteen files already implemented unchanged) **and with it `STORY-0213`, `STORY-0214` and `STORY-0405`**. Names no new ticket and raises no `DEC`. `lint_tickets.py` is deliberately untouched — 17 rows still equal 17 |
| DEC-064 | **The architect's** — `ADR-0068` §5's enumeration missed three files and the first ticket it governed stalled at thirteen against a ceiling of twelve. What is the ceiling, must the enumeration widen to test sources and how is a planner to run it *before* sizing, and does the version-literal-in-fixtures category deserve a named mechanism? Registered and answered in the same PR, because an implementation attempt raised it rather than a planner | [`ADR-0069`](ADR-0069-the-blast-radius-is-probed-not-remembered.md) — **the blast radius is probed, not remembered, and a ticket's size is its own *Files* table.** Answered by the architect on 2026-08-23. **The ceiling is deleted, not raised**: `MAX_FILES_TOUCHED_ATOMIC` is gone and there is no maximum on an `atomic:` ticket, because the set is monotone in a gate count nobody controls — it has been **four** (`ADR-0045` §4), **five** (`ADR-0047` §6), **twelve** (`ADR-0068` §5) and **fifteen** today, each step taken by an ordinary ticket that added a gate without knowing it was widening a bump. `ADR-0068` §5 said *"whatever replaces the five must not be another number"* while its own §3 wrote twelve into the linter, and **that contradiction is what stalled**. In its place the linter checks the one thing knowable at planning time: on a ticket declaring `atomic:`, **`files_touched` must equal the count of `create`/`modify`/`regenerate`/`delete`/`rename` rows in that ticket's own *Files* table** (≥ 4, unknown action = failure) — the residual `ADR-0068` §7 left unbuilt, built now and scoped to `atomic:` tickets only so the nine merged under-declaring tickets stay green. **What stops an atomic ticket is a file its *Files* table does not name, at any count** — the count-independent rule that actually fired here, promoted from ticket prose. **The enumeration becomes a probe**: `ADR-0068` §5's *"run the checks on the tree being changed"* is a **verification** procedure, unrunnable by a planner who has no changed tree, so its twelve-file list came from reading and reading stopped at the two `protocol/` directories. A probe is a throwaway one-line change made only to be read and reverted — `PROTOCOL_VERSION + 1` then `:poker-server:check` and `npm run check`; one throwaway sealed variant then `compileTestKotlin` and `tsc` — and it finds test sources because the compiler and `vitest` do not know which directory a file is in. **No count is written down anywhere again**, including here. **A version literal in a fixture is one of three things and only one of them is a literal**: the test whose subject *is* the number keeps its literal and there is exactly one per side (`ProtocolJsonTest`, and the client's derived `version.test.ts`); a test needing a *different* version writes `PROTOCOL_VERSION ± 1`, never an absolute number; every other fixture references the constant. Found while checking: **five client fixtures carry `protocolVersion: 2` and survive a bump silently** — `Welcome.protocolVersion` is generated as `number` so `tsc` sees nothing, and none of them reaches `connection.ts`'s one comparison — and `connection.test.ts`'s *"another version"* case writes the absolute `3`, which this bump turns into the current version. Costs recorded rather than discovered: **the guard surface narrows from ~fifteen incidental literals to two deliberate ones**, so if `ProtocolJsonTest` or `version.test.ts` were ever weakened nothing else would notice; **the only mechanical brake on an atomic ticket's size is gone** — a forty-row table with `files_touched: 40` lints clean; the probe costs two full `check` runs and must be reverted; the probe is complete only for gates that exist when it runs; **five fixtures stay stale until a separate ticket lands**; and the rules move twice in two days, so the trail holds *twelve* and *no number* one day apart. **Unblocks `TASK-021301`** (`files_touched: 15`, `atomic:` at seven gates, `status: ready`, the twelve files already implemented at `c904503` unchanged) **and with it `STORY-0213`, `STORY-0214` and `STORY-0405`**. **Names one ticket** — the five silently-stale client fixtures — and raises no `DEC`. Declined with a trigger: generating `Welcome.protocolVersion` as the `ProtocolVersion` literal alias |
| DEC-063 | **The architect's** — a `PROTOCOL_VERSION` bump is irreducibly **twelve** files and a schema-2 ticket is capped at **three**; which gives, and how is a bump ticket written? | [`ADR-0068`](ADR-0068-an-atomic-ticket-names-the-gate-that-forbids-splitting-it.md) — **the cap gives, narrowly: an atomic ticket declares up to twelve files and names the merged gate that forbids splitting it.** Answered by the architect on 2026-08-23. **The gate outranks the budget, always** — `ProtocolVersionLedgerTest`, `ProtocolDocumentationTest`, `verifyProtocolTypes`, `verifyDuelScript`, Kotlin exhaustiveness and TS1360 do not move, are not weakened and gain no escape hatch, which forecloses the *split the gates* family permanently: a lint rule about markdown frontmatter is not a reason to let a wire shape and the number naming it be separated by a commit boundary. **`files_touched` becomes a fact** — the count of `create`/`modify`/`regenerate` rows in the ticket's *Files* table, never smaller than that table — and schema 2 gains **one optional key, `atomic:`**, naming the gate in one line; without it the range stays `1..3` unchanged, with it `4..12`, and `atomic:` below four is rejected so the key means exactly one thing. **Twelve is a tripwire, not a budget**: it is the largest atomic unit this repository has demonstrated, so a thirteenth file becomes a decision rather than an escalation. A gate is something that fails an exit code — a compiler rule, a merged test, a verify task, a database constraint; *"they belong together"* and **a scope that grew after the ticket was written** earn nothing. **`ADR-0047` §6's "five artifacts" is wrong by seven and is replaced by a procedure, not another number** — a bump carries `PROTOCOL_VERSION` plus every file a merged gate derives from it, enumerated by running `:poker-server:check` and `npm run check`, which is twelve as of 2026-08-23 and dated so a stale list is recognisable; `ADR-0047` is not rewritten and `docs/protocol.md` is corrected in place. Measured rather than asserted: **683 schema-2 tickets, ten under-declare, nine of them merged that way**, every one naming its extra files in the open — the defect is unrepresentability, not dishonesty — and nine share one mechanism (a gate propagating a change mechanically) while `TASK-020717` simply **grew**, which is why a blanket cap raise was rejected: it would license the tenth as readily as the nine. Costs recorded rather than discovered: **the linter checks presence and a range, never truth**, so `atomic: <anything>` unlocks twelve for anyone willing to type a sentence; **twelve has zero headroom for the ticket that motivated it**, so a thirteenth file stalls the same critical path again; **the nine merged tickets are not corrected**, leaving nine permanently false fields in the trail; five places must learn the key; `files_touched` now means two things at once; and a bump ticket is a legitimately large unit for a cheap model — previously impossible, now merely expensive. **Unblocks `TASK-021301`** (`files_touched: 12`, `atomic:` set, `status: ready`) **and with it `STORY-0213`, `STORY-0214` and `STORY-0405`**. Names no ticket and raises no `DEC` |
| DEC-003 | When the big blind cannot cover its own blind, does the bar for the round stand at the nominal big blind or at the amount actually posted? | [`../duel-rules.md`](../duel-rules.md) — the bar is what was actually posted, so a big blind all-in for 60 at 50/100 leaves 60 to match and the small blind owes 10 more rather than 50. Most cardrooms hold the bar at the full blind; we do not, because the number a player faces should be genuinely at stake. A rules call, not an architecture one, so it is written into the rules rather than an ADR |
| DEC-004 | Whose cards are shown at showdown, and does the loser's hand ever become public? | [ADR-0008](ADR-0008-loser-mucks-at-showdown.md) — the loser mucks: only the winner's hand is revealed, and a mucked hand appears in no event, for anyone |
| DEC-005 | Where does a match-level event live? | [ADR-0009](ADR-0009-match-events-are-their-own-hierarchy.md) — its own `MatchEvent` hierarchy |
| DEC-006 | Where does event-log serialisation live, and in what format? | [ADR-0010](ADR-0010-engine-takes-a-serialization-dependency.md) — kotlinx.serialization, inside the engine, behind a narrowed guard |
| DEC-007 | How are the TypeScript protocol types generated, and what stops the checked-in output drifting? | [ADR-0020](ADR-0020-typescript-protocol-from-serial-descriptors.md) — an owned emitter over the `SerialDescriptor`s; a byte-comparing verify task on `check` fails CI on drift |
| DEC-010 | Do room and lobby messages belong to STORY-0202's protocol, or extend the sealed hierarchies? | [ADR-0017](ADR-0017-the-server-says-when-a-duel-ends.md) — later stories extend the existing hierarchies |
| DEC-011 | A device opens a second socket while one is live — refuse, adopt, or allow both? | [ADR-0018](ADR-0018-a-second-socket-adopts-the-seat.md) — the new socket adopts the seat, the old is closed |
| DEC-013 | Is a per-room `Mutex` enough once a duel runs inside the room? | [ADR-0016](ADR-0016-a-room-is-serialised-by-its-own-mutex.md) — the mutex stays; no actor |
| DEC-014 | Does the `duel` table gain a `hands_played` column? | [ADR-0019](ADR-0019-the-duel-table-records-hands-played.md) — yes, now, via V2 while the table is empty |
| DEC-015 | How does the end of a duel reach a client? | [ADR-0017](ADR-0017-the-server-says-when-a-duel-ends.md) — a new `ServerMessage.DuelFinished` |
| DEC-012 | Is holding a room code sufficient authorisation to take the second seat, or does joining need rate limiting or host confirmation? | [ADR-0022](ADR-0022-the-room-code-is-the-invite.md) — the code is the invite (the human's call); `RoomRegistry.join` budgets failed attempts at 10 per player per minute |
| DEC-016 | What names the opponent in a result line? | [ADR-0021](ADR-0021-a-profile-gains-a-display-name.md) — a profile gains a player-chosen display name (the human's call); nullable `player.display_name`, joined into the read path; product rules split to `DEC-017` |
| DEC-020 | What does an absent seat do at a decision point where `Fold` is illegal? | [ADR-0023](ADR-0023-an-absent-seat-checks-when-nothing-is-owed.md) — it checks; fold only when facing a bet, the action read from the engine's `legalActions`; `poker-engine` unchanged; amends ADR-0013 |
| DEC-021 | How is design work run — conversationally, or through the ticket lifecycle? | [ADR-0024](ADR-0024-design-follows-the-code-workflow.md) — ordinary ticketed work; `design/` is canonical and claude.ai/design is a render surface |
| DEC-019 | What drives `RoomRegistry.reap()` and `expireGracePeriods()` in production, with what period, scope and failure behaviour? | [ADR-0025](ADR-0025-one-ticker-coroutine-drives-both-sweeps.md) — one ticker coroutine on the application scope in `module()`; one configured period (`sweepPeriodMillis`, default 1 s), fixed delay, expiry then delivery then reap; a throwing pass is logged and retried next tick, only cancellation at shutdown ends the loop |
| DEC-022 | What is `web-client`'s toolchain, and how do its checks enter CI? | [ADR-0026](ADR-0026-vite-and-npm-drive-the-web-client.md) — Vite + npm on Node 24 (pinned once, in `web-client/.nvmrc`; CI governs); Vitest, ESLint + Prettier with `src/protocol/protocol.gen.ts` ignored by path in both; Vite's proxy carries `/api` and `/ws` to Ktor on 8080; a parallel `client` CI job runs `npm ci`, `npm run check`, `npm run build`, and Gradle stays JVM-only |
| DEC-028 | What is the credential, session and handshake model once a device id is no longer the only credential? | [ADR-0027](ADR-0027-the-session-outranks-the-device-id.md) — a session token outranks a device id and never falls back to it; the token is opaque, stored as its SHA-256 with a 30-day absolute expiry, and presented in `Hello.sessionToken` on the socket and `Authorization: Bearer` over HTTP; `PROTOCOL_VERSION` moves to 3 once, in STORY-0405. What a credential *contains* stays with DEC-027 |
| DEC-018 | Does a player see anything while the duel is paused around them? | [ADR-0028](ADR-0028-the-wire-names-an-absent-opponent.md) — yes: `OpponentPresence` carries PRESENT/AWAY/ABSENT plus a remaining-millis countdown the client renders but never acts on, and `ActedForAbsentSeat` marks **every** action the server took for an absent seat, check as well as fold. Retracts the wire half of ADR-0023's indistinguishability. |
| DEC-026 | What does a claim convert, and what happens when a device with an anonymous profile signs into a different account? | [ADR-0030](ADR-0030-a-claim-adds-a-credential-and-moves-nothing.md) — a claim attaches a credential to the `player` row already resolved (no second profile, no copied `duel_result`, no `UPDATE player`); signing into another account writes only an `auth_session`, leaving `player.device_id` pointing at the anonymous profile, so sign-out restores it by subtraction. No schema change. Amends ADR-0027 |
| DEC-027 | Does an account carry an email, what may it be used for, and can a password be recovered without one? | [ADR-0031](ADR-0031-an-optional-verified-recovery-email.md) — an optional address in its own `recovery_email` table holding **only verified** addresses, feeding a single-use one-hour reset token that revokes every session when consumed. A player signs in with a lowercase login **handle**, never the display name and never the email. Declining the email means no recovery path at all |
| DEC-033 | How does React read the duel store, and where does the single `Connection` live? | [ADR-0032](ADR-0032-react-subscribes-to-a-store-it-does-not-own.md) — one `DuelStore` and one `Connection` per tab, created by framework-free boot wiring (`bootDuelClient` in `src/store/boot.ts`) that `main.tsx` calls once, outside the tree; components read through `useDuelState()` (`useSyncExternalStore` from React core — no store library) and send through `useSend()` from event handlers only; message-triggered sends are boot reactions, so `JoinRoom` after `Welcome` is exactly-once under StrictMode by construction, and no screen ever holds the `Connection` |
| DEC-034 | Do a component's internal em ratios — the wordmark lockup's four — fall under ADR-0024 §2's "every size is born in the sheet", or may they live in the canonical card behind a drift-gate clause? (Minted as `DEC-032` on `tasks/BOARD.md`; renumbered here because this register had taken 032 first — ADR-0033's immutable header cites the original minting) | [ADR-0033](ADR-0033-component-anatomy-is-born-in-its-canonical-card.md) — the anatomy is card-born in `wordmark.html` with the gate pinning every copy's four values; colors stay unconditionally sheet-ward, and any value gaining a second independent consumer is promoted by ordinary ticket |
| DEC-035 | How does `design/check-drift.sh` read a CSS value? (Registered by #523 in **both** registers; `ADR-0034` recorded the answer here but struck neither open row, and its header's claim that the row lived only on the board is wrong — an ADR is immutable, so the correction lives here. Both open rows are struck by the change that adds this line) | [ADR-0034](ADR-0034-the-value-gate-reads-css-string-aware.md) — the awk regex `--pd-NAME:[^;]*;` is deleted and the reader becomes a third stock-perl walker, `VALUES`, scoped to `<style>`/`.css` regions, comment-stripping, string- and escape-aware, ending a declaration at a top-level `;` or its block's `}`, collapsing in-string whitespace so a wrapped string equals its one-line spelling — and refusing, by name, any shape it cannot read rather than returning a partial set. Two premises of the DEC's registration are corrected there: `ADR-0024` carries no stock-tools constraint, and the gate already runs two quote-aware perl walkers |
| DEC-001 | What exactly is one duel? | [ADR-0035](ADR-0035-a-duel-is-a-freezeout.md) — a freezeout: 100 BB, blinds rising every 10 hands, one player holds every chip. **The human's call.** The numbers stay `DuelFormat` configuration and the escalating schedule stays load-bearing, since `TASK-010715`'s termination property depends on it. Fixed-length duels remain implemented and available as configuration |
| DEC-009 | Can a duel be watched, and if so what may a spectator see and when? | [ADR-0040](ADR-0040-a-duel-may-be-watched-without-hole-cards.md) — live, with no hole card that showdown has not revealed. **The human's call.** A spectator has no seat, so the projection gains a third entry point in the engine beside `PlayerView.of` and `visibleTo` — never a sentinel seat and never a filter in transport — carrying its own leak property. Nothing is built; no epic owns spectating yet |
| DEC-017 | Is a display name refused when it is set, and can one ever be taken away? | [ADR-0038](ADR-0038-a-name-is-screened-when-set-and-can-be-taken-away.md) — a blocklist screens at set time, an operator may force-rename afterwards, and a name taken away is **retired forever** rather than released. **The human's call.** Uniqueness gains two more sources of truth; `ADR-0029`'s permanence becomes permanence *to the player*. Homoglyph impersonation is explicitly not solved. Unblocks `STORY-0410` |
| DEC-025 | Is an account ever required to play, or is anonymous play permanent? | [ADR-0036](ADR-0036-an-account-is-offered-never-required.md) — never required; anonymous play stays fully ranked, and the client offers an account after the player's **first win**, dismissible permanently. **The human's call.** `EPIC-04` gains one prompt story and gates nothing |
| DEC-029 | May a player delete their account, and what becomes of the opponent's history rows? | [ADR-0039](ADR-0039-v01-offers-no-account-deletion.md) — no deletion in v0.1, recorded as a position rather than left implied. **The human's call.** `STORY-0403`'s schema must keep both answers reachable, which forbids denormalising a display name into `duel_result` — the constraint with a real cost |
| DEC-030 | After a credential is attached, may the device id still sign in? | [ADR-0037](ADR-0037-the-device-is-a-credential-until-revoked.md) — yes, until the player revokes it from the account screens; revoking does not end the revoking session, and is offered only once a credential exists. **The human's call**, framed as risk acceptance. The account screens must state which routes are live. Amends `ADR-0030` |
| DEC-031 | May a player ever sign in with a third-party account, or is a handle and password the only credential? | [ADR-0041](ADR-0041-a-handle-and-a-password-are-the-only-credential.md) — handle and password only for v0.1 and v0.2, and the account screens are designed for one credential. **The human's call.** Explicitly *not now* rather than *never*: `ADR-0027`'s `credential.kind` is untouched, so adding a provider stays additive in the schema and costs a screen redesign |
| DEC-036 | How does an unattended run report its status to the human, on what schedule, and what makes a report survive the agent that would have written it? | [ADR-0042](ADR-0042-the-run-reports-itself-every-two-hours.md) — Telegram over stdlib Python; reports composed from **repository state** rather than the agent's memory, so a dead agent is not a missing report; an in-session two-hourly cron; the stop report carried by a `Stop` hook. **The human's call** on all three open points, taken 2026-08-15. The cost recorded rather than discovered: the heartbeat dies when Claude Code is quit, which `STORY-1104` exists to reverse in one command |
| DEC-037 | After a `Rejected`, is the decision point still open on the client, and when does a rejection stop being shown? | [`ADR-0043`](ADR-0043-a-rejection-closes-no-decision-point.md) — a `Rejected` closes nothing: the reducer keeps `pendingTurn`, clears `rejection` on the next `YourTurn`/`Snapshot`/`DuelFinished`, and counts refusals in a new `rejectionCount` so the bar's existing remount key lifts its in-flight lock. The server is **not** changed and `YourTurn` keeps meaning *a new decision point opened*, because `guard` proves the identity the client already holds is still valid after a rejection. The cost recorded rather than discovered: `DuelState` gains its first field the server did not send, and the remount discards the amount the player had dialled. Five files exceed one schema-2 ticket, so `TASK-030712` is the store half and the bar half is a sibling the planner files |
| DEC-023 | How does a rematch reach the server from a browser, and which epic owns the server half? | [`ADR-0044`](ADR-0044-a-rematch-is-one-intent-and-one-room-fact.md) — one intent in and one room fact out: `ClientMessage.OfferRematch` (a `data object`, naming no room — the socket's `RoomMembership` does) and `ServerMessage.RematchOffered(seat)`, delivered to **both** seats, idempotent on a repeat offer, and restated after a reconnect's frames so an offer made while the opponent was away is not lost. There is **no** started frame: the rematch's opening `Snapshot` is the start, and `docs/protocol.md` gains the rule that a `Snapshot` after a `DuelFinished` is a new duel. Refusals collapse to `UNKNOWN_ROOM` (the room is gone) and a new, transient `REMATCH_UNAVAILABLE` (not yet — the duel is running or its result is still being recorded). `PROTOCOL_VERSION` moves one step, taking the next number free when it lands (`ADR-0028` §8) — three unlanded bumps now share that rule. The server half is **`EPIC-02`'s**, as `STORY-0213`, which reopens the epic; `EPIC-03` writes no Kotlin. The cost recorded rather than discovered: a closed epic's metrics ledger stops covering every ticket under it, and the "a `Snapshot` after a `DuelFinished` is the rematch" rule lives in prose rather than in the type system |
| DEC-038 | Which epic ships `ADR-0028`'s server half — `OpponentPresence`, `SeatPresence`, `ActedForAbsentSeat`, none of which exist in Kotlin, in `docs/protocol.md` or in `protocol.gen.ts` — does it take a `PROTOCOL_VERSION` step of its own, and does its client half re-open `STORY-0310` or become its own story? | [`ADR-0045`](ADR-0045-presence-belongs-to-the-table.md) — **`EPIC-02`**, as `STORY-0214`, by `ADR-0044`'s argument applied to the case that ADR named and declined to file; `STORY-0208` stays `done` rather than being reopened, because its criteria are what the epic's first close was measured against. **Its own version step**, and `ADR-0028` §8's rule gains mechanics: the bump is the story's last ticket, the number is read from `develop` plus one at that moment and never written down in advance, and **at most one protocol-bumping branch is open at a time** — `STORY-0213` first, then `STORY-0214`, then `STORY-0405`. The client half is **its own story**, `STORY-0313` under `EPIC-03`, because four of `ADR-0028` §5's five emission points reach the player who *stayed*, at the table — presence is a table feature reconnect observes once. The costs recorded rather than discovered: two branches both moving 2 → 3 merge clean and green, so the version lock has no CI gate; `EPIC-03` gains a story it cannot unblock; `EPIC-02` gains a second story outside its metrics ledger; and `STORY-0214`'s first ticket must delete a passing `TASK-020806` test that `ADR-0028` deliberately retracted |
| DEC-039 | What words does a player read for the three presence states — away, timed out, back — and for an action the server took on an absent seat's behalf? (Minted and answered in the same change: it had lived as `STORY-0313`'s `## Open input` section rather than as a numbered row, so it never appeared in an open table) | [`ADR-0046`](ADR-0046-the-table-says-away-timed-out-and-back.md) — the seat plate says `Away` and `Timed out` and nothing for `PRESENT`; the line that explains them says `Your rival is away. The duel is paused.`, `Your rival did not come back. The duel continues, and the server acts for them.` and `Your rival is back.` — that last one **only** when this client previously held `AWAY` or `ABSENT`, because a resuming client is always sent `PRESENT` and its rival never left. The countdown carries no word of its own and **nothing a player reads changes when it reaches zero**. An action the server took names the server as the subject: `The server folded for your rival.` / `The server checked for you.` — never the rival as actor, never `auto-fold`, never a cause the server cannot see (`disconnected`, `left`, `quit`), never the cash-game `sitting out`. Derived from the vision's *Positioning* sentence; `ADR-0028` had reserved these words to the human, and the ADR says so rather than assuming. The costs recorded rather than discovered: `Timed out` collides with any future per-action turn clock; the mark has no home in a client that renders no action log; the return line needs the store to remember what it was last told; and `the server acts for them` teaches a player how to beat an absent seat |
| DEC-040 | **The architect's** — what mechanism holds the `PROTOCOL_VERSION` lock, so that two branches cannot both claim the same number and both merge green? | [`ADR-0047`](ADR-0047-a-protocol-version-is-claimed-in-a-ledger.md) — a **claim ledger**, `docs/protocol-versions.md`: one row per version naming a 16-hex fingerprint of the wire shape that number means, so two branches appending a row for the same number **conflict textually** and git refuses the second merge before any check runs (verified with `git merge-file`: the constant merges clean at exit 0, the ledger exits 1 with both claims in the markers). One JUnit test in `:poker-server:check` — no new CI job, no Gradle task, no network, no production code — asserts versions ascend by one, the last row is `PROTOCOL_VERSION`, and its fingerprint equals the live descriptors', so **every** wrong conflict resolution fails and an unversioned wire change fails too. Deliberately no command that writes the row: one that regenerates the ledger is one that overwrites another branch's claim. Rejected: a check against `origin/develop` (unsound without `strict = true`, since GitHub does not re-run a PR when the base moves, and unrunnable in an agent worktree with no `origin/develop`) and branch protection (`TASK-000102` already measured `strict = false` and chose it). The costs recorded rather than discovered: a bump now costs a hand-written row and a deliberate red-then-green cycle; **every** wire-shape change now forces a version bump with no escape hatch; only the last row is verifiable; and `Int`/`Long` both project to `number`, so what escapes the fingerprint is exactly what escapes `verifyProtocolTypes`. Extends `ADR-0045` §4 to a five-artifact bump commit |
| DEC-041 | **The architect's** — what shape does revoking the device binding take in the schema, given `ADR-0030` §2 says `player.device_id` is never rewritten by any identity operation? | [`ADR-0049`](ADR-0049-a-device-binding-is-a-row-and-revoking-is-final.md) — **the edge moves out of `player` into its own `device_binding(device_id, player_id, bound_at, revoked_at)` table**, keyed by the natural pair, with two partial unique indexes (`WHERE revoked_at IS NULL`) that are simultaneously the two invariants and the two hot reads: one live binding per device, one per player. Revocation is one `UPDATE ... SET revoked_at = now() WHERE player_id = ?` against a table that is not the ledger, so `player` is byte-identical across it and **no fourth writer of `player` appears** — `ADR-0030` §2's sentence is retired by being made vacuous, because `player` has no device column left to rewrite, and `resolve`'s upsert degrades to a plain `INSERT`. Revoking is **final**: an `ADR-0029`-shaped trigger refuses `revoked_at → NULL`, and the composite primary key makes a device returning to the profile it left a key violation rather than a rule someone must remember; a revoked device may still mint a **fresh** profile, so a resold phone is not bricked. `DELETE /api/me/device` requires a session (`401` otherwise — the only way to honour "revoking does not kill the revoking session"), answers `409` for a player with no credential and `204` either way otherwise. **Sessions and sockets are untouched**, which is not the same question as expiry. The costs recorded rather than discovered: revocation cannot be undone by anyone, ever; a token already on the revoked device stays valid up to thirty days (`DEC-045`); `resolve` stops being one atomic upsert and its mint must roll back whole, or a race litters orphan profiles; and the column drop, `SchemaConstraintsTest` and ten `INSERT INTO player` fixtures across seven test files land in one unsplittable PR |
| DEC-043 | **The product owner's** — what may a password be (minimum, maximum, composition, breach check), and is v0.1 shippable with no strength rule at all? | [`ADR-0048`](ADR-0048-a-password-has-one-rule-and-it-is-length.md) — **one rule: 8 to 128 code points**, counted after normalisation, and nothing else about a password is examined. No composition rule, no character rule, no dictionary, no strength meter, no expiry, no rule that a new password differ from the old — written as a decision with reasons rather than left as an omission, because a password is never displayed, never compared and never indexed, so `ADR-0029` §3's grounds for refusing characters have no analogue here. Nothing is trimmed. **The minimum is a sign-up/reset rule and never runs at sign-in**, or raising it would lock out every account below it; **the maximum runs wherever a secret is hashed, including sign-in**, before Argon2 and before the identifier lookup, because `ADR-0027` §1's four slots bound the work only if the input is bounded. **NFC before hashing, never NFKC, applied in one place** — the single irreversible commitment here, since changing the bytes hashed invalidates every stored hash and `ADR-0031` leaves an opted-out account with no reset to fall back on. Sign-up answers **`422`**, empty body, distinct from the handle's `400` and `409`, matching the `422` `ADR-0031` §5 already promised for a policy that did not exist. **Derived from the vision**, not the human's call: *"Not real-money gambling"* beside *"a counter of duels won"* fixes the proportion, and *"Lichess … not PokerStars. Dark, quiet, fast, minimal"* fills the gap. A **breach corpus is deliberately deferred, not rejected** — it is the only rule that would refuse `password1`, it costs a third-party call on the sign-up path, and it stays additive forever. The costs recorded rather than discovered: v0.1 accepts `password1`, `12345678` and a password equal to the player's own handle; `ADR-0027` §6's address-keyed budget does not slow a distributed guesser per account; and nothing is stored about a password but its hash, so a corpus added later can only check an existing account at sign-in |
| DEC-042 | **The architect's** — by what path does an operator force-rename a profile, and where do the blocklist and the retired-name set live? | [`ADR-0051`](ADR-0051-a-name-is-registered-before-it-is-held.md) — **one table is the whole namespace, and the operator's path is one function in it.** `name_registry(name PK, reason, retired_from, created_at)` holds names in use, blocked names and retired names as three values of `reason` behind one unique index on `ADR-0029` §1's ICU fold; `player.display_name` gains a foreign key into it, so a name cannot be held that was never registered. **A string never leaves that table** — a takedown promotes `TAKEN → RETIRED` and deletes nothing — which is why `ADR-0038`'s three sources of truth collapse into one `INSERT` and the `READ COMMITTED` race between a claim's subquery snapshot and the unique index's physical state **cannot arise**: the claimer conflicts with the row whatever its reason is, and never waits for a takedown to finish. Setting a name becomes two statements in one transaction (`INSERT` the registry row, then `ADR-0029` §5's `UPDATE`), and **anything but one row from the second rolls back**, or a refused claim burns a string nobody holds. `SetNameResult` gains no case: blocked, retired and taken are all `409`. `ADR-0029` §4's permanence trigger gains **exactly one** exception — `name → NULL`, and only when that name is already `RETIRED` — so a rename is still refused for everybody including the operator, and a name can only be given up by being spent in the same transaction; scoping the exception by role was rejected because it protects nothing a `DISABLE TRIGGER` does not already give away. The path is **`retire_display_name(player_id, expected_name)` called from `psql`**, not an endpoint (whatever authenticates it is a role with one member), not a Gradle task (same credentials, same accident, more moving parts), not a hand-written procedure (nothing tests a document); the second argument is an interlock that turns the wrong-database accident into a loud error. The blocklist is rows, curated in place, never seeded by a migration, matched as exact strings under the same fold — and **screening is a set-time event**: a name already held is never re-screened, and the registry cannot even express *blocked but still displayed*. The migration backfills one `TAKEN` row per named player and writes no `player` row. The costs recorded rather than discovered: an operator's mistake is permanent and the victim cannot reclaim their own name; a takedown rewrites every history row that named them, because `ADR-0039` forbade denormalising it; `player` gains a fourth writer where `ADR-0049` §7 kept three; seven test files change and `PostgresProfileWritesConcurrencyTest`'s `pg_stat_activity` filter silently names the wrong statement; and v0.1 ships an empty blocklist. Raises `DEC-046` |
| DEC-046 | **The product owner's** — is a player told that their display name was taken away, and if so by what and in what words? | [`ADR-0052`](ADR-0052-a-takedown-is-told-to-the-player-it-happened-to.md) — **told, on the surface where a name is set, in four sentences, and nobody else is told anything.** The notice appears when the player holds no name *and* a name has been retired from them, and it reads: *"Your display name was removed. A person running Poker Duels removed it — not a bug, and not another player. That name cannot be used again, by you or by anyone. Choose a new one whenever you like."* Each sentence is there because its absence produces one specific wrong belief; the four are the shipped strings. **No reason is given**, because `ADR-0051` records no actor, no reason and no log — so the player is told that a person did this and is given no way to find out why or to contest it, which is stated as the cost rather than dressed up. **The telling is derived from state, never delivered**: no notification, no *seen* flag, no dismissal, nothing to acknowledge, and it ends when the player sets a new name — which they may do immediately, with no hold and no cooling-off (`ADR-0038`'s *"asked to choose again"*). **No column and no table**: `ADR-0051` §1's `retired_from` already records the fact, so this amends §6's silence default and §1's *"nothing in production reads it"*, and one boolean about the caller — never a string — crosses the wire; the shape is `DEC-047`, the architect's. **To the player the two `null` states are distinguishable; to everyone else they are not** — a history row for an opponent whose name was retired is byte-identical to one for an opponent who never set a name, which is a criterion rather than an omission. **Derived from the vision**, not the human's call: *"we are not going to pretend otherwise"* beside *"Lichess … Dark, quiet, fast, minimal"*, read with `ADR-0029` §5's shipped rule that the player is **told** the true state of their own name. `STORY-0410` gains one read-path fact and two-fixture criteria and **nothing** on the write path; `STORY-0411` gains a fourth state plus two copy corrections — `409` becomes *"That name is not available. Try another."* (never *taken*, which for the one player most likely to trigger it is false), and the permanence line becomes *"A name is chosen once. You cannot change it later, and it can be taken away."* The costs recorded rather than discovered: the mistaken victim now has the mistake confirmed to their face with no remedy; the notice raises *why?* and the product has no second screen and no inbox; a player who never opens the name surface is still never told, because there is no push; `GET /api/me` pays for a rare state on every call; the copy makes any future un-retire a lie to people who already read it; and an operator can no longer take a name away quietly |
| DEC-045 | **The product owner's** — does revoking a device also end every other session that player holds ("sign out everywhere"), or are the two separate affordances on the account screen? | [`ADR-0050`](ADR-0050-revoking-the-device-signs-out-everywhere-but-here.md) — **one button, and it means both**: `DELETE /api/me/device` keeps `ADR-0049` §5's route, verb, guards and `204`, and gains one statement in the same transaction — `DELETE FROM auth_session WHERE player_id = ? AND token_hash <> ?`, unconditional, whether or not a binding row was updated, served by the index `ADR-0027` §2 already built. The excluded row is the caller's own, found by hashing the token they presented and never named in a body, so `ADR-0037`'s *"revocation does not kill the revoking session"* holds by construction — which is why the screen says **"everywhere except here"** and never "everywhere". One action, labelled with the permanent half (*Stop this device signing in*), offered only while `deviceRouteLive`, behind one confirmation stating three facts: it cannot be undone, you are signed out on every other device but stay signed in here, and your password becomes the only way back. No count of sessions and no list — that is the devices screen `ADR-0027` §2 declined and `ADR-0049` §9 refused. **Derived from the vision**, not the human's call: *"Lichess and Chess.com, not PokerStars. Dark, quiet, fast, minimal"* — two buttons differ by the difference between a *route* and a *token*, and the one whose name matches the emergency is the one that leaves the token alive for thirty days. `ADR-0049`'s *"`STORY-0406` ships under either answer"* is **corrected**: the answer arrives before the story is split, so the `DELETE` and its two-token criterion land with the endpoint rather than after it; `STORY-0412` gains the confirmation and no new server fact. No schema change, no migration, no route, no `ProfileResponse` field, no `PROTOCOL_VERSION` step. The costs recorded rather than discovered: a player who has **already** revoked can end no session at all, since the action is gone and no change-password endpoint exists; a cheap repeatable act now costs an irreversible one; every other session dies with no way to spare one; a duel already running on a swept device plays to its end, because a socket re-reads no session; and `ADR-0049` §2's *"it is the whole transaction"* becomes two writes against two tables. Amends `ADR-0049` §2 and §6 |
| DEC-044 | **The architect's** — the day the Argon2 cost is raised, what happens to rows written under the old parameters? `ADR-0027` §1's *"a constant change plus a rehash on next successful verify"* needs a parser that accepts other parameters; `STORY-0403`'s parser refuses everything but ours, because one that accepts `m=8` is a downgrade attack in a helper function | [`ADR-0054`](ADR-0054-a-raised-argon2-cost-is-a-ledger-entry-and-a-rehash.md) — **a closed, append-only ledger of the cost parameter sets this project has shipped, and a rehash on the next successful sign-in.** `parseArgon2PhcOrNull` keeps its whole-string comparison — one literal becomes *n*, and the strings are generated by the same `section()` the encoder uses, so the allow list cannot describe a string the encoder could not have written and there is still **no number parser on the verify path** for an attacker-supplied string to fool. What refuses a downgrade is **not** a comparison with the current cost — every historical entry is weaker than current by definition, which is what raising means — but a fixed `ARGON2_FLOOR` equal to the first set ever shipped, which never moves: appending `m=8` makes it *current* and turns `Argon2PhcEncodeTest`'s pinned literals red, inserting it breaks the strict ascent in `m × t`, and **prepending** it — the case an ascent check alone happily allows — is below the floor. `m` may never decrease, so memory hardness cannot be traded for passes at an equal product; `p` is pinned at 1 rather than ordered, since more lanes is not stronger. The rehash lives in **`PostgresCredentials.verify`** — not in `matches` (`Argon2Hasher` has no `DataSource` and must not acquire one) and not in the endpoint (`ADR-0027` §1 forbids a hash leaving `duels.poker.server.db`) — as one compare-and-set `UPDATE` on the exact string read, whose every `SQLException` is swallowed, because **a verification that succeeded must never be turned into a failure by an optimisation**. The dual-parameter path is rejected on both readings (it either *is* the permissive parser spelled twice, or it pays two Argon2 operations for every wrong password and lets a string stop being authoritative about its own tag); the **forced reset** is rejected on blast radius — `ADR-0031` makes recovery optional with *"no recovery path at all"* if declined, `ADR-0039` ships no deletion, `ADR-0051` retires a name forever — and is kept as the last resort, whose day is **the product owner's call**. **Nothing is built now**: with one entry the floor, ascent and round-trip tests are vacuous. The trigger is the first edit to `ARGON2_MEMORY_KIB`/`ITERATIONS`/`PARALLELISM`, already enforced by `Argon2PhcEncodeTest`'s literal strings, and §8 lists the eight things that day's one PR contains — including re-minting `DUMMY_PHC`, which otherwise degrades `ADR-0027` §6's enumeration defence in **two** different ways with every test still green, and re-deriving `ARGON2_MAX_PARALLEL`, since `Argon2ConcurrencyTest` proves the bound is honoured and never that it is affordable. No migration, no schema change, no `PROTOCOL_VERSION` step. The costs recorded rather than discovered: `Credentials.verify` stops being a read while its signature still says `PlayerId?`, so a caller who wraps it in a read-only transaction or a retry loop is wrong with no compiler to say so; the weakest parameters ever shipped stay acceptable forever, in source, under a green test asserting they are accepted — the exact shape a downgrade attack would take, with one constant telling them apart; the raise protects **active accounts only**, so a dump taken the day after yields every dormant row at the old cost; an affected sign-in does two Argon2 operations on a dispatcher sized for one, sequentially (peak memory does not double — occupancy does), for a window with no end date; and the `DUMMY_PHC` mitigation depends on a person reading §5 on the day, which is the weakest link and is recorded as such. Amends `ADR-0027` §1 |
| DEC-047 | **The architect's** — by what shape does `GET /api/me` carry the fact that a name has been retired from the requesting player? | [`ADR-0053`](ADR-0053-the-profile-says-the-name-was-removed.md) — **`ProfileResponse` gains `displayNameRemoved: Boolean`**, non-null and **with no default value** (`ContentNegotiation`'s `Json` has `encodeDefaults = false` while `protocolJson` has it `true`, so a defaulted field is present in every test and absent from the wire for the ~100% of players whose answer is `false`). It is `true` **iff** the caller holds no display name **and** a name has been retired from them — so the two `null` states are distinguishable to the player, and the bit **goes quiet** when they set a new name, which is `ADR-0052` §8's *"gone when it is not"* made a property of the response rather than of client rendering. Named for the product's word (`removed`, `ADR-0052` §2's copy), not the schema's (`RETIRED`). Computed by **one correlated `EXISTS`** inside the existing profile `SELECT` — **one round trip, never a `LEFT JOIN`**, because a player may hold two retired names and a join would return two rows for one profile — correlated to `p.id` rather than to a second bind parameter, so *"this bit is about the player this row is about"* is a property of the statement's shape. **The leak is closed structurally**: `retired_from` names a player id and `RECENT_DUELS_SQL` already holds the **opponent's** `player` row, so `DuelSummaryResponse` gains nothing, `retired_from` may appear in exactly one main source file, and the uncorrelated form — `EXISTS (SELECT 1 FROM name_registry WHERE reason = 'RETIRED')`, which makes every caller read `true` once anybody is retired — is caught only by two fixtures **sharing one database**, which becomes a criterion. `ADR-0051` §8's migration gains **one partial index**, `name_registry_retired_from_idx ... WHERE retired_from IS NOT NULL`, which costs the hot write path nothing (a `TAKEN` row is not in it) and is one line today against an immutable `V<n>` tomorrow. **`PROTOCOL_VERSION` does not move**: the ledger fingerprint hashes `protocolDeclarations()`, whose roots are `ClientMessage`/`ServerMessage` only, and `ProfileResponse` is reachable from neither — `docs/protocol.md` gains a row instead, which `HttpEndpointDocumentationTest` does **not** enforce (documented ⇒ exists, not the reverse). `PostgresProfileWrites` passes the literal `false`, since `NameSet` describes a player who holds a name. **Amends `ADR-0051` §1's *"No read path exposes this table"*** — that sentence is retired as an absolute and replaced by *one read path derives one boolean about the caller*; every other refusal in §1 survives verbatim. Costs recorded rather than discovered: a semijoin on the hottest read forever; a third index on a table `ADR-0051` already flagged for having two, earning nothing until the registry is large and unremovable once merged; the structural refusal spent as an absolute; the wire deliberately unable to say *this player was once moderated*, so any future feature addressing a renamed victim needs a second field; the impossible pair (`displayName` set with `displayNameRemoved: true`) prevented by a SQL string and not by the type; and `ProfileResponse` on its way to being a bag of independent flags |
| DEC-048 | **The architect's** — is `POST /api/auth/sign-up` budgeted by remote address, and what does over budget answer? | [`ADR-0055`](ADR-0055-sign-up-is-budgeted-by-address-and-over-budget-says-so.md) — **budgeted, by remote address, and over budget answers `429` rather than borrowing a lie.** The budget meters **the hash, not the request**: at most 5 requests per address per rolling 15 minutes reach `Credentials.create`, and the four refusals that cost no Argon2 (`401`, `400`, `422`, the guard's `409`) consume nothing. That is a change of *predicate*, not of mechanism — `ADR-0022` and `ADR-0027` §6 meter failure because they defend a **search space**; this meters spending because it defends a **rate**. Reading the code sharpened the question: `PostgresCredentials.create` hashes *before* the insert discovers a collision, so of `ADR-0048` §6's six outcomes only `201` and the **taken-handle `409`** cost a hash — and the success path is self-limiting (`holdsCredential` caps one device id at one sign-up ever) while the taken-handle path writes no row, so the player still holds nothing and **one device id plus one known handle is an unbounded stream of Argon2**. On the oracle: a `429` is a genuine disclosure and ships knowingly, because it is strictly weaker than the `409` `ADR-0031` §5 already spends to confirm a handle exists; because the check sits **last**, so it **cannot be read without being spent**; and because `ADR-0027` §6's non-oracular rule protects a search space, where patience defeats a budget, whereas **pacing under a rate budget is compliance, not evasion**. The four lies are refused by name — `422` sends a player to weaken a good password, `409` permanently poisons free handles they will never retry, and `401` risks the profile abandonment `ADR-0027` §5 documents. No `Retry-After` and no body. Keyed by **address alone**: device id and player are diluted by free minting, and the pair is a *conjunction*, so it is strictly more restrictive and helps the NAT case not at all. **Nothing new protects the pool** — `ARGON2_MAX_PARALLEL` bounds memory, the endpoint budget bounds arrival rate, and a second pool doubles memory against *"a small host"* while a depth-capped queue is deferred **on a measurement, not an argument**. `STORY-0404` is **unchanged** and its three "rate limiting is out of scope" notes stand verbatim; `STORY-0405` builds `AttemptBudget`, both config values and `ADR-0027` §6's sign-in budget on one type — noting that *no limiter exists in this codebase at all* today, `ADR-0022`'s included, so this is the first one written rather than a reuse. The due date is corrected from a story boundary to a condition: **no deployment may expose the endpoint without it.** The costs recorded rather than discovered: five requests deny sign-up to a whole NAT for fifteen minutes, paid by a new player at the moment they are most likely to leave; the `429` is precedent every later endpoint will cite, so the sentence to carry forward is *"a limiter may answer `429` when the endpoint's refusals are already informative"* and not *"limiters answer `429`"*; the budget map is attacker-influenced memory given **no hard cap**, because failing closed is `ADR-0022`'s rejected global lockout and failing open is a bypass; **successful** sign-ins stay unmetered on the same four slots, so this is not a bound on total Argon2 load; the four cheap refusals are free forever; and `STORY-0404` merges an unlimited endpoint whose only protection is that nothing is deployed — which **no test can fail for**. Extends `ADR-0048` §6 with a seventh row. Raises `DEC-049` |
| DEC-049 | **The product owner's** — when sign-up answers `429` (`ADR-0055` §3), what is the player told and what does the form do? | [`ADR-0056`](ADR-0056-a-throttled-sign-up-says-so-and-keeps-what-was-typed.md) — **told that it is temporary and about the connection rather than about them, and the form keeps everything it was holding.** `429` becomes a **third** kind of outcome on the sign-up form — beside success and a refusal about something they typed — and **never** the generic *something went wrong*: a `500`, a `503`, a timeout and a rejected `fetch` keep the `unavailable` treatment `web-client/src/profile/api.ts` already gives an unknown status, and no other status joins the throttled state. That mapping is the substance, because the wrong answer here is the **absence of a branch**: the existing client maps everything but `200`/`401` to *unavailable*, and under that default a deliberate refusal reads as a broken product and produces the immediate retry that — per `ADR-0055` §1's *"an over-budget request still counts"* — **extends the very window it fails to explain**. The copy may state three facts (it cannot be completed *right now, from this connection*; **nothing they typed was refused**; nothing is lost and nothing is required — they keep the profile, the coins and the duel, and may sign up later, per `ADR-0036`) and may state five things never: **no time** — no deadline, no countdown, no *15 minutes*, no *5 attempts*, because `ADR-0055` §3 ships no `Retry-After`, the window is rolling, and the numbers are environment variables that go stale silently; **no verdict on a field**; **no claim that the handle is still free**, since the budget check sits before `Credentials.create` and the taken-handle test was never reached; **no accusation**; and **no mechanism and no fault** — no *rate limit*, *throttle*, *blocked*, *security*, no error code. The form **keeps both fields, the password included, exactly as typed** — the refusal was not about the secret, and `ADR-0048` §3 permits a password no human can retype — marks neither field, leaves submit enabled, offers no *Retry now*, and **never retries by itself**, because an unattended retry is the one action that lengthens the wait. **One message for two people, written for the bystander**: nothing distinguishes the second player behind a NAT from the one who spent the budget and nothing will — the key is the address and the body is empty — so the tie is broken toward the person who did nothing, and the product will never tell an abuser to stop. A `429` spends nothing: no handle, no session, and **not `ADR-0036`'s offer**, which only *"Not now"* dismisses. **Derived from the vision**, not the human's call: *"Lichess and Chess.com, not PokerStars. Dark, quiet, fast, minimal"* — a refusal a player cannot act on is told once, quietly, with no countdown and no alarm — read beside `ADR-0036`'s *"declining does not degrade anything"*. `STORY-0404` gains **nothing** and its three out-of-scope notes stand verbatim a second time; `STORY-0405` gains **nothing on the wire and one prohibition** — the `429` body stays empty by product decision as well as transport decision, so nobody adds a `message` or a `retryAfterSeconds` to help the screen; `STORY-0412` gains the state, the mapping, the preserved fields, the copy brief and five criteria, including that the four statuses are asserted **together** and that the rendered message **contains no digit**; `STORY-0415` gains one prohibition. The costs recorded rather than discovered: `ADR-0055`'s disclosure gets **louder**, since a status a person had to open devtools to see is now printed in a sentence on a shared address; the player is told to wait and never how long, and *later* can be an hour, with the operator's two config values the only lever; a typed password sits in a form on exactly the shared machine that produced the refusal, for the length of the wait; the only deterrent a message could carry is given up; a state that may never render in v0.1 is designed, tested and maintained from the day it ships; and the client cannot tell this `429` from one `EPIC-07`'s front end invents for a reason of its own. Forecloses a countdown on this screen unless `Retry-After` ever ships, folding `429` into the generic state, and any per-field treatment of it later |
| DEC-050 | **The architect's** — by what mechanism is a duel-history cursor bound to the filter that produced it, so that replaying it under another filter is refused rather than silently reinterpreted? The answer had to settle a third payload component (fingerprint or literal text), the canonical form of a `DuelFilter`, whether the filter reaches `duelCursorOrNull` or the route, whether a mismatch is distinguishable from a malformed cursor, and what a later third axis does to cursors in flight | [`ADR-0057`](ADR-0057-a-cursor-names-the-filter-it-was-drawn-under.md) — **the payload gains a third component and it is an 11-character fingerprint of the filter, checked by the re-encode line that already decides validity.** The framing that settles the rest: a cursor names a position in an ordering of a set, the set is `(player, filter)`, and **the filter is the only part of it the client supplies** — the player comes from the resolved credential, so replay cannot vary it, and the ordering is a constant in the SQL. So `limit`, the player and the ordering stay out of the fingerprint, and only the filter goes in. **The canonical form of a `DuelFilter` is the parsed value**, never the raw query string: `duelOutcomeOrNull` yields an enum entry or refuses, `opponentSearchOrNull` yields NFC, so two spellings cannot mint two cursors and no separate canonicalisation step exists. Its text is one length-delimited line per axis that *narrows* — `"<axis>:<utf8ByteLength>:<value>\n"`, `outcome` then `opponent` — and an absent axis contributes **nothing**, which is what makes a third axis leave every existing cursor byte-identical and widening additive in fact rather than in intention. The fingerprint is the first 8 bytes of SHA-256 as unpadded base64url (`DuelFilter.NONE` → `47DEQpj8HBQ`); base64url rather than hex **because 64 + 12 = 76 keeps `theEncodedFormCarriesNoPadding` load-bearing while 64 + 17 = 81 is a multiple of three and makes it vacuous**. Both halves of the encoding take the filter as a **parameter** — `encoded(filter)` and `duelCursorOrNull(raw, filter)`, non-nullable because `DuelFilter.NONE` is a filter — so **no new refusal path is added**: the check is the same `it.encoded(filter) == raw` line, the third part is never parsed, and validity keeps living in one function rather than being a second opinion a route can forget. `DuelCursor` stays `(finishedAt, duelId)` and never learns what an axis is; `respondWithDuels`'s order becomes identity, limit, **filter, cursor**. A mismatch is a **flat `400`, indistinguishable from a malformed cursor** — the remedy is identical, a retryable status would be a lie, and this endpoint's whole refusal vocabulary is one status and no body. The fingerprint is unkeyed and is a **consistency check, never an authorisation check**: `TASK-040801`'s *opaque, not unforgeable* stands, because a forged cursor names only the forger's own rows — the day a cursor names a set the requester does not own, that is a keyed MAC and a new ADR. **Nothing in `STORY-0409`'s eleven tickets changes**: `TASK-040909`'s "write no test about that combination, in either direction" is confirmed, `TASK-040906`/`TASK-040910` stand verbatim, and `TASK-040911` writes a gap sentence a re-plan ticket deletes on purpose. The costs recorded rather than discovered: every cursor in flight on the day it deploys is refused, and **no version negotiates the encoding** (`RecentDuelsResponse` is reachable from neither message root), so this is payable early or awkwardly, never gracefully — hence the deadline is *before `STORY-0413` ships*, not before `STORY-0409` closes; a mismatch and a mangled string are indistinguishable to the developer debugging them, with no body and no log; `DuelCursorTest`'s padding fixture must be re-cut or an assertion silently stops catching anything; `DuelFilter`'s rendering becomes a compatibility surface enforced by nothing but §8 and **one golden vector test**; two SHA-256s per page; and somebody will eventually read the digest as a signature. Forecloses resuming a walk under a *widened* filter, and any shareable page-position link |
| DEC-051 | **The product owner's** — what does the client print where a display name would be, for a player who has none: on their own profile strip, and as an opponent on a duel line? | [`ADR-0058`](ADR-0058-where-a-name-would-be-the-client-prints-no-name.md) — **`No name`**, the same two words on every surface, about every player, whatever the reason there is no name. Sentence case, one space, no full stop and no mark; printed by one function (`nameOrNone`) that is the only place in the client branching on a `null` display name, because [`ADR-0052`](ADR-0052-a-takedown-is-told-to-the-player-it-happened-to.md) §5's invisibility survives only if two surfaces cannot decide separately. Explicitly **one treatment everywhere and no second-person variant**: the player's own strip prints `No name`, not *You*, so that `STORY-0413`'s history rows and `EPIC-05`'s leaderboard rows — both third-person — inherit an answer that works there. Derived from the vision: the v0.1 roadmap row *"No accounts"* makes a nameless player the ordinary shipped state rather than an unfinished one, and *"Lichess … dark, quiet, fast, minimal"* fixes the register as a plain statement about the record rather than a mascot name, a prompt or a dash. Rejected: *Anonymous* (asserts a choice that is false for the player whose name was taken away, and is the exact placeholder `ADR-0029` §6 forbids the server to mint), *You*/*Your rival* (warmer on the two surfaces asked about, nonsense on the two that inherit it), a dash (reads as missing data, not as an ordinary player), *Unnamed* (describes the person, not the record) and a prompt (*yet* is a promise, and false for a removed name). Costs recorded rather than discovered: a history list of nameless rivals cannot be scanned; the string is registrable as a display name, since `ADR-0029` §3 refuses invisible characters and not ordinary words and `ADR-0051` §5 ships the blocklist empty; and *No name* echoes the strip's neighbouring *"No profile yet."* |
| DEC-052 | **The product owner's** — when does the history search send its request: **as the player types**, or **when they submit it**? | [`ADR-0059`](ADR-0059-the-record-is-searched-when-the-player-submits.md) — **on submit; typing sends nothing.** The box sits in a `<form>` and exactly two acts search: Enter in the box, and a submit button reading `SEARCH = "Search"` — the one word `TASK-041305` reserved for the ticket that needs it. Emptying the box is a search like any other (`filter.opponent = ""`, which `duelsPath` renders as no `opponent` parameter), and **no separate *Clear* control** is added for a state submitting already reaches. Derived from the vision's *"Dark, quiet, fast, minimal"*, read against what a search actually costs on this screen: `ADR-0057` binds the cursor to its filter and `TASK-041307` drops the rows with it, so **a debounced pause is not a wasted request — it is the player's place in the record.** Three pages into a walk, one mid-word pause restarts the walk and searches a term (*Hal*) the player had not finished typing (*Halvard*). It would also fire one unindexed `POSITION` scan per pause against a read `STORY-0409` recorded as *"correct without one; not yet fast"*, and put the first timer **inside** the component tree — the client's only `setTimeout` today is the reconnect backoff, outside React by `ADR-0032`. Chosen as the cheaper reversal on thin evidence: a debounce added later is **additive**, a debounce removed later is a behaviour players already learned, and nothing here is measured — no players, and no p95 for the filtered read at any size. `TASK-041312` gains exactly four things — one Scope bullet, the word, the test `asks nothing while the player types, and once when the search is submitted`, one `verify:` line — and **stays at three files**, because finding the button by its accessible name pins the string without a golden test of its own. Costs recorded rather than discovered: the box and the rows can disagree with nothing on screen saying so (no *showing results for…* line, deliberately); widening back takes a deliberate submit rather than happening by itself; and it feels older than every other search box the player used today |
| DEC-053 | **The product owner's** — how does a player reach their whole duel record, and how do they leave it? | [`ADR-0060`](ADR-0060-the-record-is-its-own-screen-and-the-lobby-is-the-door.md) — **its own screen, which replaces the first screen: in by one control reading *Your duels*, out by one reading *Back*.** The record is never rendered beside the lobby — not below the strip, not in a panel. The door sits beneath the profile strip and **outside** it, is a `<button>` rather than an `<a>` with no address to carry, and takes its word from `HISTORY_HEADING` so the destination has exactly one spelling in the product. **It does not depend on the read**: the strip renders `null` when its background read fails, and the way to a screen may not vanish because of a failure the player cannot act on (`ADR-0036` — it gates on nothing else either). *Back* is rendered by whatever renders the swap, **not by `HistoryScreen`**, so the screen knows nothing about navigation and the affordance is assertable with no transport. The door is offered only on the branch that holds *Create a duel room* — never at the table, never on the result screen — and **a duel that starts outranks the record**, because a player who opens it mid-hand does not fold — a `PLAYING` room is never reaped for idleness, so they leave their rival at a table nothing will end. Derived from three vision sentences: v0.2's *"match history"* makes the record a commitment already made, so only placement was open; *"slot machines in the lobby"* is the vision naming accretion on the first screen as the shape it refuses; and the first success condition — *"Send a link. She opens it in a browser… We hit Rematch."* — is what that screen is for. The deciding force was inheritance: one extra section on the lobby is cheap, four are the whole product stacked on the screen a new player sees first — reversible one at a time, irreversible in aggregate. `TASK-041313` gains the door, the way back and the test `leaves the first screen for the record, and comes back to it`, asserted through controls that exist whatever the read answers; its *"exactly one of `App.tsx` and `Lobby.tsx`"* criterion is satisfied by `Lobby`, which is where the knowledge that a duel is in progress already lives. Costs recorded rather than discovered: the first screen becomes the **only** door, so every later screen is reachable from there or not at all, and the create button ends up sharing its screen with a growing column of links; **the record has no address** — nothing links to it, a reload lands on the first screen, and the browser's *Back* leaves the client instead of returning; `STORY-0415`'s offer fires on the result screen and opens `STORY-0412`'s, so whatever holds *which screen is showing* must be reachable from there too; and it forecloses the glance, since nothing on the first screen will ever show more than the strip's handful. **Raises `DEC-054`**, the architect's, deliberately unanswered here |
| DEC-055 | **The product owner's** — what is a season, and what does one do to a duel coin? Four parts: what bounds one; whether a boundary resets `player.coin_balance` or makes a season a window over `duel_result.coin_delta`; whether a finished season stays readable; and whether the ladder shows one season or also all time | [`ADR-0061`](ADR-0061-a-season-is-a-calendar-month-and-the-coin-never-resets.md) — **a season is one calendar month in UTC, derived and never stored; the ladder is a window over it; a boundary does nothing, and `player.coin_balance` is never reset.** Derived from the vision, which licenses it in two sentences that pull opposite ways: *"a counter of duels won"* that is *"not a balance"* is why nothing is destroyed at a boundary, and *"Ranked results over a season"* is why the ladder is scoped rather than lifetime — both are literally true at once under exactly one shape, an all-time counter ranked over a window, and the roadmap's *"Leaderboard and **seasons**"*, plural, is why v0.3 ships a season that ends. Bounds are **half-open** `[first instant of the month, first instant of the next)`, identifier `2026-08`, and a duel belongs to the season containing its **`finished_at`** — so a duel finishing exactly on a boundary is in the **new** season. **No table, no column, no migration, no operator, no job**: *which season is it* is a function of `ServerClock`, *which season was that duel in* a function of `finished_at`. A standing is the `SUM(coin_delta)` inside the window, and the ladder is **results, not players** — a row exists exactly for those who finished a duel that season, including at `0` for a draw, which is `ADR-0015` doing work it was not doing before. The ladder shows the **current season only** (no all-time ladder, no selector), prints the season standing rather than the strip's number, **names the season it shows** — `August 2026` — and takes that name **from the response**, never from the browser's clock (`ADR-0002`). A finished season is never gone, because it recomputes exactly from rows nothing rewrites; v0.3 simply offers no way to ask for one. `ADR-0014` is **neither superseded nor amended** — the word *supersede* there was reserved for a balance that *floats*, and a season standing is the same arithmetic over a shorter window. The reset branch was **declined for want of authority, not on the merits**: it would make the vision's *What it is* false and belongs to the human, and the ADR records that the objection is meaning rather than recoverability. Costs recorded rather than discovered: the ladder **empties on the first of every month**; **nothing records who won a season**; the strip's number and the ladder's number disagree from the second season on, so `EPIC-05`'s *"a ladder that disagrees with the strip is the defect this epic exists to avoid"* is contradicted on purpose and both stories carrying it are rewritten in the same change; the read becomes an aggregate over a join with no index; a UTC boundary meets locale-rendered times, so a duel read as September can count in August; and past standings are **recomputed, not frozen**, so `ADR-0039`'s eventual deletion will silently edit finished seasons. **Drops `STORY-0505`** — the boundary it was written to run at does nothing — and **raises `DEC-060`** and **`DEC-061`** |
| DEC-056 | **The product owner's** — what, if anything, gates a place on the leaderboard, given that device ids are free and an account may **not** be required? | [`ADR-0063`](ADR-0063-nothing-gates-a-place-and-the-farm-is-accepted-until-the-ladder-is-public.md) — **nothing gates a place, and the farming vector is accepted out loud until the ladder is public.** Derived from the vision; the human did not state this call. The ladder lists exactly the set [`ADR-0061`](ADR-0061-a-season-is-a-calendar-month-and-the-coin-never-resets.md) §4 defines — a row for whoever finished at least one duel in the season — **narrowed by nothing**: no minimum duels, no minimum standing, no account, no name, no profile age, no opponent-diversity rule, so `STORY-0502` adds no `WHERE` beyond the season window and `STORY-0503` filters no row it was sent. **A nameless player has a row and it prints `nameOrNone`'s `No name`** — the question [`ADR-0058`](ADR-0058-where-a-name-would-be-the-client-prints-no-name.md) parked here by name, answered in the listed direction, with the wire still carrying `null`. **Two profiles that only ever duel each other are an ordinary pair**, because that pair is the vision's founding case — *"the author wanted to play quick heads-up duels against his sister"* — and no server fact separates it from a farm. Two properties are pinned so a later gate cannot break them quietly: a rank is a position among **everyone** who played that season, and a season's standings **sum to exactly zero**. The decisive finding is that a farmed duel is not merely certain but **fast** — two colluding profiles shove all-in and finish in under a minute against `duel-rules.md`'s honest *"20–45 hands, roughly 5–15 minutes"* — so any rule counting duels is cleared by the farmer first and taxes the honest player instead. `ADR-0012`'s gate is **discharged rather than amended**: it is applied at the event that ADR names, *"when the leaderboard goes public"*, which `EPIC-05`'s out-of-scope table had already relocated to `EPIC-07`. The acceptance expires at an **event, not a date** — the first time the ladder is served on a public address — or earlier if a season ends with a standing shaped like a farm, which is a signal to look and never a rule. Costs recorded rather than discovered: the farm stays possible **on purpose** and the first ladder shown can be topped by somebody who never beat anybody; `No name` can be the top row and a ladder of them cannot be read; a nameless player cannot find themselves, which makes `DEC-059` load-bearing rather than a nicety; a place gate is now more expensive to add than it was to refuse, because a later one takes places away; and the top of the ladder is noise for the first days of every season. **Names one ticket** for the planner — `EPIC-07`'s definition of done gains a line requiring this acceptance to be re-affirmed in writing or replaced before the ladder goes public — and **raises no `DEC`**: the countermeasure's shape is a decision for the day the acceptance expires |
| DEC-057 | **The product owner's** — does a leaderboard row lead anywhere: is another player's profile visible to a stranger, and what is on it? | [`ADR-0067`](ADR-0067-a-leaderboard-row-is-text-and-no-id-turns-into-a-profile.md) — **a leaderboard row is text, it leads nowhere, and no id turns into a profile.** Answered by the product owner on 2026-08-22, derived from the vision: the **roadmap** settles the *whether* — v0.3 is *"Leaderboard and seasons"* and v0.4 is *"Friends, statistics, replay viewer"*, so a page about another player carrying duels played, a win/loss record or a duel list is **statistics**, one milestone later; deciding that a v0.4 thing waits is applying the roadmap, and deciding it moves would be reordering it, which is the human's. *"**A leaderboard.** Ranked results over a season"* settles the *what*: the vision names **ranked results**, not ranked people — `ADR-0061` §4's *"results, not players"* — and a result is discharged by a row. **A row is a rank, a name or `No name`, and a season standing, rendered as one line** (`4 Ada 3`): not a link, not a button, not a control, which **confirms what `STORY-0503` already ships** rather than changing it — `TASK-050313`'s *no `<a>`, no `link` role, no `button`* assertion stops being provisional. **What a stranger reads is exactly four fields** — `rank`, `displayName`, `coins`, `playerId` — and every one already ships; what is new is that this is the **boundary**. **What a stranger does not read, field by field:** the all-time `coin_balance`, duels played, wins, losses, draws, a win rate, the ladder total, the duel list (`GET /api/me/duels` gains no player parameter and no second route), streaks, movement, last seen, online state, anything about a credential, handle, email, device or session, and `displayNameRemoved`, which already *"never says anything about another player"* (`ADR-0053`). **`playerId` stays on the wire and is a name for a row, never an address**: no route takes a player id as its subject — the server serves five paths and not one answers *what about player X* — while a client may still use it as a list key and to correlate what it was already told (`ADR-0021`). That is **`ADR-0029` §7 from the other side**: §7 keeps a name from becoming an id, this keeps an id from becoming a person, and together **no path in this product turns anything into a lookup of a player**. **`STORY-0504` is `dropped`** — its premise is false and nothing moves out of it. Reopening is deliberately made a decision rather than a ticket: a new `DEC` naming **one field at a time**, an answer to what the subject is asked and can turn off on a product with no opt-in (`ADR-0063` §1), no opt-out and no deletion (`ADR-0039`) whose default player never signed up for anything (`ADR-0012`, `ADR-0036`) — **and the human's call before any fact a player did not choose to publish is published**, which is where a product decision starts having consequences outside the software. No escalation was needed for *this* answer because it publishes nothing new. Costs recorded rather than discovered: **the ladder is a list of strangers a player cannot ask about** — the gesture every ladder trains you to make does nothing, and the vision's own *rival* has nowhere to point, a real loss against the Lichess comparison; **`DEC-054` loses its sharpest argument**, since `EPIC-05`'s case for it was *"a row that leads to another player is a link"*, so an address-less client just got more comfortable; the question is **deferred and gets harder**, arriving in v0.4 as a ticket saying *just add a player page*, guarded only by prose; the asymmetry is deliberate and uncomfortable — a player is published on a ladder they never opted into and can look up nobody, while the server holds all of it; and **v0.3 ships no social surface at all**. **Forecloses nothing structurally** — that is the point — but forecloses inside `EPIC-05` even the zero-disclosure head-to-head view, which is v0.4's row. Leaves `DEC-054`, `DEC-060` and `DEC-008` untouched, **names no ticket and raises no `DEC`** |
| DEC-058 | **The product owner's** — when two players hold the same balance, what is the player told: do they share one rank number, or does something break the tie into distinct positions, and if so what? | [`ADR-0064`](ADR-0064-tied-players-share-one-rank-and-row-order-is-not-a-ranking.md) — **tied players share one rank number, and the order rows sit in is not a ranking.** Answered by the product owner on 2026-08-21, derived from the vision: *"**A leaderboard.** Ranked results over a season"* ranks a **result**, and two equal standings are one result. The rank is **`1 + the number of players standing strictly higher`** — competition ranking, so the ladder prints `3, 3, 5` and never `3, 4, 5` and never `3, 3, 4` — and its meaning is a sentence a player can state: *there are exactly `rank − 1` players ahead of me this season*. **A rank and a position in a page are two numbers and only the rank is shown**: a page may begin with the rank the previous page ended on, and a repeated rank across two pages is not a duplicate row, because `STORY-0502`'s totality and disjointness are about **players**. Nothing on the screen breaks a tie, and a tie is marked by the repeated number and by nothing else in v0.3 — no `=`, no count, no styling. The emission order among equals is **the architect's** (`DEC-061`) and constrained in one product-facing way: the key is a fact about **who a row is** — id, name collation, profile age — never about **how that player did**, because a tiebreak that measures play is a second ranking rule and [`ADR-0014`](ADR-0014-duel-coin-economy.md) reserves one for an ADR that *supersedes* it. Costs recorded rather than discovered: **the rank stops being free** — it is a function of the whole ladder rather than of the page, so `DEC-061` gets harder and a keyset cursor cannot carry a position forward as a rank; the ladder **cannot name a leader for the first days of every month**, twelve times a year, which makes `DEC-059` load-bearing rather than a nicety; a skip from `5` to `195` reads as missing rows and nothing on the screen explains it; a season can end with **no single winner**, which narrows `DEC-060`'s options; every future *top N* is ill-defined; and the size of a tie is invisible. **Sharpens `DEC-059` and `DEC-060` without answering either, constrains `DEC-061` without answering it, raises no `DEC`** — whether a tie is ever marked is a string in the ladder's text module and an ordinary ticket if it is ever wanted |
| DEC-059 | **The product owner's** — does a player see **their own** standing, and where: on the profile strip beside the coin balance, marked on the ladder, behind a *jump to me* control, or nowhere? | [`ADR-0065`](ADR-0065-the-ladder-hands-a-player-their-own-row.md) — **the ladder hands a player their own row, and the profile strip keeps the all-time coin.** Answered by the product owner on 2026-08-21, derived from the vision: *"**A leaderboard.** Ranked results over a season"* ranks a result **belonging to a player**, and a ladder whose own player cannot locate their result ranks it for everybody except the person who produced it; *"**One duel coin per win.** Not chips, not currency, not a balance. A counter of duels won"* is why the strip keeps printing that counter and gains nothing. The **ladder screen** renders one **self line** above the rows and below the season name, stating the requesting player's rank and season standing — rendered whether or not their row is in the page on screen, and unchanged as they walk pages. **The profile strip is untouched**: no rank, no season standing, no season name, and `ProfileResponse` gains no field, so `GET /api/me` never becomes a whole-ladder aggregate on a route that runs on every lobby load. Which number each surface shows is pinned: strip = all-time `player.coin_balance`, ladder rows **and** self line = the season `SUM(coin_delta)`, told apart by [`ADR-0061`](ADR-0061-a-season-is-a-calendar-month-and-the-coin-never-resets.md) §6's season name and by [`ADR-0060`](ADR-0060-the-record-is-its-own-screen-and-the-lobby-is-the-door.md) never putting the two on one screen. **`STORY-0502` therefore ships two aggregates in one response** — the page, and one player's rank, for a player who may be on no page it drew — with the mechanism left to `DEC-061`. Three states and the third is not a zero: a rank and a standing; **no place this season** for a profile that played no duel in it, printing no rank and never `0`, because `0` is a real standing a draw earns ([`ADR-0015`](ADR-0015-a-draw-writes-two-result-rows.md)); and no line at all for a request with no known device — and **the page is byte-identical in all three**, so the ladder stays readable without a profile and is never personalised. **Nothing else marks the player in v0.3**: no highlighted row, no *jump to me*, no ladder total, no movement, no tie count, no link. Costs recorded rather than discovered: the ladder read **stops being one query** and `DEC-061` gets harder for the second time in a day; the lobby still does not say where you stand, so it is a click every time on a screen `ADR-0060` said would crowd; the self line may disagree with the same player's row in the same response, which §3 permits on purpose; **the ladder response is now per-requester** and can no longer be cached as one public document, a bill `EPIC-07` pays; the two-player founding ladder gets a line restating a row beneath it; and telling a player they are rank `5` makes *which of these 190 rows is mine* a sharper question that §5 deliberately does not answer. **Constrains `DEC-061` further without answering it**, leaves `DEC-057` and `DEC-060` untouched, **names no ticket and raises no `DEC`** |
| DEC-061 | **The architect's** — is a season standing computed per request or materialised, and what does a page guarantee over an ordering that is **recomputed** while it is walked? | [`ADR-0066`](ADR-0066-the-ladder-is-computed-per-request-and-a-walk-is-pinned.md) — **the ladder is computed per request, and a walk is pinned to the instant it began.** Answered by the architect on 2026-08-21. **Per request, from the ledger, and nothing stores a standing**: no `season_standing` table, no materialised view, no summary column, no cache, no refresh job, no third ticker sweep and no change to the duel-recording transaction — `ADR-0061` §3's *"nothing writes a season down, so nothing can disagree about one"* applied one level down, which is also what keeps a duel on the ladder **the instant it commits**. The read is one `WITH standing AS (SUM(coin_delta) … )` over the season window with SQL's `rank()`, which **is** `ADR-0064` §1's competition rank. **The page guarantee is bought rather than inherited**: a request with no cursor mints `asOf = Instant.now(clock)`, the cursor carries it back, and the query's upper bound is **the cutoff, never the season's end** — so a walk enumerates *the ladder as it stood committed at the cutoff*, returning **every player of that ladder exactly once**, with the rank each held then, so ranks never decrease down a walk and the self standing (`ADR-0065` §3) is byte-identical on every page. **Two refusals are stated rather than glossed:** a walk is **not live** — a duel finishing after the cutoff is in no page of it, and page forty is as old as page one; and exactly-once has **one named exception** — a duel *committed* after a page was drawn but stamped `finished_at` *before* the cutoff moves the pinned ladder, so its **winner can be never returned** and its **loser returned twice**. Both are accepted; what the cutoff buys is that their window is the width of one duel-recording transaction rather than of the walk. The order is `coins DESC, player_id DESC` — `player.id` is **forced**, not chosen, as the only key that is identity (`ADR-0064` §4), unique and **immutable** (a `display_name` takedown would move a row mid-walk) — paged by one row-value comparison, the `DUELS_AFTER_SQL` idiom, with the `limit + 1` probe row. The page and the self standing are **two statements bounded by the same cutoff**, which agree without a transaction. A cursor whose `asOf` is outside the season the server's clock is in is a flat `400` (`ADR-0057` §5), so a walk crossing a month boundary restarts rather than serving August in September; and `ADR-0057` §7's *"a leaderboard page"* MAC clause is shown **not to engage**, because `ADR-0065` §4 makes the page identical for every reader so a forged position leaks nothing. Costs recorded rather than discovered: **every page is a full-month aggregate and there are two per page**, so a twenty-page walk is forty unindexed passes on a public unauthenticated read with no rate limit — the sharpest cost, and unmeasured; later pages are **stale** with nothing on screen saying so; **exactly-once carries its exception permanently in the contract**, so a reader can see a row twice or miss one; a walk is refused twelve times a year at the month boundary; the cursor now carries a value the server trusts from the client; and a **live walk is foreclosed**. **Unblocks `STORY-0502`, the last gate on it**, and leaves `DEC-057` and `DEC-060` untouched. **Names one ticket** and raises no `DEC`: an index for the season window — `duel (finished_at)` in a new `V7__` migration with an `EXPLAIN`-backed measurement, deliberately outside `STORY-0502` |
| DEC-062 | **The architect's** — which instrument does the server read **wall-clock** time from, when a piece of product behaviour is a function of the calendar? | [`ADR-0062`](ADR-0062-two-clocks-and-a-date-comes-from-java-time-clock.md) — **the server has two clocks and neither answers the other's question.** `ServerClock` (`System.nanoTime()`) measures **durations** — timeouts, grace windows, `ADR-0025`'s sweeps — and `java.time.Clock` reports **dates**: injected as a parameter, `Clock.systemUTC()` in production, `Clock.fixed` in a test. **No new port and no third clock type** — the wall clock is `java.time.Clock` itself, which `PostgresDuelResultSink` already takes and whose KDoc already gave the reason. `Instant.ofEpochMilli(clock.nowMillis())` is a defect wherever it appears; the no-argument `Instant.now()`, `System.currentTimeMillis()`, `LocalDate.now()`, `YearMonth.now()` and `ZoneId.systemDefault()` appear nowhere in `poker-server/src/main`; there is exactly **one** `Clock.systemUTC()`, at the composition root; a **pure** function takes the clock with **no default**, because a defaulted pure function can be called with no clock at all; and the zone is a literal `ZoneOffset.UTC`, never the clock's own. **Amends three merged ADRs in one clause each**, per this file's convention that a correction goes in the status line and *supersede* is for a decision reversed: `ADR-0061` §3 (*which season is it* is a function of the instant an injected `java.time.Clock` reports, not of `ServerClock.nowMillis()`), and — found by searching for the sentence rather than the id — `ADR-0027` §1's thirty-day session and `ADR-0031`'s twenty-four-hour and one-hour tokens, whose lifetimes land in an `expires_at TIMESTAMPTZ` compared against SQL `now()` and would each have been **born expired in 1970**. The durations are policy and do not move; both ADRs' in-memory rate-limit windows keep `ServerClock` and are right as written. `STORY-0501`, `STORY-0405` and `STORY-0502` carried the same instruction and are corrected too. `ServerClock` **keeps its name**: the rename to `ElapsedClock.elapsedMillis()` is argued in full and declined for now — eleven Kotlin files that must land atomically past a three-file ticket cap, and seven merged ADRs and fourteen merged tickets left naming a type that would no longer exist — with the trigger written down and made checkable: all four occurrences predate this ADR and came from documents that now say the opposite, so **one more, in any document written after it**, makes the name the cause and the rename a story on this argument. The cost is named rather than discovered: **the misleading name survives, so the mistake stays expressible**, and the deterrent is a KDoc, an architecture section and a static-read guard test, none of which can see a misused injected clock. Unblocks `TASK-050106`; names three tickets — the KDoc that still points at `System.currentTimeMillis`, the composition root's single `Clock.systemUTC()`, and the guard test |
